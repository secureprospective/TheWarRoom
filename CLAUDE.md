# TheWarRoom — Project CLAUDE.md
**Version:** 2.0 — July 2026
**Project path:** `/mnt/storage/claudebox/projects/TheWarRoom/`
**Pillars:** Business, Technical

---

## What This Project Is

A 32-team dynasty fantasy football ranking engine and full-stack desktop application for the Legacy NFL league. Six-layer scoring engine (Go), Wails v2 desktop shell, React + Tailwind + Zustand frontend, SQLite WAL storage, MFL API integration.

**Three-phase delivery:** Personal tool (Phase 1, current) → League-wide alpha (Phase 2) → Public beta (Phase 3).

---

## Current Build State (July 2026)

**🎬 FILM CALIBRATION (Thread C) PLANNED + README REFRESHED (2026-07-20, main `c3519b6`; docs-only, no branch — linear-allowed).** Planning-only per Christopher (no code). New `docs/roadmap/FILM_Calibration_Planning.md` (`5a65307`): the four film-feed fetchers (madden/nflproduction/pfrcoverage/veteranfilm) all EXIST + sources LOCKED, blend weights UNSET by design. Program C-0→C-4: reachability re-check → live distribution sample → 2–3 candidate weight schemes → **expert-panel + Christopher decision gate** → wire `Coverage`/CB-S first (smallest blast radius). Prereq refactor: promote `GSISForPFR` into `crosswalk.Map` (pfrcoverage = 3rd ad-hoc pfr→gsis consumer). Locked rails: durability NEVER weights, quality only as a calibrated fidelity discount, Madden a promoted regulator (α≈0.20 seat), zero-leak absolute (CFBD success-rate/havoc NOT PPA; NFLProduction capped). K film already set (0.60/0.40, DECISION-011) — only B5b-K mechanics remain. **README refreshed (`c3519b6`, GLM-drafted on Beelink / Claude reviewed+applied):** Layer-4 scouting stated wired end-to-end offense+IDP + crosswalk-once; prior FILM overclaim softened. Pushed to GitHub. **➡️ NEXT: execute FILM Thread C — C-0/C-1 (reachability + live distributions) → weight scheme via expert-panel gate; NO blind weights.** OR M2/M4 refactors (43/44) OR calendar OR M2 slice-2. Handoff 48. — Thread B + S-Phase 4b status below:

**🔬 SCOUTING WIRE-UP — Thread B (crosswalk consolidation) + S-Phase 4b (IDP `BreakoutAge`) BOTH MERGED to main (2026-07-20; Thread B `af70a5a`, S-Phase 4b `971ae50`; each FF-only off main. Two live Beelink functional gates PASSED ("pass" each); GLM review WAIVED — GLM down, same posture as S-Phase 1–4).** Two threads from handoff `47`, in the recommended order. **CLAUDE WROTE BOTH directly** (GLM down); S-Phase 4b's threshold went through the **planning-first Christopher-decision gate** (AskUserQuestion) after a LIVE distribution sample. **Thread B (`af70a5a`, mechanical/behavior-preserving):** the MFL→gsis crosswalk was re-fetched inside each assembler — BuildRAS + BuildCollegeShare + BuildCollegeDefense + BuildBreakoutAge = FOUR ~1 MB pulls per Score League (BuildSchoolTier joins on college NAME, never used it, so it was 4 not 5); birthdates fetched inside BuildBreakoutAge. Now `m1_scouting.buildScoutingDirectory` fetches the crosswalk ONCE (the one fail-loud crosswalk boundary) and threads the `crosswalk.Map` into every signal; `mergeCFBDScouting` fetches birthdates ONCE (behind the CFBD-key gate — only breakout uses them) and threads the `agetrajectory` map down. Four assembler signatures now take `crosswalk.Map` instead of a `crosswalkURL`; BuildBreakoutAge also takes the pre-fetched ages map. Behavior-preserving (same joins, fail-loud now at the single upstream fetch); test fixtures adjusted via new `crosswalkFixture` + `baBirthFixture` helpers. Set up 4b to reuse the maps rather than add a 5th/6th pull. **S-Phase 4b (`971ae50`, IDP BreakoutAge):** the defensive clone of S-Phase 4 — each rostered CB/S/LB/DT/DE's raw age at his EARLIEST college season crossing a dominator line on the AVERAGED defensive collapse share (`collapseCollegeDefense`: CB=mean(PD,INT), S=mean(INT,Tk), LB=mean(Tk,Sk,TFL), DT/DE=mean(TFL,Sk)) → same `Profile.BreakoutAge` slot + per-position §4 breakout curve. **THE METHOD (Christopher-decided 2026-07-20, planning-first):** threshold = **0.12, a SINGLE cross-position line**, grounded in a LIVE sample of 25,088 college defensive player-seasons (2021–23, via a throwaway sampler) — the offense 0.20 line marked only ~1–3% of defenders (averaged defensive shares sit far below offense yardage shares); 0.12 = the "featured defender" bar (~top 4–7%, the shoulder of every position blend's distribution — the defensive analog to what 0.20 means on offense); the CB/S/LB/edge blends run within ~0.02 of each other at the top, so ONE threshold avoids overfitting thin data (rejected the handoff's per-position guess). **What landed:** new `internal/scouting/assembly/breakoutage_idp.go` `BuildBreakoutAgeIDP` (fetches only the 6 seasons of the defensive feed; crosswalk + birthdate maps SUPPLIED by the caller — Thread B's single fetch REUSED, zero new pulls; reuses shared `breakoutAgeYears` + `BreakoutThresholdIDP=0.12`). **DISJOINT from offense by construction:** `collapseCollegeDefense` returns ok=false for offense/K/FLAG, so an offense player scanned here is skipped and the shared slot never clobbers (same disjointness proof as CollegeShare vs CollegeDefense). `m1_scouting.mergeBreakoutAgeIDP` threads the already-fetched cw+ages; `constants.go` WIRING STATUS updated (IDP BreakoutAge now WIRED). New multi-season IDP test (earliest-crossing LB@2020/CB@2021, never-cross DE, offense-WR skipped, fail-loud, bad-key, guards). Automated gates GREEN on CT105 (build · `go test -race ./...` 43 pkgs · `make lint` 0 · pre-commit hooks); both live Beelink gates PASSED; GLM WAIVED. **➡️ NEXT: FILM calibration pass (Thread C — decision-gated, do NOT ship blind film weights) OR crosswalk-adjacent consolidation is now DONE OR M2/M4 refactors (handoffs 43/44) OR the league-calendar branch.** Handoff `47` (its Thread A + B now both done; Thread C = FILM remains). ⚠️ ROTATE the free CFBD key at full beta. — S-Phase 4 status below:

**🏁 SCOUTING FILM ENGINE COMPLETE — FILM Thread C C-4 steps 1–3 MERGED + LIVE; step 4 (K5) DATA-BLOCKED → DEFERRED (OQ-016) (2026-07-21).** **Step 3 `OffenseFilm` (K3) MERGED to main (`4e4a0ef`; base wire `7529132` + GLM-fix `4e4a0ef`; FF off `a335de6`). Live Beelink functional gate PASSED ("pass" — QB/RB/WR/TE now film-differentiate, no longer neutral 1.000). GLM 5.2 blind review RAN (back up) — 0 HIGH** (bird hung headless → killed → re-run on Beelink via scripted-prompt + `EXIT=` sentinel; opencode at `~/.opencode/bin`, source `zai.env`). **What landed:** new `internal/scouting/assembly/offensefilm.go` `BuildOffenseFilm` — the universal Madden offense **backbone** (equal-weight mean of each QB/RB/WR/TE position's curated Madden sub-attrs from its rubric Madden-mapping table, `/99`, via the shared `averageTerms` extracted from `idpfilm.go`) + a **bounded FTN delta-overlay**: above the charting floor a player's charted quality is percentile-ranked within his role's rostered population, and `delta = 0.85·(percentile − backbone)` is CLAMPED to **±0.10** (the panel's floor-crosser discontinuity cap), `composite = clamp01(backbone + delta)`; below the floor OR a singleton role → pure backbone (no peers → inert overlay). `ftnRoleQuality` collapses FTN rates (QB = 1−intWorthy; RB/WR/TE = mean(contested, created, 1−drop); throwaway EXCLUDED, sign-ambiguous). QB/RB/WR/TE HARD boundary; roster-id dedup. `scouting.OffenseFilm` gained `Composite float64`; `rankings.applyScouting` offense branch is an **`else if`** of the IDP branch (position-exclusive → structural, no double-write): `0.95·Composite + 0.05·neutral` (same K2-reserved pattern as defense). `m1_scouting.mergeOffenseFilm` runs UNCONDITIONALLY (EA Madden + nflverse FTN, no CFBD key; FTN season = `SeasonYear−1`, floors = veteranfilm defaults 30/100). qb/rb/wr/te.go film doc comments: "weights unset" → WIRED. **3 Christopher planning-first decisions (AskUserQuestion): overlay bound B=0.10; FTN→[0,1] = percentile-rank within roster; K2 seat reserved NEUTRAL (not wired this step).** **2 GLM-triage rulings: MED-1 backbone = per-rubric-ROW archetype mean (inter-row attr repeats DELIBERATE, NOT flat-union dedup); MED-2 FTN percentile population = Madden-resolved above-floor only (v1).** GLM safe leads applied (singleton→pure backbone, `q` clamp, else-if guard, roster dedup); triaged out LOW-2 (RSP/Sharp fields unread) + LOW-4 (GLM misread RAS-steepness prose as the film const). Tests: composite math + FTN collapse + percentile (midpoint+singleton) + blend (3 regimes+clamp) + BuildOffenseFilm e2e (2 identical-backbone WRs → backbone±0.10 by FTN percentile, QB singleton, DT skipped) + fetch-loud + nil-guards + applyScouting offense blend. Gates GREEN CT105 (build · `go test -race ./...` · `make lint` 0 · pre-commit). **STEP 4 (K5 rookie tower) DATA-BLOCKED → DEFERRED (Christopher, 2026-07-21).** Live recon proved 3 of 4 K5 inputs unobtainable: **Madden-rookie dead** (only populated EA slug m24=2023, no current rookies); **per-player CFBD success-rate/havoc doesn't exist** (both TEAM-level in `stats/season/advanced`; per-player CFBD advanced = `usage` = production share already wired via CollegeShare/BreakoutAge, or `ppa` = points-based = zero-leak REJECT); **consensus-rank manual with no auto-source** (source-map escape hatch already failed). Only combine/RAS available and it's ALREADY wired (rookie-weighted via SL-018). A rookie today carries RAS+SchoolTier+CollegeProductionShare+BreakoutAge; his FILM slot resolves Data-Parity NEUTRAL. Rejected building a hollow tower. **Christopher's call: close the engine now; rookies evaluated on already-wired non-film signals; film slot neutral; consensus-rank manual-CSV loader deferred to OQ-016** (build a `RookieConsensus` signal — `Rank,Player,Position,College` → normalized [0,1] within class → rookie film slot PRIMARY, neutral until supplied, gated on `domain.PlayerRecord.IsRookie`; + pre-ship redundancy check consensus-rank vs RAS r<0.5 — IF/WHEN a class CSV is maintained). Docs-only closure committed to main (`3531622`, linear-allowed): FILM planning doc step-4 status + OQ-016 + numbering. **SCOUTING FILM ENGINE DONE.** One optional remainder (NOT blocking): the **K2 NFLProduction seat** is reserved `0.05·0.50` neutral in BOTH the offense and defense branches of `rankings.applyScouting` — a localized swap-in whenever wired (own small increment; needs the p75-cap population/scope decision). **➡️ NEXT (branch fresh off main): the K2 seat (small) OR M2/M4 refactors (handoffs 43/44) OR the league-calendar branch OR the Versioning & Releases phase (Alpha hard-depends on it — the standing priority).** — S-Phase 4 status below:

**🔬 SCOUTING WIRE-UP — S-Phase 4 (offense `BreakoutAge`) MERGED to main (2026-07-20, main HEAD `71d6394`; FF off `8d4c0ae`. Live Beelink functional gate PASSED ("pass"); GLM review WAIVED — GLM down, same posture as S-Phase 1/2/3).** The FIRST MULTI-season scouting signal — not a single-season clone. Fills `scouting.Profile.BreakoutAge` for the offense skill positions and feeds each position's §4 breakout-age curve. **CLAUDE WROTE THIS ONE directly** (GLM down); the v1-METHOD went through the **planning-first Christopher-decision gate** (AskUserQuestion) BEFORE any code — this signal carries genuine calibration choices, unlike the spec-locked S-Phase 3 clone. **THE METHOD (Christopher-approved 2026-07-20, PROVISIONAL v1 — recalibrate vs live data, [[lesson_inference_vs_calibration]]):** breakout = the EARLIEST college season whose within-team production share crossed **0.20** (classic dynasty dominator line); breakout age = raw age in YEARS at **Sept 1** of that season; **offense-first** (WR/TE → receiving-yard share, RB → rushing-yard share; QB has NO offense breakout source — passing is never fetched, zero-leak — so QB stays absent→neutral; K none; **defense = S-Phase 4b**, its own calibrated threshold since the averaged IDP component-share distribution won't share 0.20); **6-season** scan window (`year-5..year` — covers rookies + recent draftees, older vets fall out → neutral). **What landed (`71d6394`):** new `internal/scouting/assembly/breakoutage.go` `BuildBreakoutAge` — fetches crosswalk + nflverse birthdates (`agetrajectory.Fetch`, gsis-keyed DOB, zero-leak) ONCE and each of the 6 seasons' offense college-share feed, then per rostered player joins gsis→DOB→position, picks the position share selector, scans earliest-first for the 0.20 crossing, derives `(Sept 1 of season − DOB)/365.25`. The multi-season loop lives in the assembly leaf; `collegeshare.Fetch` stays a pure single-year Layer-1 call (its single-year limit is orchestrated-around, NOT rewritten). Missing DOB / never-crossed / non-finite-or-negative age = ordinary skips; ANY season's fetch failure surfaces loudly. Added `Profile.HasBreakoutAge` presence flag (a YOUNG age is a real high-value signal — 0 ≠ absent, same discipline as `HasRAS`/`HasCollegeProductionShare`); `applyScouting` gets a 4th copy block gated on it; WR (0.40) + QB (0.30) engine curves verified to consume `sc.BreakoutAge`/`HasBreakoutAge`. **REFACTOR (file-cap forced, clean seam):** the whole scouting-directory wiring extracted `m1_app.go`→new **`m1_scouting.go`** (m1_app had crossed the 400-line cap) and `buildScoutingDirectory` split into per-signal merge helpers (`mergeSchoolTier`/`mergeCollegeShare`/`mergeCollegeDefense`/`mergeBreakoutAge` under `mergeCFBDScouting`). New multi-season assembly tests (earliest-crossing, position selector, missing-DOB, never-cross, negative-age, fail-loud, nil-guards). Automated gates GREEN on CT105 (build · `go test -race ./...` · `make lint` 0 · pre-commit hooks); live Beelink gate PASSED; GLM template review WAIVED (GLM down). **Leads for later:** crosswalk now fetched a **5th** time per score run (RAS + schooltier + collegeshare + collegedefense + breakoutage) — the deferred assembly-layer crosswalk consolidation is now worth doing (handoff `47`). **➡️ NEXT: S-Phase 4b (IDP BreakoutAge — clone with its own calibrated threshold) OR the crosswalk consolidation OR the FILM calibration pass** (OffenseFilm + IDPFilm/Coverage stay Data-Parity NEUTRAL until the decision-gated pass — do NOT ship blind film weights). Handoff `47`. — S-Phase 3 status below:

**🔬 SCOUTING WIRE-UP — S-Phase 3 (IDP `collegedefense` share) MERGED to main (2026-07-20, main HEAD `ae1593a`; FF off `5dd225e`. Live Beelink functional gate PASSED ("all pass"); GLM review WAIVED — GLM down, same posture as S-Phase 1/2).** The FOURTH scouting signal — the defensive sibling of S-Phase 2, filling the SAME `scouting.Profile.CollegeProductionShare` slot for the five IDP positions (CB/S/LB/DT/DE). **CLAUDE WROTE THIS ONE directly** (GLM down) — head-brain recon → confirmed the combine is spec-locked (NOT a provisional call, unlike offense) → wrote → self-reviewed → gated. **WHY NO DECISION WAS NEEDED (the clean-clone finding):** the `collegedefense` fetcher emits FIVE raw within-team component shares (tackle/sack/TFL/PD/INT), and the IDP rubrics §4 already LOCK which shares each position uses AND that the combine is the equal-weight AVERAGE of the named components (explicit at S "average of… INTs and… tackles" / LB "averaged across the three event classes"; CB/DT/DE strictly parallel): **CB = mean(PassDef, INT); S = mean(INT, Tackle); LB = mean(Tackle, Sack, TFL); DT = mean(TFL, Sack); DE = mean(TFL, Sack).** Assembly emits the RAW average; the engine's per-position §4 normalization curve maps it to its index (parity with offense — `engine.ScoutingInput.CollegeShare` is documented raw). **What landed (`ae1593a`):** new `internal/scouting/assembly/collegedefense.go` `BuildCollegeDefense` + `collapseCollegeDefense` (clone of the offense collapser, joins on the RAS/gsis crosswalk shape, finite-guarded, imports collegedefense/crosswalk + domain/playerid/math — depguard-legal, zero-leak; CFBD PPA never fetched). Merged into `m1_app.buildScoutingDirectory` as a fourth block inside the CFBD-key `if`, **disjoint from the offense CollegeShare loop** — offense collapse is true only for {WR,TE,RB}, defense only for {CB,S,LB,DT,DE}, so the shared slot never clobbers. `applyScouting` already copies `CollegeProductionShare` position-blind → the five defensive rubrics (`internal/engine/l4/defense/*`) consume it with ZERO engine change. Table test pins each position's average + the offense/K skip + fail-loud + nil guards. Also refreshed the `internal/scouting` WIRING STATUS block (RAS/SchoolTier/offense+IDP CollegeShare now WIRED; film/coverage/breakout-age deferred-neutral). Automated gates GREEN on CT105 (build · `go test -race ./...` · `make lint` 0 · pre-commit hooks); live Beelink gate PASSED; GLM template review WAIVED (GLM down). **Minor lead (later refactor):** the crosswalk is now fetched a FOURTH time per score run (BuildRAS + BuildSchoolTier's teams + BuildCollegeShare + BuildCollegeDefense) — acceptable v1 cost; share the fetched crosswalk when the assembly layer is consolidated. **➡️ NEXT: S-Phase 4 = BreakoutAge** (multi-season derivation + resolving `collegeshare.Fetch`'s single-year limit — carries a pin-a-v1-method DECISION), then the FILM components (offense OffenseFilm + IDPFilm/Coverage) which stay Data-Parity NEUTRAL until the decision-gated CALIBRATION pass (do NOT ship blind film weights). Handoff `46`. — S-Phase 2 status below:

**🔬 SCOUTING WIRE-UP — S-Phase 2 (CollegeShare) MERGED to main (2026-07-20, main HEAD `f1e62cc`; FF off `d0d50a8`. Live Beelink functional gate PASSED; GLM review WAIVED — GLM down, same posture as S-Phase 1).** The THIRD scouting signal. **CLAUDE WROTE THIS ONE directly** (GLM down) — head-brain recon → pinned the v1 collapse rule with Christopher (AskUserQuestion, both recommendations approved) → wrote → self-reviewed → gated. **THE CALIBRATION DECISION (this is why it wasn't a pure clone):** the `collegeshare` fetcher emits THREE raw within-team shares (reception / receiving-yard / rushing-yard) but `scouting.Profile.CollegeProductionShare` is ONE monolithic "position-defined" slot. **v1 collapse (Christopher-approved 2026-07-20, PROVISIONAL — real calibration deferred, decision-gated):** WR/TE → `ReceivingYardShare`; RB → `0.70·RushingYardShare + 0.30·ReceivingYardShare`; QB → no source in this feed (carries no passing share) → Data-Parity neutral; every defensive position → the SEPARATE `collegedefense` feed (a later phase), neutral here; K/FLAG → neutral. **What landed (`990f6bd`):** new `internal/scouting/assembly/collegeshare.go` `BuildCollegeShare` — joins on the **RAS shape** (gsis-keyed via crosswalk `cw.GSISForESPN` + `cw.Lookup`), NOT the SchoolTier name-join; fetches crosswalk + CFBD season player-stats, collapses per position via `collapseCollegeShare` (finite-guarded), imports Layer-1 (collegeshare/crosswalk) + domain/playerid/math only (depguard-legal, zero-leak). Added `Profile.HasCollegeProductionShare` presence flag (0 is a real share → no natural absent sentinel, same discipline as `HasRAS`). Merged into the per-player Profile in `m1_app.buildScoutingDirectory` (inside the CFBD-key block, same key+year as school tier). `rankings.scorePlayer` → new `applyScouting` helper (extracted for funlen) copies each present signal per field; CollegeShare gates on `HasCollegeProductionShare`. Automated gates GREEN on CT105 (build · `go test -race ./...` · `make lint` 0); live Beelink gate PASSED; GLM review-gate WAIVED (GLM down — same posture as S-Phase 1). **Minor lead (later refactor):** the crosswalk is fetched twice per score run (BuildRAS + BuildCollegeShare) — acceptable v1 cost; share the fetched crosswalk when the assembly layer is consolidated. **➡️ NEXT after this merges: S-Phase 3 = the IDP `collegedefense` share (sibling of this — fills the same slot for DT/DE/LB/CB/S) and/or BreakoutAge** (multi-season derivation + `collegeshare.Fetch` single-year limit). — S-Phase 0/1 status below:

**🔬 SCOUTING WIRE-UP — S-Phase 0 (RAS) + S-Phase 1 (SchoolTier) MERGED to main (2026-07-20, main HEAD `65bc517`; FF-merge of the stacked `session/scouting-sphase1-schooltier` landed BOTH phases atomically. Live Beelink functional gate PASSED; GLM review WAIVED — GLM down, accepted as a template-clone of the reviewed S-Phase 0).** The SECOND scouting signal, cloning the S-Phase 0 leaf template. **CLAUDE WROTE THIS ONE directly** — the INVERTED model had GLM write S-Phase 0, but its z.ai 5hr window was spent (~92%), and a template-clone needs no GLM budget; Claude did head-brain recon → scope decision → wrote → self-reviewed → gated. **Scope decision (Christopher chose):** of the breakout/college group, do **SchoolTier only** this phase — it's the one field with NO calibration decision (engine `schoolTierNorm`/`HasSchoolTier` already exist → pure plumbing). CollegeShare (3-shares→1 collapse) and BreakoutAge (multi-season derivation) each carry a RAS-style pin-a-v1-method decision → deferred to S-Phase 2/3. AgeTrajectory DROPPED (the `scouting.Profile.AgeTrajectory` field is vestigial — nothing reads it; the engine derives age-trajectory from `spec.Age`). **What landed (`4cac1a5`):** MFL `college` bound in the players fetcher (DETAILS=1, verified live present) → threaded through `normalize.Lookup` (lookupEntry + PlayerFacts) → new `internal/scouting/assembly/schooltier.go` `BuildSchoolTier` (fetches CFBD `/teams`, joins rostered id→college→tier via exact match + a small alias map every target verified against live CFBD 2026, lifting ~94%→~98%+ FBS; imports schooltier/scouting/playerid only — depguard-legal, zero-leak) → merged into the per-player `scouting.Profile` in `m1_app.buildScoutingDirectory` → `rankings.scorePlayer` reads it PER FIELD. **KEY TEMPLATE FIX:** added explicit `Profile.HasRAS` — S-Phase 0's "directory-presence == HasRAS" shortcut does NOT survive a 2nd signal (a school-tier-only player would wrongly read HasRAS=true, RAS=0); RAS now gates on `HasRAS`, SchoolTier on its `SchoolUnset` sentinel. **CFBD key** read at wire time from `CFBD_API_KEY` (present + live-verified on CT105: 683 teams/138 FBS; UNSET → school tier SKIPPED gracefully, RAS still flows; key-present fetch failure → loud — a missing key ≠ a broken fetch). Automated gates GREEN on CT105 (build · `go test -race ./...` · `make lint` 0); combined live Beelink gate PASSED (RAS modulation vs neutral confirmed); GLM review-gate WAIVED (GLM down — accepted as a template-clone). **➡️ NEXT: S-Phase 2 = CollegeShare** (needs the 3-shares→1 collapse rule pinned — a decision), then S-Phase 3 = BreakoutAge (needs a multi-season derivation method + resolving `collegeshare.Fetch`'s single-year limit). — S-Phase 0 status below:

**🔬 SCOUTING WIRE-UP — S-Phase 0 (RAS end-to-end) MERGED to main (2026-07-20, via the sphase1 FF-merge, main HEAD `65bc517`; was branch `session/scouting-sphase0-ras` @ `34f19bc`).** The FIRST scouting signal switched on — the template every later signal clones. **Execution model ran as designed (INVERTED gate):** GLM 5.2 (OpenCode build agent on the Beelink) WROTE the code from the self-contained brief `docs/build-handoffs/scouting/S-Phase-0-RAS-GLM-Brief.md`; Claude was HEAD-BRAIN — pinned the method, reviewed every line vs source, ran the gates, owns the merge. GLM's `git push` failed (Beelink has no GitHub creds); Claude pulled the commit over SSH (`ssh -i /root/.ssh/beelink chris@192.168.1.190`, clone `/home/chris/opencode/TheWarRoom`) and pushed to origin from CT105. **What landed (one clean commit `34f19bc`):** new composition-leaf `internal/scouting/assembly` (`BuildRAS` fetches crosswalk+combine, joins rostered MFL→gsis→RawCombine, `ras_math.go` computes the per-position RAS-equivalent; imports ras/crosswalk/scouting/domain/playerid — NOT engine/stores, depguard-legal, zero-leak); `rankings.ScoutingDirectory` port + `MapScoutingDirectory` (`scoutdir.go`) nil-guarded in `New()`; `scorePlayer` sets `spec.RAS/HasRAS` when a profile is present, else unchanged (L1 imputes `DefaultRASFallback=5.0`); `m1_app.go` `buildRASDirectory` wires it (fresh 90s http.Client, fails loud on fetch error). **The pinned v1 RAS method (Christopher-approved 2026-07-20, PROVISIONAL — real calibration is a deferred, decision-gated pass):** cohort = players-DB position; per-measurable **sample** stddev (n−1) over present values; sign convention (time drills forty/cone/shuttle inverted); `RAS = clamp(5.0 + 2.0·mean(available z), 0, 10)`; σ=0/n<2 → z=0 (present-but-neutral); zero-measurables → absent from map → HasRAS false. **Head-brain review PASSED:** math correct vs brief §3, wiring sound, GLM's hand-computed vector (2-player cohort → 5+2/√2 ≈ 6.414) genuinely pins it. Automated gates GREEN on CT105: `go build ./...` / full `go test -race ./...` / `make lint` 0 (ifaceguard+filelen+golangci). **⚠️ NOT MERGED — live Beelink functional gate PENDING** (a rostered player with a real RAS shows non-neutral RAS modulation vs an absent-RAS player at exactly 1.000; steps staged in `/root/paste.md`). Christopher runs it on the Beelink desktop; merge after. **Minor lead for the calibration pass:** `ras_math.go` uses the one-pass `sumSq/n−mu²` variance (catastrophic-cancellation-prone; negligible at combine magnitudes). **➡️ NEXT = S-Phase 1 (remaining signals: breakout/college → offense film → IDP film → coverage → K film), CLAUDE WRITES THEM DIRECTLY** — GLM's 5hr window is spent (~92%), and cloning the now-proven template is repetitive work needing no GLM budget. Each clones the assembly-leaf shape. FILM weights stay UNSET/Data-Parity-neutral until the decision-gated calibration pass (do NOT ship blind film weights). Handoff `45` + the GLM brief carry the full plan. Memory: [[project_warroom_review_week]], [[project_thewarroom]].

**🧹 SLIM_MAP EXECUTION (tiers 1–3) DONE + MERGED to main (2026-07-20, branch `session/slim-cleanup`; next branch off main).** Executed the SLIM_MAP §7 plan (the review doc `docs/reviews/warroom-fullmap-2026-07/SLIM_MAP.md` now lives on main too — brought over from `session/review-harvest` so its execution + source doc land together). **Tier 1 (dead-code + dedup):** deleted `store/ping.ts` (D1) + dead `mflPerfZ` sort key (D5); de-triplicated `finite()`/`boolToInt()` into a new depguard-safe `internal/numeric` leaf (imports only `math`, so the pure engine can reach it — deleted `engine/finite.go`, cleaned now-unused `math` imports) + `numeric_test.go`; extracted FE `money`/`initials`/`Th`/`Empty` to `transactions/format.tsx` (DUP4). **Tier 2 (bug):** M4 `stage()` in all 3 components now wraps `PreviewTransaction` in try/catch → a thrown IPC call drops into the existing REJECTED modal state (Close, fire hidden) instead of hanging "previewing…" forever (§6.3). **Tier 3 (4 rulings, Christopher 2026-07-20):** #4 DELETED `TransactionsPanel.tsx` (619ln dev panel, superseded by M4 — all 14 ops ported) + `SHOW_DEV_PANEL` wiring (bundle −15KB); #1 DOCUMENTED the scouting scaffolding (WIRING STATUS block in `internal/scouting` package doc + "Deferred / unwired scaffolding" section in `SYSTEM_MAP.md`) — KEEP not carve; #2 (extract M2 service) + #3 (M4 read-model store) STAGED as handoffs, not done. **Tier 4** (L4 ~1200ln consolidation) left as an enterprise-review discussion item, not pre-empted. Gates: frontend tsc+vite clean · golangci-lint 0 + ifaceguard + filelen · `go test -race ./...` green · pre-commit hooks green. Pure refactor + error-path fix → no functional-gate dependency (visible change: B7a dev tab gone). **HANDOFFS STAGED: `43` (M2 service extraction §6.1), `44` (M4 read-model store §6.2), `45` (Scouting Data Integration §4 — the wire-up).** **➡️ NEXT (Christopher): WIRE UP THE SCOUTING SCAFFOLDING — read handoff `45` first. EXECUTION MODEL (locked 2026-07-20): GLM 5.2 does the heavy-lifting code — the wiring is loadable in 100–200k-token chunks at cheap price, so GLM WRITES the fetcher→Profile→PlayerSpec assembly; Claude stays HEAD-BRAIN — reads/reviews GLM's work against source, owns judgment/sequencing/gates/the film-weight calibration decision, triages leads-not-findings. This is INVERTED from the usual GLM-reviews-Claude gate. Start with the RAS end-to-end template (S-Phase 0), planning-first.** The verified gap: `scouting.Profile` is constructed NOWHERE; `rankings.scorePlayer` (rankings.go ~L218-231) leaves every scouting `Has*` false; the missing middle is an assembly layer (fetchers→crosswalk join→normalize→Profile) + a scouting Directory injected into `rankings.Runner`. Memory: [[project_warroom_review_week]], [[project_thewarroom]].

**⭐ UI WIREFRAME TRACK A–E COMPLETE + FF-MERGED to main (`e82f01a`, pushed 2026-07-19/20; accumulation branch `session/ui-session-a-grid` deleted; next branch fresh off main).** All five design-language sessions CONFIRMED at their vision gates (A "flying colors" / B "amazing pass" / C "perfect pass" / D "everything is a pass" / E "everything is a pass"), each via the GLM-5.2 divergent-runs loop (2 divergent seeds + 1 synthesis, Claude head-brain triage leads-not-findings, Christopher vision-gate). The visual spec now lives in `docs/ui/wireframes/session-{a,b,c,d,e}/` + `Command_Ledger.md` (A1–D12 + token contract + the input-normalization mandate) + plan §10 (per-session confirmed decisions). **A** grid/spatial (4-col instrument shell, density tiers, 320px inspector overlay, summon-over comms/calendar); **B** components/type (Inter+JetBrains Mono, delta-in-weight, ConfirmModal 480px hold-to-fire, 4 row states); **C** color/atmosphere (cold CIC navy, semantic ramps, restraint doctrine "color is data+state, structure achromatic", score→hue banding, position badges); **D** event grammar + terminal-log chat + buildable append-only calendar (one row anatomy, escalation→ALERTS tributary, VAV altitude, `/offer` Control Card, drag-move+stepper-resize); **E** mobile learning-harvest (NOT a build — `session-e/Mobile_Handoff.md` survival table + lessons ledger; "Inbox-onto-Oracle"; **fuzzy-input/voice normalize-then-act mandate = why the chat backbone is LLM-backed**). **NEXT = the BUILD TRACK** (B-1 shell/tokens → B-2 module migration → B-3 calendar fully functional → B-4 Home+Inspector → B-5 Alpha hardening → **ALPHA GATE**) — but **Alpha HARD-DEPENDS on the Versioning & Releases phase FIRST** (Christopher-led, ~2026-07-20 — the actual next thing named). Memory: [[project_thewarroom_ui_roadmap]]. — original planning record preserved below:

**🎛️ UI WIREFRAME ROADMAP PLANNED + SESSION 0 PROVEN + MERGED to main (2026-07-19, squash `d9429bc`; next branch off main).** The full UI/UX program is locked in **`docs/ui/Wireframe_Session_Plan.md`** (READ IT before any UI session): design sessions A–E (GLM-5.2 divergent runs + Claude triage + Christopher vision-gates) → build track B-1…B-5 → **ALPHA GATE** (Alpha hard-depends on the Versioning & Releases phase). Christopher's locked rulings all recorded in the plan: authority contract over the locked `UI_Direction_Document.md` (ripcord mechanism), §1.1 chat command backbone (future chat terminal w/ Guix-style language + lightweight LLM; Command Ledger maps every control to a verb; chat NOT built in this roadmap), §1.2 spectrum commitment (snappy <100ms / three altitudes glance-operate-interrogate / presets-not-toggles / NO Pro-Mode switch ever), **calendar FULLY FUNCTIONAL by roadmap end** (Session D + B-3, consumes `session/league-calendar` backend, append-a-revision drag semantics), quick-dash (calendar+chat summon/collapse from any screen), code standards non-negotiable, scope doctrine (cut, don't add). **Session 0 (test) COMPLETE:** `scripts/glm-design-session.sh` proven (Beelink-resident key, detached + sentinel, 40s round trip); worked example at `docs/ui/wireframes/session-0-test/`; 3 research digests committed in `docs/ui/wireframes/`. README rewritten with the Road-to-Alpha section. **OPEN → Christopher: (1) screenshot the Session 0 draft → save as `docs/ui/wireframes/session-0-test/README-hero.png` (README image 404s until then); (2) the Session 0 RIPCORD (comms column → summoned overlay; Claude recommends adopt — matches quick-dash); (3) "go" fires Session A (two divergent grid runs).** Memory: [[project_thewarroom_ui_roadmap]].

**✅ M2 WEEKLY POWER RANKINGS slice-1 (live MFL standings + z-score/MAD blend + sum/top-N toggle) COMPLETE + MERGED to main (2026-07-19, squash `686a574`; next branch off main).** Build_Tracker row 31. **What it is:** a headline power-ranking per franchise = a weighted blend of our forward-looking scouting engine (Σ M1 AdjustedScore per franchise) and MFL's luck-adjusted all-play win% — NOT a reinvented formula (MFL's own `pwr`/`altpwr`/all-play come free in `leagueStandings` and are shown as sortable columns). **New:** Layer-1 `internal/ingestion/leaguestandings` fetcher (clones the rosters template — DiscoverHost-first, MFLList collapse-tolerant, API-error guard, **`SanitizeNumeric` strips `$`/`,` — the leagueStandings `salary` is currency-formatted `"$120.72"`**); pure `internal/powerrankings` blend package; `GetPowerRankings(weight, aggMode)` IPC (`m2_app.go`); React `PowerRankingsBoard.tsx` + store slice + tab. **The math (durable decision, GLM-panel-shaped):** scouting standardized with **median + MAD·1.4826 (robust-z, 50% breakdown — a dynasty super-team/empty roster can't shift the scale)**, all-play with mean/std z (bounded [0,1], doesn't need robust); weighted `w·scoutingZ + (1−w)·perfZ` (default w=0.60, **free 0–100% slider**), then min-max'd to [0,1] for display. Scouting aggregates as roster **SUM or top-N starters** (N = league `Starters.Count`, degrades to sum if unreadable) — **user toggle**. **Shaped by a 3-STAGE blind GLM-5.2 review** (correctness → math-design → fix-verify), each triaged vs source: stage-2 moved off min-max (range≠variance dishonesty + dynasty-outlier fragility) → z-score; stage-3 (92%) → median/MAD for scouting → ship-confident 95%+. Correctness leads adopted: error surfacing + `.ok` empty-state, NaN/Inf rejection at the fetcher boundary, response-race seq guard, drag-safe slider, tie-averaged all-play `(W+0.5T)/G`, non-finite weight fallback. **LIVE FUNCTIONAL GATE PASSED on the Beelink (2026-07-19, ran on `f214d96`): all 4 sub-gates — loads-live (32 franchises, real names, all columns), weight slider re-orders on release, sum/top-N toggle changes ranks, sorting (PA ascending, no NaN cells).** The gate surfaced the `$120.72` salary-format bug → fixed + re-gated. Gate: make lint 0 / `go test -race` green / frontend tsc+vite clean / pre-commit hooks green. Design + full review trail in `docs/modules/M2_Power_Rankings_Design.md`. Beelink clone reset to clean main after. **Deferred to slice-2/Phase-2:** the 5 `weeklyResults` optimal-lineup columns (Bench/Max/Min PF/Coulda-Won/Woulda-Lost) + movement history; early-season low-confidence badge; standings cache so weight/agg toggles re-blend without re-fetching MFL.

**➡️ NEXT PHASE (Christopher, starting 2026-07-20): VERSIONING & RELEASES.** The app is becoming a thing that's USED (M1/M2/M4 UI merged), and Phase-2 league-alpha is the first time the binary leaves this machine — so versioning, reproducible/stamped builds, changelogs, and a schema-migration story stop being optional. **PLANNING-FIRST** (enter plan mode; frame + decide conventions before plumbing). Clean slate today: no `wails.json` productVersion, no `VERSION` file, no git tags, no ldflags version injection. Read `docs/roadmap/Versioning_and_Releases_Planning.md` (7 framed decisions D-V1…D-V7 + recommended first-session sequence) and handoff `40-Versioning-and-Releases.md`. D-V1 (SemVer scheme) is a durable/convention-setting decision → run the expert-panel gate. D-V6 (schema/data migration across releases) is the real risk once a binary ships to a league member → open it as its own thread, don't half-answer it. Other open work independent of this: M2 slice-2, the league-calendar branch ([[project_thewarroom_league_calendar]]), other Build_Tracker items.

**✅ M4 TRANSACTION UI slice-3 (TRADE builder + franchise names + D6 commissioner surfaces + pre-commit DOLLAR BREAKDOWN) COMPLETE + MERGED to main (2026-07-18, squash `6c0db73`; next branch off main).** All FOUR builds landed together after the ONE deferred live Beelink functional gate PASSED all sub-gates (2026-07-18, ran on `c479d10` / code `ed3b2de`): GATE 1 TRADE 2-team + 3-team atomic swaps (both rosters + both caps moved, all 4 guard rails blocked, "from" label honest); GATE 2 NAMES (real team names on rail/dropdowns/header, fresh db); GATE 3 D6 COMMISSIONER (phase-advance changed legal ops, non-PLAYOFFS rollover showed the engine reason, cap relief dropped the franchise cap); GATE 4 DOLLAR BREAKDOWN (quote == ledger held live — off-grid $3.006M relief quoted AND committed $3.01M). Merge gate: make lint 0 / `go test -race` green (38 pkgs) / pre-commit hooks (golangci-lint+gitleaks+ifaceguard) green. All 4 builds were GLM 5.2 blind-reviewed pre-gate (detail below). Beelink clone reset to clean main after. Handoffs `38`/`39`. **NEXT = a Build_Tracker item, OR the league-calendar branch** (`session/league-calendar`, backend WIP, separate — [[project_thewarroom_league_calendar]]). — build detail preserved below:

**🔨 M4 slice-3 BUILD DETAIL (branch `session/m4-slice3-trade`, now merged as `6c0db73`) — 2026-07-12, RC session.** FOUR builds (all GLM-reviewed, all in the merged squash above): (1) TRADE multi-leg builder + GLM review [detail below], (2) **franchise-name league-fetcher** `a1551f7` — decode the MFL `league` export's `franchises` block (id+name) in `internal/ingestion/league/{types,league}.go`, carry on `RawConfig.Franchises` (rides the rulebook's versioned JSON payload — ZERO schema change), expose `rulebook.FranchiseNames()`, `GetFranchises` stamps `M4Franchise.Name`; rail + trade dropdowns + browsed-roster header show real team names, id fallback when a config version predates the directory (existing DBs → empty until a rulebook Reload+Promote; fresh DBs immediate). New test `TestAssemble_DecodesFranchiseDirectory`. Gate: lint 0 / `go test -race` green / tsc+vite clean. (3) **D6 commissioner surfaces** `2bf7b3e` — ported the commissioner ops out of the raw dev panel into the **League Controls** tab (D8 cutover): season CALENDAR (advance phase / roll season §14 / signing window §6) + a red-divider destructive COMMISSIONER group (retirement §13 / death §13 / cap relief §13, the last using the new franchise-name picker). PURE FRONTEND (backends already existed); every op runs the same D5 preview→ConfirmModal→D4 re-send-intent path, reusing the shared modal so it inherits L1/L2/L3 hardening. `ConfirmModal.Pending.kind` extended with the 6 commissioner kinds + a `confirmLabel` switch. Gate: tsc+vite clean / gitleaks green (no Go change). **TRADE builder detail:** The multi-franchise/multi-player TRADE surface as its OWN "Trade" tab (design doc §"TRADE gets its own builder" — the only multi-franchise op, deliberately NOT folded into the subject-centric single-player workspace). **KEY: the TRADE backend was ALREADY COMPLETE** (`transactions.Trade` request+validate w/ dedup+maxTradeLegs=256, `acquisitions.Trade` atomic per-leg, `buildRequest` KindTrade, generic `Coordinator.Preview` dry-run) — so this is a **PURE FRONTEND build, ZERO Go change** (go test -race green, untouched). **New `TradeBuilder.tsx`:** rail browses ANY franchise roster → "Add →" a player to the cart → each leg gets a destination-franchise `<select>` (excludes the player's own franchise) → "Review trade…" → D5 `PreviewTransaction` → `ConfirmModal` → D4 re-send-intent `ExecuteTransaction` = one atomic N-leg swap; dedup (staged player can't re-add; engine also rejects), every list `?? []` (D9), no mflID typed (D2). **`ConfirmModal`** `Pending.kind` extended with `'TRADE'` ("Confirm trade" footer) — reused wholesale so trades inherit the slice-1/2 GLM hardening (L1 ok:false surfaced not thrown / L2 non-dismissable mid-commit / L3 stageGen discards stale previews). `App.tsx` new Trade tab. **Gate PASSED on RC:** tsc+vite clean / make lint 0 / go test -race green / pre-commit hooks (golangci-lint+gitleaks) green. **GLM 5.2 BLIND review DONE** (driven from RC over SSH, header verified `build · glm-5.2`; commit `7658d4f`): 1 MAJOR + 1 NOTE applied — **M1** pickFranchise awaited GetRoster without clearing roster first, so mid-fetch addLeg could stamp the new franchiseID onto a player still shown from the OLD roster (fix: `setRoster(null)` first + source leg origin from `roster.franchiseID`; engine always blocked a bad commit); **n2** dedup moved inside the `setLegs` updater. Triaged OUT (documented): m1/n1 conditional on non-throwing IPC getters + identical to shipped sibling. GLM verified-correct: stageGen L3, D4 re-send-intent, L1/L2/L3, atomic-cart-after-fail, D9. **⚠️ ONE gate remains, DEFERRED to a Beelink session (BLOCKS MERGE — this was an RC session, no GUI): LIVE FUNCTIONAL GATE** — build a real 2-team then 3-team swap, watch BOTH caps move on refresh. Only after it → squash-merge + session-close merge steps. The live gate now covers ALL THREE builds together: trade swaps move both caps; franchise NAMES render on rail/dropdowns (id fallback on a stale DB); commissioner surfaces work (advance a phase → legal-ops change; grant cap relief → a franchise cap drops; a rejected op shows the engine's reason). Handoff `38-M4-Slice3-Trade-Builder.md`. **Remaining slice-3: pre-commit dollar breakdown** — the LAST item, and the heaviest/least RC-friendly: needs the sealed `apply()` (14 handlers) to RETURN a breakdown so WAIVER/TAG/BUYOUT/trade cap deltas show in the quote; hits a wall (`TxWriter` reads reflect committed state, not the dry-run's own uncommitted writes) → not a pure-frontend job. **GLM review of the franchise-name + D6 builds DONE (2026-07-12, commit `767f6ea`, header verified `build · glm-5.2`, combined blind diff over SSH).** 4 leads, triaged vs source: **L2 [MED] applied** — after a RETIREMENT/DEATH commit `LeagueControls.confirm()` refreshed the phase but not the franchise directory, so the cap-relief picker's counts could show stale post-removal state in-tab until a remount → now refreshes franchises after a successful commit (frontend-only, engine authoritative). Triaged OUT: **L1 [HIGH]** kind strings + all json field tags (`toPhase`/`windowOpen`/`amountMillions`/`franchiseID`/`reason`) match Go exactly, `buildRequest` already routes the 6 kinds, CAP_RELIEF parses the million-string server-side (D2/D4 hold — GLM was blind); **L3** `MFLList.UnmarshalJSON` handles the bare-object collapse and is unit-tested in `decode_test.go`; **L4** MFL franchise ids are unique (last-wins harmless). Gate: tsc+vite clean. Frontend consistency fix → rides the same deferred functional gate, no separate re-gate. **ALL THREE slice-3 builds now GLM-reviewed; the ONLY remaining blocker is the deferred live Beelink functional gate.**

**⭐ M4 TRANSACTION UI slice-2 (TAG + EXTENSION + BUYOUT + RESTRUCTURE) COMPLETE + MERGED to main (2026-07-12, squash `64bf9fc`; next branch off main).** The four per-player PRICED contract ops now live in the real operator workspace under a "Contract moves" heading, all reusing the D5 quote→confirm→commit path. **Backend:** extracted shared `resolveTag`/`resolveExtension` so `PreviewTag`/`PreviewExtension` (new dry-run verbs) and `ExecuteTag`/`ExecuteExtension` build the sealed request from ONE price resolver — preview and commit provably cannot drift (`coordinator.go`). New **`GetLegalOps` IPC** (`m4_app.go`) returns the current phase's legal op kinds straight from the engine's single `phasePolicy` source of truth via exported **`transactions.LegalOps(phase)`** (`phase_gate.go`) — the UI shows an op iff the engine says it's phase-legal (D1), so the offseason-only §12 BUYOUT VANISHES mid-season with no re-encoded rule. `PreviewTransaction` dispatches TAG/EXTENSION through the directory-backed preview verbs, BUYOUT/RESTRUCTURE through generic `Preview`, via a shared `receiptResult` mapper. **Money stays server-side (D4):** TAG/BUYOUT send id-only, RESTRUCTURE sends the `$M` as a string parsed to cents in-tx — no dollar crosses as a JS number. Planted `PreviewTag`/`PreviewExtension` leaves-state-unchanged tests (+ Execute half as the planted mutation). **Frontend:** `TransactionWorkspace.tsx` `opCopy` per-op modal framing, `buildReq` for the 4 ops, ActionPanel "Contract moves" section (EXTENSION year-select, RESTRUCTURE `$M` input, TAG/BUYOUT buttons) each shown only when `GetLegalOps` includes it; `ConfirmModal` kind union extended. Every list `?? []` (D9). **LIVE FUNCTIONAL GATE PASSED on the Beelink** (Gates 0 + A–F, all name-driven, BUYOUT phase-legality appears/disappears with the phase live). **GLM 5.2 BLIND review applied:** 3 leads adopted (all D1 consistency hardening — **L3** `signable` now reads `legalOps.includes('SIGN')` not a hardcoded phase check, killing the exact re-encoding D1 prevents; **L5** ROSTER_STATUS gated by `can()` like WAIVER; **L6** `inputMode="decimal"` on the restructure input); 3 triaged OUT vs source (L1 buildRequest+MoveMillions present, L2 ExecuteTransaction TAG/EXT symmetric=no drift, L4 workspace remounts on tab-switch→legalOps re-fetched). Fixes are frontend edge/consistency only → no re-gate (§10 precedent). Gate: lint 0 / `go test -race` green / tsc+vite clean / planted tests / GLM applied. Handoff `37-M4-Transaction-UI.md`. **NEXT = M4 slice-3** (TRADE multi-leg builder + D6 commissioner surfaces off the dev panel + franchise-name league-fetcher extension + pre-commit dollar breakdown — needs the sealed `apply()` to return a breakdown), OR another Build_Tracker item.

**M4 TRANSACTION UI slice-1 (ROSTER_STATUS + WAIVER + SIGN) COMPLETE + MERGED to main (2026-07-12, squash `1f0abfe`).** The FIRST real operator UI beyond the raw-mflID dev panel (Build_Tracker row 30 — completes the Phase-1 critical path). **4-expert BLIND design panel** (GLM 5.2 + Gemini + DeepSeek + hy3) → **9 locked decisions** in `docs/transactions/M4_Transaction_UI_Design.md` (D1 subject-centric IA / D2 names-from-server-no-mflID / D3 quote→confirm→commit / D4 commit re-sends INTENT never the quoted price / D5 preview = sentinel `errDryRun` rollback on the REAL handler / D6 commissioner ops off the common path / D7 slice = ROSTER_STATUS+WAIVER+SIGN / D8 phased shell cutover / D9 defensive rendering). Mockup approved by Christopher. **Backend** (`m4_app.go`): name-enriched reads `GetRoster`/`GetFreeAgentPool`/`GetFranchises` (player identity via the players-DB Lookup, degrade-safe; **franchise names DEFERRED** — league fetcher doesn't parse them, rail shows ids) + `PreviewTransaction`; `coordinator.go` `Preview`/`PreviewSign` dry-run the REAL handler via `errDryRun` rollback (one write path, no compute drift), planted `preview_integration_test.go` proves byte-identical state. **Frontend** (`components/transactions/{TransactionWorkspace,ConfirmModal,LeagueControls}.tsx`): franchise rail → named roster → per-player action panel with only phase-legal ops; SIGN phase-gated to OFFSEASON+REGULAR_SEASON (advisory UI, backend authoritative). `App.tsx` Transactions(default)+League Controls tabs, dev panel behind `SHOW_DEV_PANEL` (D8). Every list guarded `?? []` (D9). **PREVIEW dollar decision (Christopher): ship slice-1 with will-succeed/rejection-reason pre-commit; the dead-cap dollar lands on the post-commit roster+cap refresh** (revisit pre-commit dollar breakdown when TAG/EXTENSION/BUYOUT land — needs the sealed `apply()` to return a breakdown). **LIVE FUNCTIONAL GATE PASSED on the Beelink** (Gates 0 + A–F, all name-driven, cut/sign moved the cap). **GLM 5.2 BLIND review applied** (commit `a577185`): 3 MAJOR flow fixes — **L1** `confirm()` ignored `ExecuteTransaction`'s ok:false → a rejected commit showed as SUCCESS (now holds the modal in the rejected state); **L2** modal dismissable mid-commit → a backdrop/Cancel during the atomic WriteTx = FALSE CANCEL of a destructive op (now non-dismissable while busy); **L3** a preview resolving after cancel reopened the modal (stageGen ref token discards stale previews); + **L6** `GetFreeAgentPool` missing `state.Err()` guard. Triaged OUT vs source: L5 (SIGN dispatch byte-identical value assertion, signed live), L7 (zero-value `Lookup.Facts` nil-map-safe), L9 (WriteTx `errDryRun` propagation proven by the planted test), L10 (Execute/Preview identical order); L8/L4-gate stale-phase = NOTE deferred. Fixes harden cancel/reject EDGE paths only → no re-gate (§10 drift-guard precedent). Gate: lint 0 / `go test -race` green / tsc+vite clean / GLM applied. Handoff `37-M4-Transaction-UI.md`. (Superseded by slice-2 above.)

**⭐ B7c §6 COMMISSIONER UFA CALENDAR (signing-window gate) COMPLETE + MERGED to main (2026-07-11, squash `3e5066c`; next branch off main).** Builds the Free_Agency_Design **Q4 forward seam** — a commissioner-discretion free-agency signing window that gates SIGN on top of its phase floor. The window **rides the append-only `season_phases.meta` slot as a `from==to` SUB-PHASE directive** (`{"ufa_window":"open"|"closed"}`) — NOT a phase transition, so `CurrentPhase` / season derivation are untouched. It **defaults OPEN** when no directive exists (old rows have `meta=''`, excluded by the `meta LIKE '%ufa_window%'` latest-row read) → **v1 behavior preserved EXACTLY**; and it **PERSISTS across phase transitions AND rollovers** until the commissioner toggles it again ("closes at Super Bowl, stays closed until reopened"). New op **`SET_SIGNING_WINDOW`** (`KindSetSigningWindow`, legal in EVERY phase); state primitives **`SigningWindowClosed`** (read, committed pool) + **`AppendSigningWindow`** (write, rejects a redundant toggle) on `SeasonScope`, new file `internal/store/state/signing_window.go`; `gatePhase` layers the window override onto **SIGN only**. IPC: `buildRequest` SET_SIGNING_WINDOW case + `windowOpen` DTO field. **GLM-5.2 BLIND review = 0 correctness defects**; all 3 cheap advisories applied (load-bearing **one-request-per-`WriteTx`** comment on `gatePhase` — the committed-read gate is safe only because `Execute` runs one Request/tx; LIKE-scan forward-seam note; fake error uses the word). Gate: lint 0 / `go test -race` green / tsc+vite clean / GLM clean. Merged on automated gates + review per the §6 precedent. README updated (UFA calendar → Feature Showcase). **⚠️ FRONTEND commissioner control for SET_SIGNING_WINDOW + LIVE FUNCTIONAL GATE (Gate H) DEFERRED behind the panel lockup below** (backend op is callable now; the dev-panel control is deferred to keep the panel pristine for the lockup bisect). Handoff `35-UFA-Calendar.md`.

**⭐ B7c §6 MIN-SALARY FLOOR (wired to real draft-year experience) COMPLETE + MERGED to main (2026-07-11, squash `09f4575`; next branch off main).** Completes the §6 floor that shipped as a stub (enforce-if-present, always-skipped). Experience is now DERIVED from the MFL players-DB `draft_year` (DETAILS=1): `experience = season − draftYear` (rookie draft class = 0), computed against the AUTHORITATIVE in-tx season. New **`Coordinator.ExecuteSign(ctx, sign, dir)`** resolves the draft year from the players-DB `Directory` PRE-tx (mirrors `ExecuteTag`/`ExecuteExtension` — draft year never crosses IPC); `Sign.apply` derives experience via `w.Season()` and enforces `MinSalaryFloor`. **Christopher's ruling: missing/sentinel/implausible draft data → 0 experience → the rookie $330k floor (the LENIENT direction — lowest §6 minimum).** MFL sends `draft_year` for all players but with sentinels — `"0"` (dropped at normalize typing, value must be >0) and the epoch placeholder `"1970"` (rejected by a season-relative **plausibility window** `maxPlausibleCareer=30`, so a sentinel can't spuriously earn the HIGHEST $630k floor). The now-dead `experienceKnown` seam was REMOVED — the floor is ALWAYS enforced. `draft_year` VERIFIED live (present on all 2588 MFL players). Data path: `players` fetcher (raw) → `normalize.PlayerFacts.DraftYear`/`HasDraftYear` → `ExecuteSign`. **GLM-5.2 BLIND review = 0 real defects** (it scratch-tested the boundary math); its one lead APPLIED — `TestIntegration_SignPlausibilityWindowBoundary` pins 1996 (30yr→$630k honored) vs 1995 (31yr→rookie), so a narrowed window can't silently mis-floor a vet. Gate: lint 0 / `go test -race` green / tsc+vite clean / GLM review clean. Merged on automated gates + review per the §6 deferred-gate precedent (Christopher's call). **⚠️ LIVE FUNCTIONAL GATE DEFERRED to next session — BLOCKED by a NEW-found bug (below), not this change. CARRY-FORWARD: the commissioner UFA calendar seam (`signingWindow()` via `season_phases.meta`) is the other §6 follow-up.**

**✅ B7a TRANSACTIONS-PANEL WEBVIEW LOCKUP FIXED + MERGED to main (2026-07-11, squash `53aab20`; next branch off main).** Root cause was NOT the compositor and NOT §6 logic: an **empty free-agent pool marshals as `mflIDs:null`** (Go nil slice → JSON `null`); `refreshFreeAgents` stored the `null` verbatim and the render then dereferenced `freeAgents.length` — an **unhandled render error unmounts the entire React root** (nav + sidebar), so the whole app looked frozen until a reload (which restarts on the Rankings tab and never re-mounts the panel → "reload recovers"). Fix = guard with the codebase's `?? []` list idiom (`TransactionsPanel.tsx`; matches `RankingsBoard`). Same session **added the `SET_SIGNING_WINDOW` (§6 UFA calendar) dev control** (Gate H is now runnable — "UFA window (§6)" CLOSE/OPEN + note) and **regenerated the Wails bindings** — the `windowOpen` DTO field was missing from `models.ts`, and `createFrom` silently drops unlisted fields, so OPEN would never have crossed IPC (the integration tests bypass IPC, so they never caught it). **LIVE GATE PASSED on the Beelink: Gate 0 (panel mounts, no freeze) + Gate G (min-salary floor) + Gate H (UFA window close→reject→open→lands→redundant-reject) + Gates A–F all pass** — the three stacked deferred gates are now CLEARED. Frontend tsc+vite clean; Go untouched. **NEXT = M-series UI (Build_Tracker row 30, M4 Transaction UI) — the real front-end beyond the dev panel.**

**🗎 (RESOLVED) prior open-bug note — the B7a TRANSACTIONS dev panel LOCKS THE WEBVIEW on tab click.** Surfaced during a §6 live gate; fixed above (`53aab20`). **This is a PRE-EXISTING §6 bug, NOT the min-salary change and NOT the APU compositor** — Christopher confirmed it's reproducible on click, and it is almost certainly what got the §6 gate deferred as a "compositor lockup." RULED OUT: missing binding (GetFreeAgents/GetCurrentPhase bindings exist), a heavy render (live pool is EMPTY — 0 status events), lockless/fast IPC methods (read pool, no mutex), and **concurrent-mount-IPC** (sequencing the two mount calls did NOT fix it — reverted, so the panel is exactly as §6 shipped for a clean investigation). **NEXT DIAGNOSTIC (do first): open the webview devtools console (right-click → Inspect → Console) on the Beelink, click B7a Transactions, read the error** — then bisect the §6 panel additions one at a time (comment out `refreshFreeAgents`, then the SIGN control, then the pool list). Until fixed, NO §6/floor/UFA-window functional gate can run (the panel is the only surface). **THREE gates now stacked behind it:** Gate G (min-salary floor) + Gates A–F (§6 click-through) + **Gate H (UFA calendar: close window → a floor-clearing SIGN is rejected; reopen → it lands — needs a SET_SIGNING_WINDOW dev control, add it WITH the lockup fix).** Combined gate steps staged in `/root/paste.md`.

**⭐ B7c §6 FREE AGENCY v1 (pool + record-a-signing) COMPLETE + MERGED to main (2026-07-11, squash `4b31662`; next branch off main).** Scope LOCKED with Christopher = the free-agent POOL + a SIGN op that RECORDS a signing — NOT a live auction (bid points / sniping / 30-bid cap / RFA §7 / comp picks are deferred as multi-user Phase-2/3). **Panel-gated design** (GLM 5.2 + Gemini + DeepSeek, blind; brief + outcome + the three Christopher rulings in `docs/transactions/Free_Agency_Design.md` / `_Panel_Brief.md` / `_Panel_Outcome.md`). Three pieces: **(1) the pool** — `player_status_events`, an append-only DOUBLE-immutable availability log (`domain.PlayerStatus` FREE_AGENT / RETIRED / DECEASED; write-only Go API + BEFORE UPDATE/DELETE RAISE(ABORT) triggers, the house ledger idiom); pool = latest event FREE_AGENT AND off-roster (`state.FreeAgents`). **(2) the enforced release chokepoint** (the panel's UNANIMOUS top risk) — `ReleasePlayer(ctx, mflID, status, reason)` now records status, so all five removal paths declare where the player went: waiver-cut/buyout/rollover-expiry→FREE_AGENT, retirement→RETIRED, death→DECEASED. **(3) UFA promotion** — folded into `RolloverSeason` BEFORE the snapshot advance (roster still at N so ReleasePlayer's season-scoped delete matches, but "expired" = no PAID cell ≥ N+1, judged against `next`); an expired contract releases to the pool at **$0 dead cap** (expiry ≠ cut). **(4) the SIGN op** (`internal/transactions/freeagency`, behind the Coordinator depguard — PROVEN, planted import fired): FREE_AGENT eligibility; **derived §12 buyout lockout** (zero new writes — rejects if a `dead_cap_ledger` row exists with reason==`deadcap.BuyoutReason` AND `league_year ≥ season`; the buyout row's own audit trail IS the lockout); **§6 min-salary floor** = `MinSalaryFloor(experienceYears)` enforce-if-present, else SKIP+flag (v1 always skips — no experience source yet; R1); **clears prior contract_years cells** (logged) then lays a fresh contiguous PAID(N..N+years-1)+trailing-UFA term (source "signing"); **NO cap-ceiling block** (R2, consistent with every other op); `signingWindow()` seam = OFFSEASON+REGULAR_SEASON now, the SINGLE plug-in point for the future first-class **commissioner-discretion UFA calendar** (window closes at Super Bowl kickoff, reopens on commissioner command — finer than the 3-phase model, uses `season_phases.meta`). IPC (SIGN + `GetFreeAgents`) + React dev control + clickable pool list. **GLM-5.2 BLIND code review: L1 REFUTED (misread — `execPlayer` binds all 3 placeholders); L2 (phantom-franchise guard), L4 (sub-$10k-rounds-to-$0 guard), L7 (lockout test teeth) APPLIED; L3/L5/L6/L8 triaged as consistent-with-idiom / roster-joined-benign** (`docs/transactions/Free_Agency_GLM_CodeReview.md`). Gate: lint 0 / `go test -race` green / tsc+vite clean / depguard proven / **headless `-probe` HEALTHY on the live Beelink DB**. **⚠️ LIVE GUI FUNCTIONAL GATE DEFERRED (carry-forward):** the Beelink WebKitGTK/APU compositor locks up after the first click (`-probe` proved the BACKEND healthy — startup chain green, `FreeAgents` query 3ms; the lockup is pure rendering, the known APU-compositor bad-state a reboot cleared for §11/§14). Christopher chose to merge on the automated gates + probe and run the click-through later. Gate steps staged in `/root/paste.md`. **NEXT = run the deferred live gate (reboot the Beelink → plain `wails dev -tags webkit2_41`), OR M-series UI, per `docs/build-handoffs/Build_Tracker.md`.** **CARRY-FORWARD:** re-sign lockout on a franchise fold (lockout is global by design); experience-source → enable the §6 floor; RFA §7 + the real auction remain multi-user/Phase-2.

**⭐ B7c §14 SEASON-ROLLOVER MACHINERY COMPLETE + MERGED to main (2026-07-11, squash `f1c6fa7`; next branch off main) — completes the §8–§14 transaction rulebook.** The `season` int, previously frozen from MFL config (`ingestion.SeasonYear`) at startup, is now **DERIVED from the append-only `season_phases` log** (league-global latest row). **Panel-gated design** (GLM 5.2 + Gemini + DeepSeek, blind, triaged vs source; brief + full reviews in `docs/transactions/Season_Rollover_Panel_Brief.md` + `Season_Rollover_Design.md`), decisions **D1–D7**: **D1** season derived from the phase log (`refreshSeason` re-derives `s.season` in `load()` and — the **boot-order fix, D7 corollary** — in `Initialize` BEFORE `hasState`, so a rolled-over DB rebooted with a stale config season does NOT re-seed); `CurrentPhase`/`seedInitialPhase` now read the league-global latest (drop the season scope); config demoted to genesis-seed-only. **D5** dedicated **`ROLLOVER_SEASON` op** (`internal/transactions/request_season.go`, alongside `AdvancePhase`): commissioner-confirmed, **PLAYOFFS-gated** (the 2nd phase-restricted op), sole occupant of its `WriteTx`. The store primitive `txWriter.RolloverSeason` (`season_phase.go`, in the embedded `SeasonScope` iface): appends the PLAYOFFS→OFFSEASON transition carrying **season N+1** (the bump lives in that row → D1's derivation makes it current) + **advances the mutable roster/contract snapshot** `UPDATE rosters/contracts SET season=N+1` (**D7 advance-the-snapshot** — they're runtime state, not append-only). **D2** dead_cap/cap_relief rows are UNTOUCHED (stay `league_year=N` as audit; new season sums 0 by absence); **D3** `transaction_counts` untouched (N+1 reads 0 by absence — per-season op limits reset); **D4** `is_restructured` NOT reset (it's a per-CONTRACT lifetime guard, distinct from the per-team-per-season counter — GLM's panel catch, ruled by Christopher). **D5 monotonic:** a fail-loud guard asserts in-memory season == committed phase-log latest before rolling; `ADVANCE_PHASE` rollback appends with the current season so it NEVER rewinds the year. **D6 the make-or-break seam:** every scoped read tracks the refreshed `s.season` (all reads under `wmu`; verified no stale-read/race). Gate: lint 0 / `go test -race` green / tsc+vite clean. **GLM blind CODE review = NO live bug** (verified the seam fixed, no race, drift test has teeth); applied L1 (`TestIntegration_RolloverSurvivesReboot` — real op → close+reopen → derived season survives), L3 (monotonic assert), L4/L5 (test hygiene); L2 sole-occupancy documented (structural today — one leaf Request per WriteTx). **Functional gate PASSED live on the Beelink** (advance to PLAYOFFS → Roll season → phase flips to OFFSEASON, cap rolls to next year's cells, op limits reset; non-PLAYOFFS rollover cleanly rejected). Review saved at `docs/transactions/Season_Rollover_GLM_CodeReview.md`. **CARRY-FORWARD (non-blocking):** (1) frontend board-level cap refresh after a league-wide rollover (the txn panel refreshes the viewed franchise; a global board would need a broad refetch — pre-existing, not rollover-specific); (2) the "no re-bid on a bought-out player until next offseason" + trailing-UFA-promotion carry-forwards are blocked on the FREE-AGENCY POOL, separate from rollover. **NEXT = free agency / UFA promotion, or M-series UI — per `docs/build-handoffs/Build_Tracker.md`.** **Beelink clone: reset to clean main after the gate (`[[reference_beelink_functional_gate]]`).**

**⭐ B7c §13 SPECIAL SITUATIONS (retirement / death / cap relief) COMPLETE + MERGED to main (2026-07-11, squash `b64798f`; next branch off main).** Build_Tracker successor to §12. **Functional gate PASSED live on the Beelink (all 3 gates: retirement / death / cap relief); GLM-5.2 blind CODE review = SHIP (0 MAJOR; 1 MINOR applied — cap-relief now snapped to the $10k grid; the rest triaged as documented design decisions).** **RETIREMENT** (`internal/transactions/deadcap/special.go`): dead-cap charge = **30% of the player's REMAINING contract** — `retirementCharge` sums the ACTUAL PAID cells strictly after the current season (ledger is king, no annual×years approximation), ×30%, ONE `RoundToNearest10k` snap; `Retire` reuses the §8 release/void/dead-cap WRITE path; $0 when no remaining paid year (final-year/UFA). **DEATH (Gaines Adams Rule):** removal with **ZERO cap penalty** — releases + voids cells + records a **$0 dead-cap audit row** (reason `gaines-adams §13`) as the explicit no-penalty marker. **CAP RELIEF:** a commissioner-discretionary CREDIT in its OWN sibling ledger `cap_relief_ledger` (`internal/store/state/cap_relief.go`) — append-only, DOUBLE-immutable (write-only Go API + BEFORE UPDATE/DELETE RAISE(ABORT) triggers), `CHECK (relief_cents > 0)`, **NO UNIQUE key so reliefs accumulate** (multiple appeals/franchise/season), deliberately NOT a negative `dead_cap_ledger` row (dead cap has CHECK≥0; a credit would corrupt its semantics). **CapUsed = Σ(cells) + Σ(dead_cap) − Σ(cap_relief), floored at 0** (`state.go` split into `applyDeadCap`/`applyCapRelief`); a relief for an unknown franchise is silently ignored (documented — floors to 0 anyway). All three are sealed `Retirement`/`Death`/`CapRelief` requests behind the Coordinator depguard, **legal in every phase** (`allPhases()`). IPC (RETIREMENT/DEATH/CAP_RELIEF in buildRequest, CAP_RELIEF parses AmountMillions via `ParseMoneyMillions`) + React dev controls (3 forms). Also landed: **unequal-cell §12 buyout e2e** (`TestIntegration_BuyoutUnequalCellsUsesMean` — extension-first fixture makes cells 8/8/12M; buyout charges 75%×mean = exactly $7M, pinning the mean vs a flat approximation — the §12 carry-forward). Gate: lint 0 / `go test -race` (38 pkgs) / tsc+vite / depguard / functional gate + GLM review. **GLM leads triaged: relief-snap MINOR → APPLIED (`deadcap.Relieve` snaps once, plant-tested `TestIntegration_CapReliefSnapsToGrid` — off-grid $3,006,000 → $3.01M → on-grid CapUsed).** **CARRY-FORWARD (documented, non-blocking): (1) unknown-franchise relief swallow (by-design); (2) no relief idempotency guard (by-design — reliefs must accumulate); (3) Death/VoidCells test-strength NOTEs; (4) `allPhases()` hardcoded vs enum.** **NEXT = season-rollover machinery** (PLAYOFFS→OFFSEASON increment; shared carry-forward with §11's in-season restructure unlock) — **its OWN panel-gated session** (the §12 expert panel flagged season-int lifecycle as the critical must-fix; needs design + expert panel BEFORE code). **Beelink clone: reset to clean main after the gate (`[[reference_beelink_functional_gate]]`).**

**⭐ B7c §12 Contract Buyout + SEASON-PHASE machinery (Vision-2026 D3) COMPLETE + MERGED to main (2026-07-11, squash `73727e5`; next branch off main).** Build_Tracker row 27 successor to the §10 extension. **Functional gate PASSED live on the Beelink (phase transitions + buyout flow); GLM-5.2 blind CODE review = SHIP (0 MAJOR/0 MINOR, 3 coverage-hardening notes applied); expert-PANEL design gate ran first (GLM 5.2 + Gemini + DeepSeek, blind, triaged vs source).** **NEW: a first-class season calendar (D3).** `internal/store/state/season_phase.go`: an **append-only `season_phases` transition log** (current phase = latest `seq` row), DOUBLE-immutable (write-only Go API + `BEFORE UPDATE/DELETE RAISE(ABORT)` triggers, dead-cap-ledger parity), an idempotent **OFFSEASON genesis seed** (the SOLE source of the initial phase — `CurrentPhase` has NO fallback), and a nullable `meta` column for future finer phases. `domain.Phase` enum = **OFFSEASON / REGULAR_SEASON / PLAYOFFS** (only rulebook-justified phases — the machinery is built to take finer phases later as one constant + one gate-map row, no schema change). **`ADVANCE_PHASE` op** (sealed `AdvancePhase{To,Note}`, commissioner-confirmed, no clock automation, no-op rejected, any target allowed for rollback). **Declarative default-deny op→phase gate** (`phasePolicy` pure switch, NOT a var-map — gochecknoglobals) checked as the FIRST step inside `Execute`'s WriteTx (race-free under the single writer, planted `bogusReq` proves default-deny): all six shipped ops legal in EVERY phase (no regression, GQ2), **BUYOUT offseason-only** (the one restricted op). **LOCKED INVARIANT (panel-unanimous must-fix): offseason = START of season N** — the loaded `season` int is the season OFFSEASON belongs to, so a buyout counts against / charges dead cap to / computes `remaining` off season N (`domain.Phase` header carries it). `TxWriter` gained an embedded **`SeasonScope`** interface (Season/OpCount/IncOpCount + CurrentPhase/AppendPhaseTransition) to stay under the 10-member interfacebloat cap. **§12 BUYOUT** (`internal/transactions/deadcap/buyout.go`): pure `buyoutRatePct` (2/3/4 yrs remaining → 60/75/90%; 1/5/6/0 → NO rate, ok=false) + `buyoutCharge` (rate% × avg remaining salary, ONE `RoundToNearest10k` snap, §8 idiom) + `remainingAfter` (years STRICTLY after the current season, half-up mean). `Buyout(ctx,w,mflID)` reuses the §8 **release/void/dead-cap WRITE path** (NOT §8's charge formula), two per franchise per season (`transaction_counts` op_kind "BUYOUT"); **<2 or >4 remaining → fail loud to the §13 commissioner cap-relief path (GQ3, no invented rate)**. Sealed `Buyout{MFLID}`; every figure resolved in-tx, only the id crosses IPC. IPC (`GetCurrentPhase`, BUYOUT/ADVANCE_PHASE in buildRequest) + React dev controls (phase banner + Buyout + Advance-phase). Gate: lint 0 / `go test -race` green (38 pkgs) / tsc+vite clean / depguard PROVEN (planted rankings→deadcap import fired the deny). GLM track record: **§12 buyout 0 defects** (SHIP; 3 coverage notes → apply-order + counter-fail-rollback + rejected-burns-no-slot tests, parity with §11). **CARRY-FORWARD:** (1) unequal-cell buyout e2e test (needs an extension-first fixture; mean already unit-tested 4/6/8M); (2) classify §9 tag / §10 extension offseason-windows when finer phases land (v1 = all-phase per GQ2); (3) **season-rollover machinery** (PLAYOFFS→OFFSEASON increment — shared carry-forward with §11's in-season restructure unlock); (4) "no re-bid on a bought-out player until next offseason" (needs the free-agent pool). Design + panel/review: `docs/transactions/Season_Phase_Design.md`, `docs/build-handoffs/reviews/b7c-buyout-panel.md`. **NEXT = B7c §13 Special Situations** (retirement 30% of remaining per year / death = Gaines Adams zero-dead-cap release / cap-relief appeal — mostly commish, reuse the waiver/deadcap path; the season-phase concept §12 introduced is now available). **Beelink clone: reset to clean main after the gate (`[[reference_beelink_functional_gate]]`).**

**⭐ B7c §10 Contract Extension COMPLETE + MERGED to main (2026-07-10, squash `90d9d08`; next branch off main).** Build_Tracker row 27 successor to the §9 tag. **Functional gate PASSED live on the Beelink; GLM-5.2 blind review closed (3 fixes + 1 coverage applied).** New store surface (`internal/store/state/ledger_extension.go`): `LedgerWriter.PaidCells` (read) + `AppendExtensionYears` — promotes the reserved UFA slot to the first paid extension year, appends further years, re-lays a fresh trailing UFA slot; every new/promoted cell tagged `source="extension"` (the no-2nd-extension marker — NO `is_extended` flag, per the ledger_schema design intent, Christopher-confirmed after I flagged the schema note). `readContractTail` asserts exactly one UFA slot (GLM m1). Both new methods on the embedded `LedgerWriter` so `TxWriter` stays at its 10-member interfacebloat cap. Handler `contracts.Extend`: prices each added year at **150% of the highest-paid REMAINING year (EXCLUSIVE of the current season — the §8 convention; unified with eligibility after GLM M1 caught the `>=` vs `>` split), raised to the §10 position floor**; enforces ≥1yr-remaining/UFA-ineligible, no-2nd-extension (cell-source), ≤6 total, one-per-franchise-per-season (`transaction_counts` op_kind "EXTENSION"); **resets `is_restructured`** (the §10 restructure unlock); a term/ledger **drift guard** fails loud if `expiration_year != last paid cell` (GLM M3). `pricing.PositionFloor` step table (QB15/WR10/RB·TE·LB8/DE7/S5/DT4/CB·K3 $M). `Coordinator.ExecuteExtension` resolves ONLY the position floor via `Directory`; the price resolves IN-TX from the player's own cells; no money crosses IPC. Sealed `Extension{MFLID,AddedYears,floor(unexported)}`. IPC EXTENSION branch + "Extend (§10)" React dev control (year count only). **Tagged-player extension (120% of tag, §9 cross-rule) REJECTED — deferred v1 follow-up.** **§10 rulebook interpretation locked: "each season resets the counter" — `transaction_counts` is season-keyed so the restructure limit resets each season; the extension's is_restructured reset is the per-contract unlock on top (cross-season effect while §11's per-season counter stands — GLM m3 independently confirmed the in-season unlock is illusory).** Gate: lint 0 / `go test -race` green (38 pkgs) / tsc+vite clean / depguard proven (rankings→contracts import fired the deny). **Functional gate ran on `a08aad2` (before the review fixes `07fded2`); the fixes were drift guards + a non-manifesting `>=`→`>` change, so re-gating wasn't required.** GLM track record: **§10 ext 3** (M1/M3/m1). **CARRY-FORWARD:** (1) re-sign must void prior extension cells (free-agency build); (2) the in-season restructure unlock needs season-rollover to fully realize. **NEXT = B7c §12 Buyout** — BLOCKER: needs an OFFSEASON/season-phase concept that does not exist yet (gate-check that design FIRST); rate table by years remaining (2→60%/3→75%/4→90% of avg remaining salary), writes a §8-style dead-cap charge. Then §13 special situations (retirement 30% / death zero-dead-cap / cap-relief — mostly commish, reuse the waiver/deadcap path). **Beelink clone reset to clean main after merge; reset-before-gate hygiene now in memory `[[reference_beelink_functional_gate]]`.**

**📐 VISION 2026 LOCKED + MERGED (2026-07-06, squash `b4dd92b`).** Planning-only session (no build): the approved strategic plan lives at **`docs/roadmap/Vision_2026.md`** — three horizons (Capologist chat / Portfolio Desk / January War Room planner), two-product frame (league instance vs owner cockpit), two doctrine invariants (numbers-never-from-the-model; every write staged-and-confirmed), MFL cutover order (next cuttable dependency = real L2 scoring), and locked decisions **D1–D6** (fork = `VACUUM INTO` + saved-plan-as-intent-replay; calendar = append-only phases + ADVANCE_PHASE op + gate map in Coordinator.Execute, build WITH §12; content = Transaction Recap first w/ numeral-validator; confidence = CoverageReport NOT a scalar — UI surfacing needs the Hard-Constraint unlock, docketed; GM persona = derived GMProfile + Record Book, no new tables). README rewritten visionary (29/38, ledger/tag/restructure current). **Build order UNCHANGED: next session = B7c §10 Extension** per handoff 31. Read Vision_2026.md before any horizon/roadmap work; full session artifact at `/root/.claude/plans/thewarroom-use-haiku-mellow-beacon.md`.

**⭐ SALARY-LEDGER CUTOVER COMPLETE + MERGED to main (2026-07-05, squash `2d521c2`).** All 4 ships landed; the per-year ledger is the sole cap source of truth. Functional gate PASSED live on the Beelink; Ship-4 GLM-5.2 blind review closed (MAJOR `load()` drift-guard blast-radius → softened to warn-and-continue; MINOR → tx-wrapped the 3 `ALTER DROP COLUMN`s; 3 NOTES confirmed correct). **Two fast-follows carried (NOT blockers):** (1) move `app.startup`/`initStoreFloor` OFF the Wails UI thread (goroutine + `runtime.EventsEmit` progress + shell loading state + `defer recover()`) — the real fix for the opaque black-screen startup class hit this session; (2) surface the `load()` cell-drift WARNING in the UI (currently stderr-only, invisible in the GUI). **Diagnostics now in main:** `thewarroom -probe` (headless step-by-step startup with per-step timeouts, run over SSH against a DB copy via `XDG_CONFIG_HOME`) + `startupErr` stderr logging. **INCIDENT (2026-07-05):** the "params store not initialized" + black-screen was a **corrupted active `thewarroom.db`** (truncated by an earlier crash), NOT code — `db.Open` failed at step 0. Recovered from the `thewarroom.db.ship3fresh` backup; corrupt file preserved as `.CORRUPT.*`. Diagnosed via `-probe`; see memory `[[lesson_thewarroom_db_corruption_probe]]`. **⭐ NEXT SESSION OPENS HERE = B7c §10 Extension** — branch fresh off main `git checkout main && git pull && git checkout -b session/b7c-extension`; open by pasting `docs/build-handoffs/handoffs/31-B7c-Extension-Buyout.md`. §10 was PARKED for the cutover and is now nearly free with cells: position floor table + ≥1yr-remaining/UFA-ineligible + ≤3 added/≤6 total + 150%-of-highest-remaining pricing + one per GM per season (`transaction_counts` op_kind "EXTENSION") + an extension re-unlocks a restructure. Then §12 Buyout (BLOCKER: needs a season-phase concept). Full cutover history + the 2 fast-follows in memory `[[project_thewarroom_ledger_cutover]]`.

**⭐ ACTIVE: SALARY-LEDGER PIVOT (branch `session/salary-ledger`, NOT merged) — 2026-07-04.** The single-annual-salary contract model was a ROOT architectural miss: multi-year rules (§8 dead cap / §9 tag / §10 extension / §11 restructure) need per-year salaries and had none — numbers were inferred and commissioners did the math by hand. Pivoting to a per-year **ledger**: `contract_years` (one cell per player-year = the money source of truth) + `contract_year_changes` (append-only, double-immutable dated audit log). Two expert panels (GLM 5.2 + Gemini + DeepSeek) locked the design. **Governing principles: (1) the ledger is KING** (cap/dead-cap/years-remaining COMPUTED from cells, never stored as competing truth); **(2) all math OUT of the buttons** (React sends intent only; Go computes/rule-checks/writes/logs). **Universal FLAT $10k rounding** — `domain.RoundToNearest10k`, every figure, no per-op split. **Cutover (locked, triple-verified):** Ship 1 additive foundation (tables+seed, nothing reads cells) DONE `12b8c06` → Ship 2 dual-write ALL ops with derived-cap PARITY (restructure DONE `08602b1`; **NEXT = tag**, then waiver §8, then wire the global parity gate) → Ship 3 ATOMIC read-flip + delete adjusted-salary (functional gate on Beelink HERE) → Ship 4 drop legacy REAL columns + dead-code sweep. **DO NOT flip reads before every op is refit; DO NOT skip parity.** Seed = flat-fill fencepost (stated year = last PAID year, offseason-after = UFA). §11 restructure = money MOVEMENT between cells (conserved), dest = `expiration_year` (owner-pick UI later); a contract ending this season can't restructure (tightening — old model silently swallowed it). `state.TxWriter` kept at 10 methods (interfacebloat) — group new cell ops into an embedded `LedgerWriter` when tag/waiver need `SetCell`/`VoidCells`. Design docs: `docs/transactions/Salary_Ledger_Design.md` + `docs/league-rules/Math_Rules_Reference.md` (canonical math — read instead of re-deriving). **§10 Extension is PARKED** (was the planned next session; becomes nearly free once cells exist). Gate every commit: `make lint` 0 + `go test -race ./...` green.

**B7c §9 Franchise Tag COMPLETE + MERGED to main (2026-07-04, squash `08e4250`; next branch `session/b7c-extension`). Core tag flow FUNCTIONAL GATE PASSED live on the Beelink (4 steps); GLM-5.2 blind review closed (2 real fixes + planted tests).** New `internal/transactions/pricing.go`: a `Directory` port (players-DB position join — state stays position-blind, the M1 seam) + pure `tagPrice` (top-5 BASE salaries at the position league-wide, averaged, round half-up) + `tagFloorPrice` (max(avg, 120%×prior BASE salary); prior = current Salary at the season boundary — no history store, v1). `Coordinator.ExecuteTag(ctx, mflID, dir)` resolves the price AUTHORITATIVELY from its own Reader + a per-call Directory (the Lookup is lazy — passed per-call, not at startup), then delegates to Execute; NO money crosses the IPC boundary (frontend sends only the id). Handler `contracts.Tag` (behind the proven Coordinator depguard): sets AnnualSalary=AdjustedSalary=price + IsTagged, RESETS IsRestructured (a tag is a fresh contract → a later §8 cut charges the standard 35%, GLM M4b), one-tag-per-team-per-season via `transaction_counts` (op_kind "TAG", independent of RESTRUCTURE). Restructure now REJECTS a franchise-tagged player (a tag is a fixed one-year deal, GLM M4a). Sealed `Tag{MFLID, price(unexported)}`; IPC TAG branch + React "Tag (§9)" control. **Rulebook rulings locked with Christopher:** tag pool = BASE salary (not effective — "salary" is the contract face figure, restructure is a cap mechanic); reject restructure-if-tagged. **GLM-5.2 BLIND review (Beelink, Claude drove it over SSH): 2 real defects fixed (M4a guard, M4b flag reset), both planted-tested; M2 refuted (WriteTx holds `wmu` end-to-end → check+bump atomic), M3 confirmed as-built, M1 accepted v1 tradeoff (price resolved outside the tx — single-user, wmu serializes writes; carry-forward = league read inside the tx). Full triage in `docs/build-handoffs/reviews/b7c-tag-glm-review-outcome.md`.** **DEFERRED (handoff 31): §9 "two consecutive years" + "2nd tag = 120% of first" need cross-season per-player history (buyout's season-phase bucket).** Gate: lint 0 / `go test -race` green (TestTagPrice, TestTagFloorPrice + 5 tag integration tests incl. 2 composition guards) / tsc+vite clean / depguard proven. **NEXT = B7c cont. Extension §10** (handoff `31-B7c-Extension-Buyout.md`): position floor table + ≥1yr-remaining/UFA-ineligible + ≤3 added/≤6 total + 150%-of-highest-remaining pricing + one per GM per season (transaction_counts op_kind "EXTENSION") + an extension re-unlocks a restructure; then Buyout §12 (BLOCKER: needs a season-phase concept) + §13 special (retirement 30% / death zero-dead-cap / cap-relief — mostly commish).

**B7c §11 Contract Restructure COMPLETE + MERGED to main (2026-07-04, squash `3041910`; next branch `session/b7c-tag`). FUNCTIONAL GATE PASSED live on the Beelink — all 5 steps green.** First B7c contract op (Build_Tracker row 27 successor). New pkg `internal/transactions/contracts` behind the Coordinator depguard (PROVEN — a planted used import from rankings fired the deny): pure `MaxMove` encodes the §11 tier table (≥$3M→$1M / ≥$6M→$2M / ≥$12M→$3M, <$3M ineligible) as a step function; `Restructure` lowers the cap-counting (adjusted) salary by the owner-chosen move — flat math, CapUsed is the SUM so cap drops by exactly the move — and sets `is_restructured` (a later §8 cut then charges 50% not 35%, the B7b wiring now reachable). **Limits encoded so a rule violation is unrepresentable:** one-per-contract (`is_restructured` guard), one-per-team-per-year (new durable `transaction_counts` table `(league,franchise,season,op_kind)→count` + TxWriter `OpCount`/`IncOpCount`), move>0 ≤ tier-max, plus a local move≤effective guard (GLM M1 — newAdjusted can't go negative independent of any backstop). §11 tiers on the BASE Contract-Year Salary; move subtracted from EffectiveSalary (equal in v1). Sealed `Restructure{MFLID,Move}` request; IPC + React "Restructure" dev control (move entered in $M → exact cents via `domain.ParseMoneyMillions`, integer string-math, NO float money). **GLM-5.2 BLIND review on the Beelink (opencode `build` agent, key sourced from `~/.config/opencode/zai.env`, `/coding/paas/v4` endpoint): 0 MAJOR.** Applied M1 (local non-negative guard) / N1 (`OpCount` two-bump-in-one-tx footgun doc) / N2 (planted-failure rollback + apply-then-bump ordering tests) / N3 (validate-rejection table). Triaged OUT vs source: M2 (`ParseMoneyMillions` is integer string-math, no `ParseFloat`) + N6 (`ContractChange` copies all 7 facets, nothing zeroed). **DEFERRED — M3: no DB-level ceiling on the per-season counter (a blanket `CHECK(count≤1)` is WRONG — tag/buyout allow 2/year, so ceilings are per-op-kind and correctly live in the handlers behind the depguard); carry-forward.** Gate: lint 0 / `go test -race` green / tsc+vite clean / depguard proven / §11 tier table pinned (every fencepost incl. `3*m−1`/`6*m−1`/`12*m−1`). **FUNCTIONAL GATE PASSED (2026-07-04, live on the Beelink after the APU compositor reboot): `wails dev -tags webkit2_41` launched clean; all 5 restructure steps green — cap drops by EXACTLY the move / over-max rejected / 2nd-per-team rejected / 2nd-per-contract rejected / restructured cut charges 50%. (Beelink clone lives at `/home/chris/opencode/TheWarRoom`, NOT the old `/home/chris/TheWarRoom` the handoff assumed. Beelink is an AMD APU, NOT NVIDIA — homelab "PRIME" note is LivingBot.)** **NEXT = B7c cont. — Tag §9** (handoff `30-B7c-Tag-Extension-Buyout.md`): the first op with a NEW dependency — cross-store top-5-by-position salary aggregation (mirror M1's cross-store read seam), floor = 120% of prior-year salary, max 2 consecutive years, per-team-per-season count via the new `transaction_counts` limiter (op_kind "tag", ceiling 2 in-handler — M3 stays in-handler, NOT a table CHECK). Then Extension §10 (position floors + 150% pricing + multi-op history), Buyout §12 (BLOCKER: needs a season-phase concept), §13 special (retirement 30% / death zero-dead-cap / cap-relief — mostly commish).

**B7b — Money→int64-cents + §8 Waiver-Cut Dead Cap COMPLETE + MERGED to main (2026-07-04, squash `ffec0c3`; next branch `session/b7c-contract-ops-ii`).** Build_Tracker row 27 — the FIRST money-bearing transactions, **functionally verified live on the Beelink** (a §8 cut removes the player, cap moves — his salary leaves, the dead cap lands — the franchise stays visible via its dead cap, and it persists on a reread). **v1 scope was RESCOPED from the stale "Acquisitions" naming to the rulebook-grounded set: cents migration + `dead_cap_ledger` + §8 waiver-cut ONLY.** Delivered as 3 commits: (1) **`domain.Money int64` CENTS** — a zero-float string→cents parser (no float64 intermediate; kills `parseSalary`'s ParseFloat) + an additive REAL→cents migration (fresh DDL has the cents cols + CHECK>=0; existing DBs get idempotent ALTER/backfill `CAST(ROUND(x*1e8))`/0-mismatch-verify); float survives ONLY at 3 documented display edges (engine L5 cap-ratio, M1 + txn IPC DTOs). (2) **`dead_cap_ledger`** — append-only, keyed to an ABSOLUTE `league_year`, DOUBLE immutable (no update/delete Go API + BEFORE UPDATE/DELETE RAISE(ABORT) triggers, the B6 idiom); CapUsed DERIVED = live contracts + this-season ledger charges (a dead-cap-only franchise stays visible). (3) **§8 waiver-cut** (`internal/transactions/deadcap`): pure `Charge()` = 35% × salary × remaining years (50% if restructured, 0 if claimed/expiring), exact integer cents, round half-up; **remaining years = expiration_year − season (EXCLUSIVE — a final-year/UFA player = 0 remaining → $0, Christopher-confirmed, matches §10's own usage)**; the **whole charge lands in the CUT year (Christopher-confirmed locked decision)** — flat math, no per-year spread; `Waive()` reads terms → computes → releases → charges, all atomic in one `WriteTx`; depguard PROVEN (a planted used import from rankings fired the deny). `state.TxWriter` gained **ReleasePlayer / Player (pre-tx read) / Season** — the read+release seam B7c ops reuse; `Waiver{MFLID}` sealed Request + IPC/React "Waiver (cut)" surface. **NFL-model LEAKAGE STRUCK from OQ-014 + handoff 28: no signing bonus, no proration/acceleration, no Hamilton/largest-remainder split — this league's dead cap is flat §8 math** (caught by Christopher: signing bonus had no basis in the actual rules). **Review: GLM 5.2 was DOWN → Gemini BLIND review hand-delivered; 3 leads, ALL triaged — no live defects.** (Ledger UNIQUE key assumes one cut/player/franchise/year — sound today since §8 bars re-signing + no FA returns a released player; re-key on the cut EVENT when FA lands. Cap-CEILING enforcement is unbuilt league-wide — its own build. Both carried forward + documented in-code.) `state.go` split → `schema.go` for the 400-line cap (AD-17). Gate: lint 0 / `go test -race` green / tsc+vite clean / depguard proven / §8 formula table pinned. **NEXT = B7c — Contract Ops II** (handoff `29-B7c-Contract-Ops-II.md`, Build_Tracker row 27 successor): restructure §11 (owner-discretionary move bounded by the $1M/$2M/$3M tier max — do NOT blend with §6), tag §9 (needs cross-store top-5-by-position aggregation), extension §10 (position floors + multi-op history), buyout §12 (needs a season-phase concept), §13 special situations (retirement 30% / death / cap-relief — mostly commish). Restructure is the natural first commit (no new dependency; completes the dead-cap story via is_restructured→50%). Also drop the frozen legacy REAL money columns one release on.

**B7a — Transaction Foundation + Coordinator COMPLETE + MERGED to main (2026-07-03; next branch `session/b7b-contract-ops`).** Build_Tracker row 26 — the FIRST runtime mutator, **functionally verified live on the Beelink** (a real trade persists state + cap on both franchises; a bad-leg trade rolls back WHOLE — atomic). New pkg **`internal/transactions`**: the `Coordinator` is the SOLE `state.Writer` holder (AD-02), exposing `Execute(ctx, Request) (Receipt, error)`; every mutation runs through state's NEW **spanning-tx surface `WriteTx(ctx, func(TxWriter) error)`** — one SQLite tx + one reload, all-or-nothing. **`state.Writer` reshaped to `Reader + WriteTx`** so a mutation IS a transaction; the three single-op methods became one-op WriteTx calls (one validation path, zero SQL dup). Sealed `Request` set (`Trade` multi-leg + `RosterStatusChange`) built in the ROOT pkg so callers never import a handler subpackage; handlers live in **`internal/transactions/acquisitions`** behind the `transactions-only-through-coordinator` depguard (**PROVEN** — a planted import from rankings fired the exact deny). Dev IPC (`ExecuteTransaction`/`GetFranchiseState`, typed/ifaceguard-clean) + a React "B7a: Transactions (dev)" tab (load franchise → trade → confirm) = the functional gate; app.go wires the Coordinator once (`initStoreFloor`). **GLM 5.2 BLIND review earned its keep — 1 MAJOR + 2 MINOR + 1 NOTE applied, each planted-tested; 7 triaged OUT vs source.** The MAJOR was a genuine catch none of the focus points named: a tx that COMMITS then fails its in-memory reload left memory silently stale — now WriteTx retries the reload once, else **POISONS the store** (`state.Err()` sticky; poisoned store refuses writes and `GetFranchiseState` surfaces staleness instead of serving a pre-tx roster). MINORs: no-op self-move rejected (A↔B swaps unaffected); trade legs capped (256). **DECISION — OQ-014 Money RESOLVED (2026-07-03) via a 2-expert panel (GLM 5.2 + Gemini, parallel brief): money = `int64` CENTS** (largest-remainder splits, additive REAL→INTEGER migration, kill the `parseSalary` float64 intermediate); companion locks: dead-cap v1 SIMPLE (salary + signing-bonus proration), NO cap rollover. Record in `docs/roadmap/Roadmap_and_Open_Questions.md`. Gate: lint 0 / `go test -race` green / tsc+vite clean / depguard proven. **GLM track record: B7a 3 / M1 3 / B6 1 / K 0 / S 0 / CB 0 / LB 0 / DE 0 / TE 0 / WR 1 / RB 1 / DT 3 / QB 2.** **NEXT = B7b — Money→int64-cents migration + Contract Ops + (simple) Dead-Cap** (handoff `28-B7b-Contract-Ops-DeadCap.md`, Build_Tracker row 27) — now unblocked by the OQ-014 lock: additive cents migration + `contracts`/`deadcap` handler subpackages behind the Coordinator + the largest-remainder split + the randomized-ordering money property test. **GATE-CHECK before code:** migration timing, dead-cap ledger table shape, acceleration rule, signing-bonus proration divisor. Add/drop (free-agency) still deferred — needs a free-agent-pool concept in state.

**M1 — Asset Rankings COMPLETE + MERGED to main (2026-07-02, squash `2105f6a`; next branch `session/b7a-transactions`).** Build_Tracker row 25 — the FIRST VISIBLE engine validation, **functionally verified live on the Beelink** (32-team board renders, AD-20 team drill-down, position filter, cap-efficiency view, skip-if-present re-score all confirmed by click-through). New composition-class orchestrator **`internal/rankings`** (ports: `state.Reader` / `Directory` = `normalize.Lookup.Facts` / `ConfigSource` = new public `rulebook.ActiveVersion` / `output.Writer` / `Registry`): scores every rostered player through composition + the full 10-rubric registry, persists ONE batch to B6 stamped with the ACTIVE config version (DECISION-010 — orchestrator stamps, B6 never mints). **BasePoints = the LABELED MFL 2025-YTD proxy** (option (b), L2 pending — the label rides every IPC payload + a UI banner) via new fetcher `internal/ingestion/playerscores` (post-filter empty guard, per-record YTD week assert, CheckAPIError; live 1650 / top 476.75). **Age** from players `DETAILS=1` birthdate (raw at the fetcher, typed at normalize; 2021/2630 live; 6 rostered players lack it → VISIBLY excluded, never faked). Exclusion policy gate-checked: unknown-id / FLAG / missing-birthdate / implausible-age reported with reasons; absent proxy → ZeroBase; negative proxy → floored + counted SEPARATELY (NegativeBase). App startup wires the FULL store floor (rulebook/state/output seed from live MFL only on a fresh DB); **sandboxCap is DELETED — every score path reads the real league cap**. React M1 tab (default) + score-report panel; `state.EffectiveSalary` exported (M17 — board and cap usage share one definition). **GLM 5.2 BLIND review — bird was DOWN → paste.md manual relay; the full-prompt opencode run HUNG → per-file chunks pulled via Z.AI direct was the reliable path (Christopher-proven). 3 MAJOR + 4 MINOR survivors applied, each planted-tested** (nil-Registry guard — a nil map silently scores the whole league identity-L4; all-aggregate payload bypassed the empty guard; GetRankings hid a persisted board on a display-only directory failure — now degrades with "(unknown id)" + Warning; skip path Scored=0/Existing; ZeroBase/NegativeBase split; skip-check hoisted above the MFL fetches; YTD assert). CapEff-units lead closed VERIFIED-correct (PlayerState.Salary documented millions). Accepted-tradeoff NOTEs recorded in the fix commit (Scores/Write TOCTOU, shared 120 s budget, process-lifetime directory cache). **GLM track record: M1 3 / B6 1 / K 0 / S 0 / CB 0 / LB 0 / DE 0 / TE 0 / WR 1 / RB 1 / DT 3 / QB 2.** Gate: lint 0 / `go test -race` green / tsc + vite clean / sql-confined depguard proven over rankings (planted import fired). **NEXT = B7a — Transaction Foundation + Coordinator** (handoff `27-B7a-Transaction-Foundation.md`, Build_Tracker row 26) — the sole runtime mutator: the `internal/transactions` Coordinator becomes the ONLY `state.Writer` holder; handlers live behind `Coordinator.Execute` (the depguard rule already exists — prove it, M3). **GATE-CHECK before code:** v1 transaction set, atomicity shape (spanning tx vs per-step+compensation), dead-cap write semantics (OQ-014 Money precision likely bites here), UI surface now vs B7b.

**B6 — Per-Season Output Store COMPLETE + MERGED to main (2026-06-28, squash `025e2a8`; next branch `session/m1-asset-rankings`).** Build_Tracker row 24. The FIRST persistence of engine OUTPUT and the FOURTH/FINAL Layer-2 store — **the Layer-2 store floor is now COMPLETE** (rulebook/state/params/output). New pkg `internal/output` cloning the B3c/B4 template (Reader/Writer split, single-writer law, `db.Pools` read/write split, parameterized SQL) with ONE deliberate divergence: **NO reader RWMutex** — this store keeps no in-memory cache, reads go straight to the concurrency-safe read pool (GLM caught the cargo-culted second lock; B3c's `mu` guards in-memory maps that don't exist here). **DOUBLE immutability (AD-04 / DECISION-010):** append-only Go API (`Writer` exposes only `Write` — no Update/Delete) + SQLite `BEFORE UPDATE/DELETE` triggers `RAISE(ABORT)` so the DB rejects mutation even if a caller bypasses the Go API (planted raw UPDATE+DELETE both error). Rows keyed `(season, scoring_config_id, mfl_id)`; **`scoring_config_id` is STAMPED by the caller** (rulebook active version), never minted here — a new config writes NEW rows, old rows are never re-scored. **Record shape = flattened `engine.Result` scalars into queryable columns** (M1 ranks by `adjusted_score` in SQL), AdjustedScore at full float64 precision (L6 exact-equality tiebreak); confidence stays off-record (Hard Constraint). **Reads return final ranking order** — the ORDER BY encodes the L6 `TiebreakerKey.RanksAbove` protocol (adjusted_score DESC, veteran, RAS, scarcity, then mfl_id as the stable key). **`Write` validates the WHOLE batch before any insert** (unsigned-digit mfl id — closes the playerid signed-string gap from the pre-B6 audit; known cap tier; all score fields finite — L5 does NOT re-check accumulator finiteness; no in-batch dup id) → a bad batch rolls back whole. A cross-config duplicate is rejected as the **exported `ErrDuplicate`** (so the caller can `errors.Is` the drift signal), detected via **modernc's TYPED PK/UNIQUE constraint code** (not a brittle "constraint failed" substring that would also catch CHECK/NOT NULL/FK). depguard: new `store-output-no-siblings` rule (output never imports another store) + `internal/output` added to the engine-is-pure / layer1 / sql-confined deny+exempt lists; **proven** — a planted store import fired the rule. **GLM 5.2 BLIND review earned its keep: 3 MAJOR + 1 MINOR + 1 NOTE, all real + applied** (typed constraint code / exported ErrDuplicate / L6 ORDER BY + tie test / in-batch dup guard / mu removal); **triaged 3 leads OUT** as template-consistent (read-path CapTier re-check = single-writer law; `Writer`→`*Store` cast = matches B3c, no extra mutation surface; per-row INSERT loop = perf only at season scale). Gate: `go build` / `make lint` 0 / `go test -race ./...` green (14 output tests). **Functional gate = `TestDecision010NewConfigDoesNotRescore`** — B6 is a store-only layer with no UI surface by design (the click-through gate lands at M1), Christopher accepted this (option 1). **GLM track record: B6 1 / K 0 / S 0 / CB 0 / LB 0 / DE 0 / TE 0 / WR 1 / RB 1 / DT 3 / QB 2.** **NEXT = M1 — Asset Rankings** (handoff `26-M1-Asset-Rankings.md`, Build_Tracker row 25) — the FIRST VISIBLE engine validation: a "score-the-league" orchestrator runs all 32 real rosters through the pipeline, persists to B6 tagged by the active config id, and renders a ranked board (global + per-team drill-down AD-20 + position filter + cap-efficiency) in Wails/React. **HARD GATE-CHECK BLOCKER:** `engine.PlayerInput.BasePoints` is a SUPPLIED input (L2 base scoring was deferred at B5a — no L2 file); M1 needs a real BasePoints or the rankings are meaningless — Christopher must pick (a) build minimal L2 from MFL scoring history, (b) a labeled MFL-fantasy-points placeholder proxy to keep M1 visible-first (handoff's recommendation), or (c) defer M1 until L2, BEFORE any code.

**B5b-K — kicker rubric (the LAST L4 build; DECISION-011 reversal of the structural-exclusion model) COMPLETE + MERGED to main (2026-06-28, squash `375ce31`; next branch `session/b6-output-store`).** Build_Tracker row 23. New pure pkg `internal/engine/l4/kicker/k.go`: `Combined = film × 1.000 × 1.000 = film`. **Film ACTIVE** — S-curve over a **Madden 0.60 / NFLProduction 0.40** composite at the gate-check-pinned **±3% cap / steepness 10.0 / inflection 0.50** (K sparse-data → conservative cap; Christopher's call — the curve was UNSPECIFIED in any doc, DECISION-011 deferred it here). **RAS forced to exactly 1.000** (SL-020 partial, unread const like qb.go) + **Breakout forced to exactly 1.000** (no breakout framework at K). NO SL-019 / SL-021 / NGS anchor. **AD-10 REVERSED + SHIPPED** (K combine no longer flat-1.000 — film now moves its valuation; the roadmap/Scouting_Schema DECISION-011 record is now executed; K_Rubric.md status header updated, §2/§3/§4 body flagged STALE pending full rewrite). **Versioned the boundary** — `engine.ScoutingInput` + `composition.PlayerSpec` gained `MaddenFilm`/`NFLProduction` sub-signals (the 0.60/0.40 split is pinned IN-ENGINE, so K consumes the two components SEPARATELY, NOT a pre-blended FilmComposite); Data-Parity per sub-signal (absent → neutral 0.50; both absent → film short-circuits to exactly 1.000); `validateScouting` range-gates both via a new shared `checkUnitRange` helper (M17 — also cut a gocyclo-22 back under the limit). **`eval3C` EXTENDED** — proves the active film path does NOT leak into K's RAS/breakout (both exactly 1.000 with film populated, Combined==FilmEffective); 3C stays GREEN. Suite stays 13, 0 FAIL; 3G/3H stay PENDING. K registered in `rubrics()`+`realRegistry()` (replaced `engine.IdentityLayer4()` — removed the now-unused engine import from both sites); K Alpha/Bravo fixtures (1001/1002, identical base/age/salary so the gap is film-only) + `TestRealKRankingDifferentiates`. `TestKFilmActiveMaddenMajority` pins the 0.60/0.40 split + cap + steepness THROUGH Apply via Madden-1.0/NFLProd-0.0 → 1.013864 vs Madden-0.0/NFLProd-1.0 → 0.986136 (opposite-direction offsets prove the 0.60 majority; a swap is CAUGHT — deliberately chose a 1.0/0.0 pair because a near-equal pair like 0.90/0.80 would resolve within tolerance and pass invisibly under a swap, the GLM-on-S lesson). depguard `engine-is-pure` PROVEN over kicker pkg (planted USED `params.ValueType("")` import fired the exact "engine cannot import stores" message, reverted, NOT committed). **GLM 5.2 BLIND review: clean build, 0 MAJOR / 0 MINOR** (independently recomputed both film magnitudes to ~5e-7, confirmed all four pins enforced THROUGH Apply, found no RAS/breakout leak path); applied 3 cosmetic NOTEs (dead `!approx` half of an exact `!=` check / both-absent short-circuit wording / "asymmetry"→opposite-direction offset); **triaged 1 NOTE OUT** (empty-struct/unused-receiver = registry uniformity, GLM itself said intentional, lint passed). Gate: `make lint` 0 / `go test -race ./...` green / functional gate PASSED live on the Beelink ("pass" — 3C stays green, K Alpha > K Bravo). **GLM track record: K 0 / S 0 / CB 0 / LB 0 / DE 0 / TE 0 / WR 1 / RB 1 / DT 3 / QB 2.** **ALL 10/10 L4 RUBRICS ARE NOW REAL — the Tier-2 Layer-4 arc is COMPLETE.** **CARRY-FORWARD:** SL-OQ-041 (FG 70+) / SL-OQ-042 (Madden K pipeline) / CAL-032 carried; K film cap/steepness is a v1.0 starting pin, revisit at the film-weight calibration pass. **NEXT = B6 — Per-Season Output Store** (handoff `25-B6-Output-Store.md`, Build_Tracker row 24) — the FIRST persistence layer for scores: clones the B3c/B4 store template (Reader/Writer split, single-writer law, two-lock idiom), persists `engine.Result` tagged by `scoring_config_id` (DECISION-010), DOUBLE immutability (append-only Go API + a SQLite UPDATE/DELETE trigger, AD-04). Completes the Tier-2 store floor → then M1 (Asset Rankings, first visible engine validation). **PRE-B6 AUDIT COMPLETE (2026-06-28):** Full-codebase GLM 5.2 BLIND review executed (three passes: engine+rubrics+composition+harness, stores+DB+primitives, ingestion pipeline). Findings at `docs/build-handoffs/audit-glm-2026-06-28.md`. B6-specific watch list: (1) reject non-finite `AdjustedScore` at write time — L5 does not re-check accumulator finiteness; (2) persist `MFLID` as TEXT verbatim; (3) replicate wmu/mu two-lock idiom exactly; (4) validate any string IDs as unsigned digits only (playerid signed-string gap); (5) store AdjustedScore at full float64 precision (L6 tiebreak depends on exact equality); (6) verify the madden `null`-rating silent-zero before B6 write path depends on K scores.

**B5b-S — safety rubric (the SECOND NGS-anchor position + the FOURTH SL-019 instance) COMPLETE + MERGED to main (2026-06-27, squash `4209026`; next branch `session/b5b-k`).** Build_Tracker row 22. New pure file `internal/engine/l4/defense/s.go` (170 lines), structurally PARALLEL to cb.go with **FOUR spec-driven divergences** (S_Rubric §2/§3/§4/§7), all gate-checked with Christopher before code: **RAS steepness 10.0** (NOT CB's 11.0 — S athletic profiles continuous across box-to-deep, not bimodal); **breakout steepness 11.0** (NOT CB's 10.0 — S college-production data cleaner); **College Production Share = INT+Tackle** at thresholds **0.08/0.14/0.20** (NOT CB's PD+INT 0.08/0.16/0.24 — safeties lower absolute market-share ceilings; the INT+Tackle-vs-PD+INT bundle SELECTION is handled upstream at `ingestion/collegedefense/fetcher.go`, the engine consumes one normalized share); **`S.HasNGSAnchor()=true`** (2nd NGS position). Everything else MATCHES CB by design: **SL-019 strength 0.30** (reuses `curve.SL019` VERBATIM, M17), film STANDARD ±5%, High RAS ±8%, weights 0.20/0.25/0.40/0.15, peak-28 age-traj + breakout-age curves, **NO SL-021 cushion**. **School tier = TEMPLATE** + `peakLimit(PosS)=28` + `cushionGuard(PosS)=(0,0)` (all verified). §5 worked examples verified by hand (Hamilton push 1.110 / Mathieu pull-attempt 1.053 — 8th Lockett-pattern instance; SL-019 lifts Mathieu age-34 traj 0.00→0.135 vs Peterson's 0.279 at CB — reward scales to athletic profile). **Case 3I needed ZERO eval change** — `eval3I` already loops `{PosCB,PosS}` and asserts every registered NGS position reports the anchor, so registering S just makes the S branch live (NO flip — 3I green from CB; S co-asserts). Suite stays 13, 0 FAIL; 3G/3H stay PENDING. S registered in `rubrics()`+`realRegistry()`; S Alpha/Bravo fixtures (0901/0902) + `TestRealSRegistryHolds3I` + `TestRealSRankingDifferentiates`. `TestSMagnitudeThroughApply` pins RAS steep 10.0 (not 11.0 → 1.05081) + breakout steep 11.0 (not 10.0) + SL-019 0.30 (not 0.35 → 1.01492) + **the school 0.25 / college 0.40 weights individually** (the GLM fix — they resolved equal in all other tests, so a swap would have passed invisibly), all at the call site. depguard `engine-is-pure` PROVEN over s.go (planted USED `store/params` import fired, reverted). **GLM 5.2 BLIND review: SHIP-ABLE, 0 MAJOR** (independently recomputed every magic number ≤1e-3, confirmed the four divergences correct + the magnitude tests genuinely pin them); applied 1 MINOR (the per-weight pin above); **triaged 1 MINOR OUT** (INT+Tackle bundling correctly at the data layer) + 4 NOTEs OUT (shared-helper/parallel-to-CB — assertBand/approx shared, DT-cap coupling + Present-NaN identical to CB, 0.278 matches CB's table). Gate: `make lint` 0 / `go test -race ./...` green / functional gate PASSED live on the Beelink ("pass" — 3I stays green, S Alpha > S Bravo). **GLM track record: S 0 / CB 0 / LB 0 / DE 0 / TE 0 / WR 1 / RB 1 / DT 3 / QB 2.** **CB+S complete the IDP set + the NGS pair — 9/10 L4 rubrics live.** **CARRY-FORWARD:** SL-018 receding RAS schedule + L3 RAS-buffers (CB/S 0.25×RAS_norm) + coverage film weights (incl. S's 0.30 NGS anchor) pend their data/calibration stage; SL-OQ-035/036 (SafetyRole free-vs-box split — stays SCHEMA-only, one S rubric for all safeties in v1.0) + CAL-028/029 carried. **NEXT = B5b-K** (handoff `24-B5b-K.md`, Build_Tracker row 23) — the LAST L4 build: flips the K identity PLACEHOLDER to a real Madden-driven rubric (DECISION-011: Film ACTIVE @ Madden 0.60 / NFLProduction 0.40; RAS+Breakout stay forced to 1.000 so case 3C STAYS green; reverses AD-10). **GATE-CHECK BLOCKER:** the K film cap/steepness/inflection are UNSPECIFIED in any doc (DECISION-011 defers them to this build) — Christopher must pin them before code (handoff recommends ±3% / steep 10.0 / inflection 0.50). The K_Rubric.md §2/§3/§4 body is STALE (old exclusion model) — code from Scouting_Schema.md → DECISION-011, NOT the rubric numbers.

**B5b-CB — cornerback rubric (the FIRST NGS-anchor position + the THIRD SL-019 instance, first at REDUCED strength) COMPLETE + MERGED to main (2026-06-27, squash `8eab1f9`; next branch `session/b5b-s`).** Build_Tracker row 21. New pure file `internal/engine/l4/defense/cb.go` mirroring de.go — `Combined = film × RAS × breakout`. **ACTIVE RAS, HIGH tier** (cap ±8% / **steepness 11.0** — CB athletic profiles bimodal, sharper transition / raw÷10 / rookie weight 1.00 only — SL-018 receding deferred; AD-09 not forced). **Film STANDARD ±5%** (NOT DT/LB's SL-005 ±3% compression — CB is rich-data via NGS, not a thin-data IDP), Data-Parity neutral (coverage film weights unset). **NO SL-021 cushion.** **Breakout** cap ±5%, **steepness 10.0** (NOT DE/LB's 11.0 — CB college data noisier); weights **0.20/0.25/0.40/0.15** (School Tier elevated 0.25, College Production Share PD+INT 0.40, breakout age dropped 0.20); curves vs CB_Rubric §4 (breakout-age half-year ≤19.5→1.00/20.5→0.75/21.5→0.45/≥22.5→0.15, college-share ≤0.08→0.15/0.16→0.55/≥0.24→1.00, **peak 28** age-traj ≤24→1.00…28→0.50…≥32→0.00). **School tier = TEMPLATE** (no CB branch — verified template + `peakLimit(PosCB)=28`). **SL-019 REUSE at REDUCED strength 0.30** (vs TE/DE 0.35 — CB longevity more variable within position, §1/§4): inherits the shared `curve.SL019` verbatim on breakout-age (present-only) + age-trajectory (always); all five §4 worked examples verified by hand (0.405/0.278/0.6425/0.368/0.279). **Case 3I converted `gatedPending`→real `eval3I` via a rubric introspection hook `CB.HasNGSAnchor()`** (the gate-check decision — option (a); mirrors `DT.PFFAlpha`: rubric INTERNAL, never on Layer4Output, since film weights are unset so NGS-presence is invisible at the engine boundary). eval3I asserts PER-REGISTERED-POSITION (CB reports the anchor, the WR control does NOT, the loop also covers PosS), so **CB alone flips 3I green** — S will co-assert without being required (3I no longer waits on S). Suite stays 13, 0 FAIL; 3G/3H stay PENDING. CB registered in `rubrics()`+`realRegistry()`; CB Alpha/Bravo fixtures (upgraded 0601 + added 0602) + close-gate/ranking tests; `TestCBMagnitudeThroughApply` pins RAS steepness 11.0 + SL-019 strength 0.30 + raw÷10 at the call site. depguard `engine-is-pure` PROVEN over cb.go (planted USED `store/params` import — `var _ = params.ValueType("")` — fired exact message, reverted). **GLM 5.2 BLIND review: 0 HARD defects** (recomputed all math/curves/worked-examples by hand, confirmed correct); applied 1 MINOR (dropped a vacuous `TestCBDepguardPure` no-op — no other rubric has one; the manual planted-import run + the linter IS the gate) + narrowed the clamp-test comment; **triaged 2 OUT vs source** — FilmRaw passthrough (debug-only per types.go + the boundary `validateScouting` fail-louds a non-finite FilmComposite before Apply, matching DT/DE/LB), and `schoolTierNorm` SchoolUnset→0.0 (boundary-attributed, consistent across all positions — GLM itself said "no action in cb.go"). Gate: `make lint` 0 / `go test -race ./...` green / functional gate PASSED live on the Beelink ("pass" — 3I flips green, CBs differentiate). **GLM track record: CB 0 / LB 0 / DE 0 / TE 0 / WR 1 / RB 1 / DT 3 / QB 2.** **CARRY-FORWARD:** SL-018 receding RAS schedule + L3 RAS-buffers (CB 0.25×RAS_norm, §3) + coverage/IDP film weights (incl. CB's 0.30 NGS anchor) pend their data/calibration stage; SL-OQ-033/034 + CAL-026/027 (CB scouting/NGS source) carried. **NEXT = B5b-S** (handoff `23-B5b-S.md`, Build_Tracker row 22) — the near-identical NGS twin (second NGS anchor + FOURTH SL-019 @ 0.30): copy cb.go, swap the few S-divergent constants (RAS steepness **10.0** not 11.0, breakout steepness **11.0** not 10.0, College Production Share = **INT+Tackle** ≤0.08/0.14/≥0.20), add `S.HasNGSAnchor()` so 3I co-asserts S (no flip — already green; no new case). Then B5b-K (row 23) flips the K identity placeholder to real Madden-driven.

**B5b-DE / LB (Build_Tracker rows 19–20) — COMPLETE + MERGED 2026-06-27** (squash DE `202b0e0` / LB `2be23f1`). Full per-build detail (mechanics, weights, GLM findings, carry-forwards) is in `docs/build-handoffs/Build_Tracker.md` rows 19–20 + project memory. Headlines: **DE** = the SECOND SL-019 instance (first on defense — proved `curve.SL019` reuse, strength 0.35, RAS steep 10.0 not TE's 11.0), flipped case 3E; **LB** = the FIRST non-SL-019 IDP (composes DT's SL-005 ±3% film + RB's Medium RAS @ steep 11.0, SL-019 + SL-021 BOTH excluded) + the EDGE classification router (`composition.ResolveRubricPosition`, pass-rush share ≥0.50→DE else LB), flipped cases 3D + 3J + split `cases_eval_dispatch.go`.

**B5b-QB / DT / RB / WR / TE (Build_Tracker rows 14–18) — all COMPLETE + MERGED 2026-06-27** (squash QB `7048881` / DT `c12bd98` / RB `f9875f6` / WR `448a2bf` / TE `6d7a1b3`). Full per-build detail (exact mechanics, weights, GLM findings, carry-forwards) is in `docs/build-handoffs/Build_Tracker.md` rows 14–18 + the project memory. Headlines: **QB** set the offense skeleton + SL-020 (RAS forced 1.000) + the `engine.ScoutingInput` boundary; **DT** set the defense skeleton + SL-021 cushion (both halves) + SL-005 ±3% film + extracted the shared `internal/engine/l4/curve` pkg (M17); **RB** added Medium-tier RAS (rookie weight 0.60) + position-aware `schoolTierNorm` + the Herbert pull (case 3B); **WR** is the canonical High-tier offense template (SL-019 excluded, AD-09 not forced) + the Lockett non-pull (case 3A on WR+QB); **TE** = the FIRST SL-019 RAS-modulator instance (strength 0.35, High RAS steep 11.0) — extracted the shared `curve.SL019` helper (M17) that DE/LB/CB/S all reuse, flipped case 3M.

**Testing Harness v1 COMPLETE + MERGED to main (2026-06-27, squash `0f421ed`; next branch `session/b5b-qb`).** Build_Tracker row 13 — the HARD GATE before any B5b rubric. Two new packages plus the Wails sandbox. **`internal/composition`** — the boundary B5a deliberately omitted: reads B4 params through narrow PORT interfaces (`ParamReader`/`CapReader`, so it is fake-testable with NO live DB) + a harness `PlayerSpec` → `engine.PlayerInput`+`engine.Calibration`. Supplies per-position peak limits (Engine_Spec:118) + L1 hygiene defaults (RASFallback 5.0) until B5b ships them; reads the real cap-tier %s + decay from B4. **Fail-loud on poisoned PLAYER input AND poisoned STORE values** (the boundary is the pure engine's fail-loud gate — defense-in-depth, GLM B1). The engine stays UNTOUCHED and pure (depguard `engine-is-pure` still green; composition imports engine, never the reverse). **`internal/harness`** — the **Module 3 three-state validation runner: PASS / FAIL / PENDING**, where PENDING is an encoded spec awaiting its B5b block and is NEVER counted as a failure (GLM-driven correction to my "92% red" framing). **Rubric-pluggable: cases auto-evaluate the moment a rubric registers in `RubricRegistry`** — proven with a fake rubric (3C is the wired SL-020 exemplar that flips PENDING→PASS; 3L runs today via the real schema decode). Plus Module 1 rookie ranking over identity-L4 (the "scouting baseline") exposing every engine intermediate. **Wails IPC** (`ScoreRookies`/`RunValidationSuite`/`GetParams`/`SetParam`-live-override, all typed/ifaceguard-clean) + **React** (Module 1 table, Module 3 board, live admin sidebar — debuggability over polish). **CONTRACT DECISION held — "version the boundary":** did NOT forward-design `Layer4Input` for all 10 rubrics; B5b adds sub-signal fields localized to engine+composition+harness. **GLM 5.2 BLIND review (inlined, sliced):** 1 real boundary gap (store-value finite/range guard) + 2 real-low (absent-RAS zeroing, doc accuracy) fixed; **2 leads VERIFIED SAFE vs source** (params.Store is RWMutex-synchronized → no IPC race; `NewPipeline(nil)`→identity L4 → no panic). Gate: `wails build` green (Beelink needs `libwebkit2gtk-4.1-dev` + `wails dev -tags webkit2_41`); `make lint` 0; `go test -race` green; **functional gate PASSED — operated live on the Beelink, the board moves on a param change.** Sandbox note: Module 1 uses a fixed league cap ("1000") until a real MFL league is loaded; cap-tier %s + decay are the real admin-tunable B4 values. **NEXT = B5b-QB** (handoff `15-B5b-QB.md`, Build_Tracker row 14) — the first REAL Layer 4 + the offense skeleton-setter; registering it flips Module 3 case 3C green.

**B5a — Engine Pipeline COMPLETE + MERGED to main (2026-06-27, squash `6a47ef6`; next branch `session/testing-harness`).** The pure-function scoring SPINE. **`internal/engine`** — L1 hygiene (RAS imputation + salary floor), L3 decay (`(1-rate)^max(0,age-peak)`), L5 cap scaling (salary%-of-cap → Cold ×1.15 / Neutral ×1.0 / Hot ×0.85), L6 tiebreaker (veteran→RAS→scarcity) as PURE FUNCTIONS; **L4 is a pluggable `Layer4` interface with an IDENTITY default** (Combined 1.0 — real per-position rubrics are B5b; DI seam mirrors B3c's Writer/Reader). `Pipeline.Score(PlayerInput, Calibration) (Result, error)` chains them and accumulates at the two FIXED points (`ScoutingAdjusted = BasePoints × AgePull × Layer4Output.Combined`, then `AdjustedScore = ScoutingAdjusted × CapMultiplier` — Backend_Architecture:259,266). **THE HARD FACT held: the engine imports NO store/db/IO** — depguard `engine-is-pure` GREEN proves it; every input arrives as a PARAMETER via `PlayerInput`+`Calibration` (B4 globals via the composition boundary + per-position values from B5b). **L2 base scoring is DEFERRED — `BasePoints` is a supplied input** (its own block; no L2 file, AD-17 moot). **Fail-loud on non-finite inputs across L3+L5** via a shared `finite()` helper (M17): L3 rejects NaN/Inf age/peak/rate + a negative base (decayRate>1); L5 rejects a non-positive/non-finite cap AND non-finite salary/boundaries. Confidence scores stay OFF `Result` (Hard Constraint). **GLM 5.2 BLIND review found 2 REAL fail-loud gaps** (L3 NaN-pull poisoning the score MED; L5 NaN-salary silently → Neutral LOW) — both triaged to source + fixed PRE-merge; GLM correctly self-dismissed the raw-vs-cleaned-RAS seam as intentional. Gate: `make lint` 0 / `go test -race ./...` green (3 planted-failure gates: div-by-zero cap + 2 NaN). **GLM/opencode HUNG TWICE on repo exploration — the reliable path was INLINING the source into the prompt** (scp self-contained prompt to bird). **NEXT = Testing Harness** (handoff `14-Testing-Harness.md`, Build_Tracker row 13) — a HARD GATE before any B5b rubric; it builds the FIRST composition boundary (reads stores + manual input → engine inputs) and the L4 validation sandbox (Wails/React).

**B4 — Admin Parameter Store COMPLETE + MERGED to main (2026-06-26, squash `9f2c675`; next branch `session/b5a-engine-pipeline`).** The THIRD and FINAL Layer-2 store — **the Layer-2 config floor is now COMPLETE, so B5a (the engine pipeline) is UNBLOCKED.** **`internal/store/params`** — a CONFIG store like B3b but SIMPLER: ships built-in Go DEFAULTS (NO external fetch source), seeds ONCE on a fresh DB, loads existing on restart WITHOUT reseeding (an operator's calibration is never overwritten by a newer default). **Shape (GLM-5.2 design consensus): a GENERIC `(param_key, position)` typed key/value table** with a per-row `[min,max]` range + `is_calibrated` flag, fronted by a TYPED surface (`GetCapTiers()` → Cold 1.2 / Hot 4.8, `GetGlobal(Key*)`, `Definitions()`). Adding a parameter is a DATA ROW, not code — future per-position calibration tables fold in WITH the engine layer that consumes them, never before. **v1.0 scope = Option C** (cross-pollinated w/ GLM 5.2 — the lean-but-not-over-fit choice): seed the Layer-5 cap-tier **percentages** (AD-21 — calibration; the cap AMOUNT stays in B3b, never duplicated) PLUS the two global scalars with stated defaults (Layer-3 decay 0.03, Cushion Guard 8.00/0.10); per-position tables deferred. **Admin-only override path** w/ a `validateOverride` RANGE gate, NEVER through B7 (AD-05); two-lock concurrency, seed in one tx. **GLM 5.2 BLIND review earned its keep again — 3 REAL defects fixed:** NaN bypassed the range gate (`NaN<min`/`NaN>max` both false) and round-tripped silently into calibration → now rejects all non-finite, planted-tested (HIGH); `GetCapTiers` read its two boundaries under two separate RLocks → a concurrent `load()` swap could cross snapshots, now one lock hold (MED); `created_at` overwritten on every upsert → renamed `updated_at` (LOW). 3 forward-risk leads deferred (see handoff `13`). Gate: `make lint` 0 / `go test -race ./...` green (8 tests incl. planted NaN / out-of-range / unknown-param / no-reseed gates). **NEXT = B5a Engine Pipeline** (handoff `13-B5a-Engine-Pipeline.md`, Build_Tracker row 12) — the pure-function L1–L3 + L5–L6 spine with L4 as a pluggable dispatch seam. THE HARD FACT: the engine is depguard-PURE (`engine-is-pure`) — it imports NO store; all inputs (PlayerRecord, scoring config, cap amount, B4 calibration) arrive as PARAMETERS via a later composition boundary. With B5a done, the Testing Harness (row 13, a HARD GATE) precedes any B5b rubric.

**B3c — League State Store COMPLETE + MERGED to main (2026-06-26, squash `52c9760`; next branch `session/b4-admin-parameter-store`).** The SECOND Layer-2 store and the 32-team MUTABLE runtime state. **`internal/store/state`** — owns the `rosters` + `contracts` tables + derived cap usage. **Reader/Writer split enforces the single-writer law (AD-02/AD-05):** `Writer()` (mutations: `MovePlayer`/`SetRosterStatus`/`ApplyContract`) is for B7a's DI ONLY; `Reader()` returns a `readerView` wrapper that holds an UNEXPORTED `*Store` field (not embedded) so it is NOT type-assertable back to the mutation surface — the boundary can't be escaped, proven by `TestReaderCannotMutate`. **`Initialize()` PULLS from a normalize seed `Source` and seeds ONCE; there is NO `Reload()`/versioning** — runtime state is B7-owned at runtime (the deliberate divergence from B3b's config store; an existing DB loads as-is, never reseeds, proven by `TestInitializeDoesNotReseed`). Atomic per-player transactions on the single-writer pool (`db.Pools.Write()`, MaxOpenConns=1); reads deep-copy nested slices; mutations serialize under `wmu` (outer write lock) + `mu` (reader RWMutex) — the two-lock idiom cloned from B3b. **Fail-loud is the house style:** empty-seed guard (`errEmptySeed`), `requireOneRow` (`RowsAffected==1` — a 0-row UPDATE on memory/DB drift errors instead of silently no-op'ing), and an orphan-roster reconcile in `load()` (the rosters⋈contracts inner join is checked against the roster count) — all three proven by planted drift tests. **Franchise identity is PLAYER-DERIVED in v1.0 (Christopher's call):** a franchise appears only while it holds ≥1 player; the canonical 32-team list is owned by a future registry, documented on `Franchises()`. AD-21 held: B4 owns cap-tier percentages, this store owns no cap amount. **GLM 5.2 BLIND review:** cleared the boundary, SQL arg-ordering, and cap math by tracing them; found 2 REAL fail-loud gaps (silent 0-row UPDATE; inner-join orphan drop) — both triaged to source, fixed, planted-tested. Gate: `make lint` 0 / `go test -race ./...` green (14 tests). **NEXT = B4 Admin Parameter Store** (handoff `12-B4-Admin-Parameter-Store.md`) — third Layer-2 store, a CONFIG store like B3b but SIMPLER (ships built-in defaults, no external fetch); admin-only write path NOT through B7 (AD-05); holds engine calibration params incl. cap-tier percentages (AD-21). With B4 done the Layer-2 config floor is complete and B5a (engine) can start.

**B3b — League Rulebook COMPLETE + MERGED to main (2026-06-26, squash `12afc3c`; next branch `session/b3c-league-state-store`).** The FIRST Layer-2 config store — it SET THE STORE TEMPLATE that B3c/B4 clone. Two packages. **`internal/store/rulebook`** (the store): MFL config stored as IMMUTABLE versioned snapshots with one explicit ACTIVE pointer; **`Reload()` SIDE-LOADS** — re-fetches, stores a candidate version, returns a `ChangeSet` (the commissioner-gate signal), and does NOT auto-promote (Christopher's call: stability over a fresh read — reads keep serving the stable active version until confirmed); **`Promote(ver)`** is the one operation that moves active, used for BOTH confirm-new and rollback-to-prior (immutable versions + a repointable pointer = rollback is free); **commissioner overrides** are SEPARATE layered records (a re-pull NEVER clobbers them — proven by `TestOverrideSurvivesReloadPromote`) gated by `validateOverride` (the bad-data guard); all SQL parameterized; `db.Pools` read/write split. **`internal/ingestion/league`** (Layer-1 fetcher): **corrected the handoff premise** — scoring is NOT in the MFL `league` export, it lives in the separate `rules` export (verified live), so the fetcher makes TWO calls and assembles one RAW config; `$t`-XML-wrapper unwrap + MFLList single-element collapse decode. **GLM 5.2 BLIND review found 2 REAL concurrency defects** (HIGH: `GetScoringRules`/`ActiveConfig` returned slices aliasing the in-memory active snapshot → silent corruption; MED: `Promote`/`SetOverride` raced via a shared `loadActive`) — both triaged to source, fixed (deep-copy slice reads; a `wmu` write-mutex serializing admin mutations), re-verified with `-race` tests. The GLM gate earns its keep (vs retired Gemini's false-positive history) — still triage every finding. Gate: `make lint` 0 / `go test -race` green / live `TWR_LIVE_MFL=1` PASS (cap=125, 6 scoring blocks, MG rule decoded). **Verification add-on (Christopher's requirement):** rule-change verification is built (`ChangeSet` + Promote/rollback); the notification *popup* is the deferred thin UI piece; NEW-PLAYER verification stays routed to OQ-013 (roster/sync layer), NOT the rulebook. **NEXT = B3c League State Store** (handoff `11-B3c-League-State-Store.md`) — second Layer-2 store, 32-team MUTABLE runtime state; clones the rulebook FILE SHAPE but DIVERGES: `StateWriter` via DI to B7 ONLY / `StateReader` to all else, `Initialize()` PULLS from B3 (normalize), NO `Reload()` (B7 is the sole runtime mutator, AD-02).

**(Earlier 2026-06-26) B2b-Fetch-Kicker — K FETCHER COMPLETE (branch `session/b2b-fetch-kicker`, `887fda4`, merged in the B2b-Fetch arc squash `2cad6da`).** One new fetcher closes K's data needs; the B2b-Fetch arc's module-close refactors are DEFERRED (Christopher's scope call) to handoff `09-B2b-Fetch-module-close.md`. **kicking** (`internal/ingestion/kicking/`) — K's Layer-4 **NFLProduction** sub-signal (0.40 under DECISION-011). **Recon corrected the handoff premise:** nflverse `player_stats_season` is offense-only (no FG/XP columns); kicker production lives in the SEPARATE `player_stats_kicking_season` release file. Emits RAW gsis-keyed **counting stats only** — FG made/att/missed/blocked, FG-by-distance buckets (`0_19`…`60_`, made+missed), `fg_long`, PAT counts — REG-only, structural zero-leak. **Rate columns (`fg_pct`/`pat_pct`) deliberately unbound:** a rate is made÷att = analysis the engine derives (Approach A), not a fetcher output. **TRADED KICKERS (Christopher's call 2026-06-26):** this file is one row per (player, team, season_type) with NO source cross-team aggregate row (unlike PFR's `[0-9]TM`) — so a traded kicker's per-team REG splits are SUMMED into one season record (counts+buckets add, `fg_long`=max). Legitimate raw-count aggregation, distinct in mechanism from the pfrcoverage prefer-aggregate dedup (no aggregate row exists here; this fetcher emits no rates to recompute). Live 2024: 45 distinct kickers (7 traded, summed); Greg Joseph (NYG 13/16 + NYJ 1/1 + WAS 2/3 → games 8, FGMade 16, FGAtt 20, FGLong=max) spot-checked vs raw. **Madden K (0.60 signal): NO new code** — recon-verified live that m24 carries `kickPower_rating`/`kickAccuracy_rating` (+`awareness`) for kickers, already captured by the `madden` fetcher's dynamic `*_rating` parse and resolved via the existing name+birthdate injected resolver (Cairo Santos 94/85/63, Tyler Bass 96/80/65, etc.). **SL-OQ-042 RESOLVED** (premise superseded by DECISION-011: Madden K is a live signal not archival; whole-roster pull is not position-conditional, so "keep K in routine pulls" holds with zero new logic); **CAL-032 stays open** (predictive utility = calibration). Doc: `docs/data-layer/Kicker_Scouting_Source_Map.md`. Gate: `make lint` 0 / `go test -race ./...` green / live `TWR_LIVE_NFLVERSE=1` PASS. **NEXT = B2b-Fetch module-close** (`docs/build-handoffs/handoffs/09-B2b-Fetch-module-close.md`): M17 pfr→gsis bridge promotion into `crosswalk.Map` (3 consumers: touchshare/ras/pfrcoverage), shared CFBD long-format helper extraction (collegeshare/collegedefense), agy/Gemini re-review of all arc seam changes. That session CLOSES the B2b-Fetch arc → then B3b (League Rulebook).

**B2b-Fetch-Defense — MODULE COMPLETE + MERGED to main (2026-06-26, squash `677102a`; built on `session/b2b-fetch-defense`, branch deleted).** Every defensive scouting input is now fed; **no defensive fetcher remains to build.** New seam + two new fetchers, all RAW/gsis-keyed per THE CONTRACT, zero-leak verified, planted-failure gates, live-verified on CT105. **gzip seam** (`0b6d12b`, `extcsv.go`): `FetchCSVGz`/`StreamCSVGz` over a shared `openCappedCSV` (the cap bounds DECOMPRESSED bytes — also a gzip-bomb guard; a non-gzip body FAILS LOUD at gunzip). **pfrcoverage** (`55d5c28`, `internal/ingestion/pfrcoverage/`) — the CB/S **Coverage anchor** (`scouting.NGSCoverage`). **Recon overturned the handoff premise: nflverse publishes NO defender NGS file** (swept all 25 release tags — `nextgen_stats` is offense-only; its `avg_separation` is the receiver's). **Christopher's call: REBIND the anchor onto PFR advanced defense** (`pfr_advstats` → `advstats_season_def.csv.gz`) — targets/`cmp_percent`/yards allowed, **passer rating allowed** (the headline coverage metric), `dadot`. Coverage-allowed quality, not tracking separation; field name retained, source documented. Counting stats missing-is-zero; rates `*float64` absent≠0 (a 0 passer-rating-allowed is perfect coverage, never faked). **Traded-player dedup (verified live):** prefer PFR's own `[0-9]TM` season-aggregate row, drop the per-team splits; splits with no aggregate are ambiguous → dropped (the fetcher must not sum+recompute rates — that's analysis). pfr→gsis via the injected-resolver pattern. Live 775/2024, all targeted, ratings in [0,158.3]. **collegedefense** (`0b142c9`, `internal/ingestion/collegedefense/`) — the defensive **CollegeProductionShare** breakout signal. The schema slot is ONE monolithic `CollegeProductionShare`, position-defined upstream; the IDP rubrics define it from DIFFERENT within-team market shares (CB: PD+INT; S: INT+Tackle; LB: Tackle+Sack+TFL; DT/DE: TFL+Sack), so this fetcher emits each COMPONENT share RAW (tackle/sack/TFL/PD/INT) + raw counts and the engine combines per position. CFBD `defensive` + `interceptions` categories (recon: INT lives in interceptions, not defensive → two calls; sacks/TFL fractional). Same espn→gsis injected resolver as collegeshare; PPA NEVER fetched. Live 665/2023, all 5 shares in [0,1], top tackle share ~0.186. **DECISION — IDP film ELIMINATED (Option-D parallel):** recon found NO clean automatable source for any IDP film sub-signal (IDPShow/IDPGuru/DynastyNerds are paywalled content brands; PFF/TDN already eliminated on offense) — so 100% of the IDP film component is unautomatable. **Christopher's call: redesign IDP film on Madden defense sub-attrs** (already in m24 `RawMaddenRating.Attributes`: tackle/manCoverage/zoneCoverage/hitPower/…) + NFLProduction + pfrcoverage. **NO new fetcher needed; weights UNSET (separate calibration pass).** Schema `IDPFilm`/`NGSCoverage` annotated (`20f35ec`). Full decision record: `docs/data-layer/Defense_Scouting_Source_Map.md`. **MODULE-CLOSE CARRY-FORWARD (do at the B2b-Fetch arc close, i.e. next session):** promote the pfr→gsis bridge into `crosswalk.Map` as `GSISForPFR` (M17 — now 3 consumers: touchshare/ras/pfrcoverage; currently duplicated in live-test helpers); extract the shared CFBD long-format helpers (M17 — collegeshare/collegedefense duplicate statRow/fetchCategory/parsers/poison-emit); agy/Gemini re-review of the gzip seam + the offense-arc seams; the Film reweight calibration pass (offense AND defense — weights UNSET). **NEXT = B2b-Fetch-Kicker/Archival** (`docs/build-handoffs/handoffs/08-B2b-Fetch-Kicker.md`): K sources (Madden K ratings likely already in m24 + nflverse kicking columns) + SL-OQ-042 archival; closes the B2b-Fetch arc. K is the simplest position — small scope. The `07` handoff is fully executed.

**B2b-Fetch-Offense — MODULE COMPLETE + MERGED to main (2026-06-22, squash `520eb0a`; built on branch `session/b2b-fetch-offense`).** All 9 offense-relevant scouting fetchers built, reviewed, and LIVE-verified on CT105; squash-merged to main after the lint+race gate passed on main and pushed (origin main @ `520eb0a`). This closing session added the final two fetchers + their seams. **CollegeProductionShare** (`6fe195f`, `internal/ingestion/collegeshare/`) — the rookie college-dominator signal, gsis-keyed RAW. The HARD GATE (rookie player-id keying) was RESOLVED by recon before any code: **CFBD's `playerId` IS an espn athlete id, and dynastyprocess `db_playerids.csv` carries both `espn_id` and `gsis_id`** — a clean automated bridge (verified live: Caleb Williams espn 4431611 → mfl 16579 / gsis 00-0039918), beating all three handoff candidates (`/draft/picks`, name-match, defer-to-manual). Christopher's two calls: **gsis-keyed** output (preserves THE CONTRACT + the single engine join path) and the bridge map lives in **the crosswalk package** (one db_playerids fetch → two maps). The crosswalk now exposes `GSISForESPN`; `espn_id` is OPTIONAL there (must not break the foundation MFL→gsis map) and an ambiguous espn→2-gsis is **dropped** per the RAS combine-collision precedent (4 live). collegeshare does TWO CFBD calls (receiving + rushing, no team param = all FBS players in one ~3.5 MiB long-format response each — NOT ~130 per-team calls), team-sums locally, emits within-team REC/YDS/rush shares + raw counts. Keying is via an **INJECTED resolver** (no crosswalk import — decoupled, fake-testable). Live 2023: 407 records, top reception share Malik Washington 0.444 (led FBS). TWO documented live properties: placeholder gsis (e.g. `ALL092954`) for not-yet-played rookies = a consistent join key (OQ-013 reconciles later, this signal's PRIMARY target); reception share is [0,1], yardage shares can be tinily negative (real negative-yardage plays), emitted RAW. **Shared CFBD client** (`0046674`, `internal/ingestion/cfbd.go`) — `NewCFBDClient` (h2-disabled) + `GetCFBD` extracted from schooltier at the 2nd CFBD caller (M17); schooltier refactored + live re-verified (681, identical). **Madden** (`1a382ed`, `internal/ingestion/madden/`) — the cross-position film "regulator" (+ K's majority Layer-4, DECISION-011), gsis-keyed RAW. **Recon degraded the premise (push-back): current-season m26 500s AND m25 is EMPTY — only m24 (2023, two seasons stale) is populated.** Christopher's call: **build lean vs m24 now**. Pages the EA api by offset (1992 records), parses every numeric `*_rating` sub-attr into a name→int map (string ratings like `runningStyle` skipped; non-int number fails loud), keyed via an INJECTED name+birthdate resolver (~77% live, brittleness contained; unresolved = clean miss). Live m24: 1404 records, all ratings [0,99]. **Module close:** zero-leak re-verified FIELD-BY-FIELD across all 9; scouting/types.go source-drift FIXED (TouchShare = snap_counts not FantasyPros; PFFGrade/DraftNetwork/RSP/Sharp marked ELIMINATED-retained-pending-Film-redesign). **3 SEAM CHANGES flagged for module-close agy/Gemini re-review:** IntCell/FloatCell→extcsv, StreamCSV, the new cfbd.go. **NEXT = B2b-Fetch-Defense** (`docs/build-handoffs/handoffs/07-B2b-Fetch-Defense.md`): HARD GATE = the GZIP seam (nflverse next_gen_stats is gzipped, extcsv lacks gunzip) for the CB/S NGS Coverage anchor; plus the parallel IDP source-access decision (expect an Option-D-style elimination → Madden-defense-subattrs fallback). **The rubric FILM REWEIGHT (offense + defense) is a separate CALIBRATION pass — weights UNSET, not a fetcher job.**

**B2b-Fetch-Offense — STREAMING SEAM + VETERAN-FILM + SCHOOLTIER DONE (2026-06-21, branch `session/b2b-fetch-offense`, NOT merged, 17 commits ahead of main).** This arc cleared the two hardest offense fetchers plus a streaming seam, all live-verified on CT105: **`StreamCSV`** (`f4eb542`, `extcsv.go`) — a row-by-row sibling to `FetchCSV` for files too big to buffer (pbp is ~95 MiB; `ReadAll` would OOM CT105's 2 GB), same byte-cap-fail-loud / by-name binding, holds one reused row at a time; M3 over-cap + callback-abort proven in the same commit. **Veteran-Film** (`afc90a4`, `internal/ingestion/veteranfilm/`) — joins FTN charting (human-charted booleans, no player id) to STREAMED pbp (gsis ids, no charting) on `(game_id, play_id)`, attributing each charted play's flags to its receiver/passer gsis, aggregating per player into RAW trait RATES (receiver: contested-catch/created-reception/drop; passer: int-worthy/throwaway). RATES-not-counts is **structural zero-leak** (no fantasy/EPA/WP column is ever bound). Multi-year-capable (pools counters, computes rates once; default `[2025]`); **floor-suppression** is Christopher's call, floors are PARAMETERS (provisional 30 targets / 100 attempts — the value is a calibration decision) with denominators emitted for engine gating. Live 2025: 209 receivers / 45 passers above floor, top-WR drop 2.9%, top-QB int-worthy 2.7%, all rates in [0,1]; end-to-end real-URL test passed in 271 s. **SchoolTier** (`a19d285`, `internal/ingestion/schooltier/`) — CFBD `/teams` → `scouting.SchoolTier` keyed BY SCHOOL (sidesteps the rookie keying gap). It is the one fetcher that **imports `scouting` and emits a FINAL value, not raw** — justified+documented: the tier is position-INDEPENDENT, so no engine-side position normalization is reserved (depguard permits it; `scouting` is a leaf). Edge cases locked by Christopher: **Pac-12 year-aware** (POWER_FOUR ≤2023, GROUP_OF_FIVE ≥2024 after the realignment husk), **Notre Dame → POWER_FOUR** (sole FBS-Independent special-case). Live: 681 schools, P4=68/G5=66/FCS=129/nonFCS=418, spot-checks pass. **CFBD GOTCHAS (carry forward):** it's an authed JSON API (bearer), NOT a CSV — does NOT use extcsv; **HTTP/2 must be DISABLED** (CT105 gets an h2 PROTOCOL_ERROR — `NewClient` pins HTTP/1.1); the **`CFBD_API_KEY` env var has trailing newlines** Go's header validation rejects (callers must `TrimSpace`); lenient JSON decode (external boundary). The CFBD client is LOCAL to schooltier — **extract to a shared `ingestion/cfbd.go` at the 2nd CFBD caller (M17)**. **NEXT = CollegeProductionShare**, but it is GATED on an unresolved **rookie player-id keying** decision (CFBD `playerId` → MFL/gsis for college-only players the crosswalk omits — Christopher's call + a recon pass), then Madden (EA, lower-durability fallback), then module close. **Open the next session by pasting `docs/build-handoffs/handoffs/06d-B2b-Fetch-Offense-collegeshare.md`.**

**B2b-Fetch-Offense — 3 OF 4 nflverse OFFENSE FETCHERS DONE (2026-06-21, earlier in this arc).** This arc added the three "easy" nflverse fetchers on the proven seam, all gsis-keyed RAW per the CONTRACT, all live-verified on CT105: **TouchShare** (`bc842ed`, RB snap workload from snap_counts — per-game→season aggregate, SL-OQ-021 GamesActive honored at Layer 1, injected pfr→gsis map; 637 live), **AgeTrajectory** (`94e3d44`, players.csv birth_date — emits the raw DATE not a derived age since age needs an as-of and would go stale; 24961 live), **RAS** (`bb15e01`, combine.csv measurables — *float64 so missing-is-ABSENT-not-zero, ht parsed from "F-I", injected pfr→gsis map; 4832 live). Plus a SEAM change: **`ingestion.IntCell`/`FloatCell` extracted to extcsv.go** (`eac431f`) and nflproduction refactored onto them (M17 — TouchShare was the 2nd caller; no copy-paste), nflproduction live re-verified 607. **RAS dedup policy = Christopher's call (drop-ambiguous-and-continue, silent exclude):** live data showed combine.csv shares one pfr_id across two distinct players (CB+OLB "Derrick Johnson", gsis `00-0023449`); a gsis with 2+ rows is dropped ENTIRELY (not fail-loud, not first-wins) — lenient EXTERNAL boundary, a clean missing-signal beats a silently-corrupt RAS. All: `make lint` 0 / `go test -race` green / live PASS. **TWO SEAM CHANGES flagged for module-close agy/Gemini re-review:** the IntCell/FloatCell extraction (done) and the pbp StreamCSV (next). **DOC DRIFT to fix at module close:** `scouting/types.go` Profile.TouchShare comment still says "FantasyPros touch share" — it's snap_counts now (Option D). **NEXT = Veteran-Film** (the hard one: FTN→PBP join, needs a streaming extcsv variant first because pbp is hundreds of MB and will fail the 64 MiB cap by design), then the CFBD pair (key cleared) + Madden fallback. **Open the next session by pasting `docs/build-handoffs/handoffs/06c-B2b-Fetch-Offense-veteranfilm.md`.**

**B2b-Fetch-Offense — earlier in this arc (2026-06-20):** First fetcher code landed + reviewed: `internal/ingestion/crosswalk` (MFL→gsis foundation leaf — dynastyprocess `db_playerids.csv`; closes the gsis→MFL crosswalk gap the source map omitted — nflverse players.csv has NO mfl_id; direction is MFL→gsis because gsis→MFL is one-to-many, MFL keeps dup records), `internal/ingestion/nflproduction` (RAW REG-season offensive box-score keyed by gsis; zero-leak is STRUCTURAL — no fantasy field exists to hold the leak), and the shared `internal/ingestion/extcsv.go` seam (`FetchCSV`/`CSVColumns`/`IsMissing` — external-HTTP-CSV, fails loud on byte-cap, binds columns BY NAME). make lint 0 / go test -race green / live-verified (crosswalk 7979, nflproduction 607 REG). Both fetchers passed an **agy First-Instance Template Review** — triaged hard: agy's high-severity findings FAILED triage (false BLOCKERs that would have double-prefixed errors / flagged the project's own M1 idiom), the two that SURVIVED were applied (shared-helper extraction + an M3 cap-gate test). **CONTRACT decided (do not relitigate): scouting fetchers emit RAW; the ENGINE normalizes — position-specific, Engine_Spec Approach-A; the scouting Profile is position-blind, so a fetcher structurally cannot normalize.** **agy team upgrade this session:** version-controlled live memory backbone (`TheWarRoom/GEMINI.md` v2.0, was 9 days stale) + a REPORT CONTRACT (cite-from-clone / stdout / severity rubric — kills agy's fabrication + Autonomy-Mirage + severity-inversion) + a TRIAGE LEDGER learning loop; see `[[feedback-agy-team-flow]]`. 5 commits: `7f9dd32` crosswalk · `990c0ae` nflproduction+seam · `0d4372d` cap-gate test · `ee40c20` handoff 06b · `f9316c0` agy backbone. **Next:** TouchShare / AgeTrajectory / RAS / Veteran-Film per handoff `docs/build-handoffs/handoffs/06b-B2b-Fetch-Offense-remaining.md`. **Open gate (Christopher):** ~~CFBD key~~ **CLEARED 2026-06-20** — key minted + live-verified (HTTP 200), in CT105 env `CFBD_API_KEY`; targets confirmed absent → receiver share = reception/yardage share. CFBD fetchers (CollegeProductionShare, SchoolTier) now unblocked. **REVIEWABLE:** the `RawProduction` field set.

**B2b-Fetch-Offense — SOURCE-ACCESS DECISION LOCKED (2026-06-19), no fetcher code yet. STRATEGIC PIVOT: eliminate manual entry. After 4 recon passes (Claude + Gemini) + dual architectural vote → Option D (Pragmatic Scouting Hybrid): veterans 0 manual, rookies 1 manual input/yr (a consensus draft-rank CSV). PFF / Matt Waldman RSP / The Draft Network / Sharp Football ELIMINATED (no clean automatable source, two sweeps). This FORCES a Film-component redesign (for QB/WR/TE those 4 sources were 100% of the Film component): veteran film = FTN-charting composite (PRIMARY — FTN→PBP join on game_id/play_id verified live) + Madden (FALLBACK — current-season EA API is BLOCKED, all m26 variants 500; birthdates still safe via historical slug). All automated endpoints verified live on CT105: nflverse (player_stats/snap_counts/players/combine/ftn_charting/pbp), CFBD /stats/player/season + /teams, EA ratings-api (historical only). TWO LOCKED PRINCIPLES from the vote: durability NEVER influences a rubric weight (engineering → cache/fallback); quality informs weights only as a fidelity discount via CALIBRATION — the new film weights are UNSET (a calibration-against-data job, not a blind number). Full decision + verified source map + zero-leak ledger: `docs/data-layer/Offense_Scouting_Source_Map.md`. Handoff rewritten: `docs/build-handoffs/handoffs/06-B2b-Fetch-Offense.md`. OQ-015 RESOLVED → Option D. HARD GATE before any fetcher code: (1) Christopher mints a CFBD key (collegefootballdata.com/key) → then confirm targets→reception-share; (2) Madden decision holds. Branch session/b2b-fetch-offense (docs only so far, NOT merged). Review gate: Gemini 3.1 (agy out).**

**B2b-Schema — Scouting Schema COMPLETE + merged (2026-06-19, `4da4b04`). `internal/scouting` leaf locks the unified scouting field set for all 10 positions (`Profile` + `OffenseFilm`/`IDPFilm`/`NGSCoverage` pointer groups + `SchoolTier`/`SafetyRole` enums); position boundaries are STRUCTURAL (Coverage non-nil at CB/S only); zero-leak verified field-by-field; imports only playerid. AD-16 human-review gate passed (per-position walk). SL-OQ-035/036 reserved via `SafetyRole` (S monolithic v1.0). DECISION-011 recorded: K Layer-4 is Madden-driven (Madden 0.60 / NFLProduction 0.40) — reverses K structural-exclusion + AD-10 + SL-020-at-K; mechanics rewrite carried to B5b-K. Also resolved OQ-012 this session (League Schedule Methodology, `179176e`). Next: B2b-Fetch-Offense (scouting fetchers, QB/RB/WR/TE) — real gate there is SOURCE ACCESS (most scouting sources aren't MFL endpoints).**

**Standing build pattern (2026-06-19, all remaining sessions):** open each module session with a **Haiku Explore recon fan-out** over the handoff's READ FIRST docs (cheap recon/fetch tier — gathers an inventory, never owns design/gates/locked-decision reversals); verify its load-bearing claims against source before acting. Codified in `docs/build-handoffs/Handoff_Protocol.md` (Recon Phase + `== RECON ==` template field).

**B2b-Schema close (2026-06-19):** Haiku recon assembled the 10-position scouting-input inventory; Claude verified the load-bearing claims (SL-OQ-035/036 verbatim, NGS=CB/S-only) against source; Claude + Christopher did the design + AD-16 walk. K's Madden-majority direction (DECISION-011) absorbed by the schema with no structural change — `MaddenFilm` was already a core field; only K's characterization was corrected. make lint 0 / go test -race green. Design doc: `docs/scoring-engine/Scouting_Schema.md`.

**B3 — MFL Data Normalization COMPLETE (2026-06-18). Raw→typed domain records; the internal type system is LOCKED (AD-18 reviewed deliverable). OQ-004 (EDGE→DE) + OQ-005 (salaryAdjustments dead-cap) RESOLVED. Live-verified CT105→MFL end-to-end.**

**B3 close (2026-06-18):** `internal/domain` (leaf: `PlayerRecord`/`Roster` + `Position`/`ContractStatus`/`RosterStatus` enums + `PosFlag` admin sentinel) and `internal/normalize` (pure Layer-1: `NewLookup` cross-ref join, `classifyPosition` PK→K/EDGE→DE/XX→FLAG, dirty-`contractStatus` table, salary str→float, RISK-003 site #2 via `playerid.New`, fail-loud per record, deterministic intra-roster id sort). Plus TWO B2-addendum Layer-1 fetchers the join needed: **`players` — now LEAGUE-scoped** (the global `api` feed OMITS commissioner-created players: live ids 0816 Gosnell/0820 Roberts/0835 Childress/0838 Robinson, present in the 2621-record league feed, absent from the 2578 global) and **`salaryadjustments`** (the per-franchise dead-cap ledger resolving OQ-005). **TWO Gemini reviews** (agy still out): normalize (4/5 findings applied incl. determinism sort + reserved-range hardening; the suggested `ToUpper` REJECTED — would break MFL's case-specific aggregate codes Def/Coach/Off/ST) and the salaryAdjustments money path (**BLOCKER**: MFL returns HTTP 200 with `{"error":{"$t":…}}`, which — with this fetcher's deliberate no-empty-sentinel policy — would silently wipe every team's dead cap on an outage; fixed with shared `ingestion.CheckAPIError`). **Live PASS (CT105→MFL): 1217 records / 32 franchises (zero loss), reserved-range invariant clean on the 2621-player DB, salaryAdjustments franchise 0025 = $5.495 ≈ displayed $5.49.** Christopher's calls: reserved-range anomaly **flags+continues** (not halt); negative salary adjustments are **valid** (commissioner credits). New: **OQ-013** (created→official id reconciliation ramp — refresh/sync layer) + **OQ-014** (Money-type / cap-math precision — `Salary float64` kept for B3, decision deferred to cap-math). `salaryadjustments` aggregation into cap usage is downstream engine work (deferred).

**B2 close (2026-06-18):** `internal/ingestion` formalized — shared boundary helpers (`ValidatePlayerID` RISK-003 site #1, `IsTeamAggregateID` 0151–0782 filter, `MFLList[T]` array/object-collapse decoder) + two fetchers: `rosters` (template-setter, league-specific, DiscoverHost-first) and `schedule` (non-league `api`-host path, built against the live-captured `nflSchedule` shape). Gemini 3.1 Pro first-instance review of the rosters template caught TWO real BLOCKERs the linters could not: MFL collapses single-element arrays to bare objects (would crash a 1-player franchise) and an empty `{"rosters":{}}` payload decoded to a silent empty slice (downstream DB-wipe risk) — both triaged against source and fixed (the `MFLList` decoder is now shared so every fetcher inherits it; zero-length guarded with a sentinel). Christopher's two template-contract calls: **fail-loud per record** (don't add partial-success machinery until a dirty fetcher needs it) and **`Fetch` takes year/leagueID as args, not package globals** (data-driven, multi-league/season-ready). Live PASS on the Beelink: rosters 32 franchises / 1217 records (payload-size cross-check confirms the aggregate filter isn't eating real players); schedule 16 matchups, exactly one home team each. **Contracts/salaries fetcher DEFERRED (OQ-005). Aggregate-filter airtight verification (no real player in 151–782) carried to B3's players-join.**

**B1 close (2026-06-18):** `internal/mfl` formalized to WF 1A + 3 T4 fixes; the transport template (`New`/`Do`/`DiscoverHost`, rate-limit + 429 backoff + host routing) is locked — fetchers inherit it, never re-implement it. TWO Gemini 3.1 Pro first-instance reviews (client, then the live test) each caught real BLOCKERs the linters could not: a `NaN` rate slipping past `rps<=0` into the limiter (→ `Wait` blocks forever), and retries bypassing the limiter (→ concurrent storm). Both triaged against source (M13) and fixed. Live smoke test (`TWR_LIVE_MFL=1`, opt-in) PASSED on the Beelink: discovered host `www47`, `Do(rosters)` → 200 with a 140KB real rosters payload. Squash-merged to main. **Q2.1 deferred (Christopher blessed):** a league-specific call made before discovery falls back to the `api` host rather than hard-erroring — "discover first" is a B2 ingestion responsibility, not a transport constraint.

All architecture documents are on disk. Engine specification, 10 position rubrics, UI architecture, backend architecture, Layer 4 pre-build audit, and testing harness specification are complete. MFL scoring rules decoded and verified.

**Session 4 completions:**
- ~~Token-tightening pass~~ — **COMPLETE.** 7 edits across 4 files. All 28 docs surveyed.
- ~~SL-OQ-043~~ — **CLOSED.** Option A. SL-022 assigned. WR SL-019 excluded for v1.0; calibration revisit flagged for v1.1.
- ~~Folder restructure~~ — **COMPLETE.** All numbered folders → `docs/` with semantic kebab-case names. All path references updated across all files.
- ~~GitHub repo~~ — **LIVE.** https://github.com/secureprospective/TheWarRoom (main, initial commit, 28 files).

**Session 5 completions:**
- ~~Planning arc Session 1 (Logical Building Blocks)~~ — **COMPLETE.** 26 blocks defined across 4 tiers. Three-layer architectural law locked. All structural decisions recorded. Full block list at `/root/.claude/plans/yes-each-module-is-immutable-wigderson.md`.

**Session 6 completions:**
- ~~Planning arc Session 2 (Build Sequencing)~~ — **COMPLETE.** 32-session ordered build plan with dependency gates, six structural wireframes, and open flags. Full plan at `/root/.claude/plans/very-good-now-i-replicated-feigenbaum.md`. Self-audited (11 corrections applied) before handoff to Opus 4.8 for Session 3 audit.

**Session 7 completions:**
- ~~Planning arc Session 3 (Build Sequencing Audit, Opus 4.8)~~ — **COMPLETE.** Verdict PASS WITH FLAGS; 25 decisions locked (AD-01–AD-25), reviewed piece-by-piece with Christopher. Audit at `/root/.claude/plans/session-3-audit-build-sequencing.md`. Build plan grew 32→38 sessions (B7 split into B7a–d; B2b split schema-first + 3 fetcher groups; QB-first/DT-second rubric order).
- ~~Three Session 2 open flags~~ — **RESOLVED.** Per-team roster view → M1 drill-down (AD-20); B7 sub-session design → foundation-first split (AD-02); Go overlay label → corrected, reframed as net-new authoring (AD-19).

**Session 8 completions (2026-06-13, Pre-Build Friction Testing):**
- ~~G0 Go overlay~~ — **AUTHORED.** `.golangci.yml` (v2), `.pre-commit-config.yaml` (SHA-pinned), `Makefile.snippet`, `schema/example.go`, `README.md` added to `christopher-coding-standards` on `session/go-overlay-g0` (commit `86bcde6`, NOT YET MERGED). Found and fixed a SEVERE depguard glob bug (`files:` patterns need a leading `**/`, or the three-layer-law check fires on **zero files**).
- ~~T1/T2/T3 friction tests~~ — **COMPLETE.** Full log at `Fable_Friction_Log.md` (13 numbered frictions + final synthesis). Measured (not estimated) confidence: plan-fidelity 78%, realized-enforcement 72%, collab-plumbing 58%, end-goal ~60%.
- ~~B1 transport client (`internal/mfl`)~~ — agy (CT104) built it end-to-end from a self-contained brief, scored 10/10 on T2's lint-based conformance rubric. agy's own First-Instance Template Review (T3) then found 2 real LOGIC bugs the lint pass couldn't see (host-discovery couldn't recover from a stale cached host; a caller-supplied `JSON` param could override the mandatory `JSON=1`) — **both fixed and verified**, commit `d573420` on `session/prebuild-friction-testing`.
- **NEW BLOCKER FOUND:** Friction #12 — CT105's stored GitHub PAT can read but not push to `secureprospective/TheWarRoom` (403). Both session branches (`session/prebuild-friction-testing` @ `d573420`, `session/go-overlay-g0` @ `86bcde6`) are committed locally but **not on origin**.

**Session 9 completions (2026-06-13, Confidence-80 pass — all four metrics ≥80%):**
- ~~Gate 1 — AD-06 enforcement~~ — **LOCKED: struct-wrap.** `playerid.PlayerID` is now `struct { id string }` (unexported field); the bypass `playerid.PlayerID("99")` fails to **compile**. Template `templates/go/playerid/example.go` in coding-standards (`session/go-overlay-g0` @ `b77709b`).
- ~~Gate 2 — `interface{}`/`any` escapes~~ — **LOCKED: custom `ifaceguard` vettool** (`tools/ifaceguard/`), pinned + `analysistest`-guarded, wired into `make lint` + pre-commit. Closes the gap no golangci-lint linter covered.
- ~~Plan-fidelity sweep~~ — **third unenforced-claim found + closed:** the 400-line file cap had no gate (funlen caps functions, not files); added a `filelen` Makefile gate. Gate 5 WF1A contradiction reconciled; Gates 1/2 recorded LOCKED in companion plan Section 10.
- ~~Collab workflow (Section 9)~~ — **REBUILT + VALIDATED.** 9.8 Triage Protocol (new), 9.2 git/SSH-relay dual-channel, 9.6 memory-symmetry corrected. T1 re-run (7/7 gates fire), T4 (agy re-review, **0 hallucinations**, triage exercised), and **T3-as-designed PASSED** (agy's standing clone pulls the pushed branch from git — primary channel live).
- ~~Friction #12 (PAT)~~ — **FIXED.** New fine-grained PAT (TheWarRoom + coding-standards, Contents R/W). All branches pushed to origin.
- **STRATEGIC PIVOT (Christopher, 2026-06-13):** the **local-AI idea is cut.** CT106/AiderBox **retired** (hardware upgrade needed, no fix), Hermes/Ollama brain cut, weak-local-model conformance path **descoped** (not deferred). Collaboration refocuses on **Claude (CT105) + agy/CT104** only — agy is a capable agentic CLI, not a local model, so it is unaffected. See `[[strategic-pivot-claude-agy-only]]` in auto-memory.

**Forward source of truth:** `docs/build-handoffs/Build_Tracker.md` — the 38-session checkable sequence. Read at the start and end of every build session. Every session closes by writing the next session's handoff per `docs/build-handoffs/Handoff_Protocol.md`.

**Immediate next steps (in order) — pre-B0 confidence pass COMPLETE (all 4 metrics ≥80%):**
1. ~~Merge the G0 overlay~~ — **DONE 2026-06-13.** `session/go-overlay-g0` squash-merged to coding-standards `main` (PR #8, `cf45454`, all 6 CI checks green); TheWarRoom `session/confidence-to-80` merged to `main` (`bbe7cf0`). **B0 is unblocked.**
2. Resolve OQ-004 (EDGE mapping) and OQ-005 (salary adjustment) before B3.
3. Build and validate the Layer 4 testing harness per `docs/build-handoffs/Testing_App_Specification.md` (Session 13).
4. First code build: B0 — Project Scaffold (Session 1). **B0 carries forward:** copy the G0 overlay (`.golangci.yml`, `playerid/`, `tools/ifaceguard/`, Makefile, pre-commit) into the repo; commit a pinned toolchain for agy/Claude parity (9.6); apply the B1 fixes from T4 (stale `client.go:36` comment, `time.NewTimer`+`Stop`, `rps>0` guard).
- **DONE this session:** PAT/Friction #12 (FIXED, branches pushed) · AD-06 + interface{} enforcement (LOCKED) · Section 9 collab workflow (REBUILT + VALIDATED, both channels live).

**Next branch:** **B5b-WR is COMPLETE + MERGED to main (2026-06-27, squash `448a2bf`).** Start the next session by branching fresh off main: `git checkout main && git pull && git checkout -b session/b5b-te`. **Open the next session by pasting `docs/build-handoffs/handoffs/19-B5b-TE.md`.** B5b-TE = Build_Tracker row 18 — the tight-end rubric, the FIRST offense position with **SL-019 modulators ACTIVE** (QB/RB/WR all exclude SL-019; TE turns it on in scoreRAS/scoreBreakout). It adds the TE rubric to the EXISTING `internal/engine/l4/offense` package on the shared `curve` helpers: standard Film × RAS × Breakout plus the SL-019 RAS-modulator on the breakout sub-signals — read `TE_Rubric.md` and triage every value. Likely flips/co-gates case 3E (SL-019 breakout modulator) and any TE-gated case; confirm the case wiring at the gate. Handoffs `07`–`18` are fully executed; `19-B5b-TE.md` is written.

**(Superseded) prior brief — `docs/build-handoffs/handoffs/06b-B2b-Fetch-Offense-remaining.md`** — it carries the proven seam, the 4 remaining fetchers with exact sources/gotchas (incl. snap_counts keys on pfr not gsis; pbp is hundreds of MB and needs the extcsv cap raised before Veteran-Film), and this session's learnings. The source-access "real gate" is now RESOLVED → Option D (see `docs/data-layer/Offense_Scouting_Source_Map.md` and the rewritten handoff `06-B2b-Fetch-Offense.md`). **Do NOT re-litigate sources or build a manual manifest — PFF/RSP/DraftNetwork/Sharp are eliminated; fetchers pull CFBD + nflverse + EA + a one manual rookie consensus-rank CSV.** **HARD GATE before any fetcher code:** (1) Christopher mints a CFBD key at `collegefootballdata.com/key`, then confirm CFBD targets→reception-share; (2) Madden current-season stays a lower-durability fallback (EA API blocked). The fetchers populate data; **do NOT set new rubric weights — the Film-component reweight is a separate CALIBRATION pass** (durability never weights; quality only as a calibration fidelity discount). **Collaboration model (UPDATED 2026-06-26): Claude builds, the review gate is GLM 5.2 (Z.ai Coding Plan, via OpenCode on bird). agy/Gemini are RETIRED as code reviewers (Gemini's confident-but-wrong rate was too high — a 5-yr-stale repo it called "updated every cycle", a 404 URL twice, a Madden fix falsified live 3×). The reviewer works BLIND; its output is leads, not findings — TRIAGE every finding against source. Gemini/GLM remain available at troubleshooting/visual gates only.** CT105 reaches MFL + all the above sources (HTTP 200; CFBD needs a key); the Beelink remains the Wails/GUI dev machine (Go 1.26.4 + Wails 2.12 + pnpm + WebKit 4.1; `-tags webkit2_41`).

**SL-022 still active:** WR SL-019 excluded for v1.0 (SL-OQ-043 closed, Option A). Layer 3 carries WR aging. Calibration revisit flagged for v1.1.

---

## Session Start Protocol

1. Confirm branch: `git branch --show-current` — never work on main
2. Read `docs/roadmap/Roadmap_and_Open_Questions.md` — check open OQs that affect the session's work
3. Load the folder(s) relevant to the task:
   - Engine work → `docs/scoring-engine/` + `docs/build-handoffs/Layer4_PreBuild_Audit.md`
   - UI work → `docs/ui/UI_Direction_Document.md` + `docs/backend/Backend_Architecture.md`
   - Data pipeline work → `docs/data-layer/`
   - Transaction work → `docs/transactions/`
   - Testing → `docs/build-handoffs/Testing_App_Specification.md`
4. Build command: `make lint` (ifaceguard + filelen + golangci-lint, all must pass) then `make test` (`go test -race ./...`). Go 1.26.4 is at `/usr/local/go/bin` (NOT on default `$PATH` — Friction #1; prepend it). **On CT105 (2GB RAM), `golangci-lint run ./...` OOM-kills unless you warm the build cache first (`go build ./...`) and cap memory — run lint as `GOMEMLIMIT=1500MiB GOGC=20 golangci-lint run ./...` (or via `make lint` with those env vars set). `.golangci.yml` already pins `concurrency: 1`.** `wails build` produces the desktop binary (needs the GUI libs; headless CT105 compiles it but cannot display it).

---

## Functional Verification on the Beelink (READ before asking Christopher to run anything)

CT105 is headless and its firewall may block outbound — GUI runs and live network tests happen on the **Beelink (192.168.1.190)**. To avoid the B1 round-trips, follow this every time:

1. **PUSH the session branch to origin from CT105 FIRST.** The Beelink pulls from origin; it cannot see an unpushed local branch. (B1 cost a round-trip because the branch wasn't pushed.)
2. The Beelink clone is **`/home/chris/opencode/TheWarRoom`** (verified 2026-07-04; the CT105 `/mnt/storage/...` path does NOT exist there; `/home/chris/.config/TheWarRoom` is just the SQLite data dir). Go is `/usr/local/go/bin/go`.
3. The Beelink may be on a **stale branch** — the paste.md batch must `git fetch origin` before `git checkout <branch>`.
4. Per [[feedback_paste_md_copypaste]]: any command Christopher runs goes in `/root/paste.md`, labeled with why + where (target machine, repo path) + what PASS looks like.

Live/network tests are **opt-in and env-gated** (e.g. `TWR_LIVE_MFL=1`) so they never fire in the default suite/CI; they still compile + lint with everything else (no build tag). Copy `internal/mfl/client_live_test.go` as the pattern.

---

## Hard Constraints (Never Route Around)

- **No work on main. Ever.** Branch naming: `session/<short-description>`.
- **MFL Player IDs are always string type.** IDs under 1000 require leading zeros. Enforce at ingestion boundary. All SQLite columns TEXT.
- **Confidence scores are internal engine flags only.** Never surface in UI.
- **Layer 2 / Layer 4 zero scoring leaks.** No sub-signal within Film, RAS, or Breakout may reference fantasy points, projected volume, MFL scoring config, or format-dependent volume stats.
- **Layer 4 structural mechanics never exposed in Admin UI.** Only admin-tunable parameters per SL-017.
- **NGS Coverage Metrics anchor applies at CB and S only.** Excluded at all other positions — critical check.
- **SL-019 not applied at DT.** Cushion Guard replaces it. Running both would double-protect.
- **Do not reopen locked decisions.** If a locked decision creates a technical constraint that feels wrong, flag it to Christopher — do not route around it silently.
- **Do not add features not in the documents** without Christopher's explicit direction.
- **Never use `git --no-verify`.**

---

## Key Document Map

| Need | Document |
|---|---|
| **Build doctrine — LOAD BEFORE any build/review (both agents)** | `docs/agent-codex.md` — 17 motifs w/ receipts, slop catalog, security baseline, canon→motif map (canonical in coding-standards) |
| Full engine architecture | `docs/scoring-engine/Engine_Specification.md` |
| Any position's rubric | `docs/scoring-engine/<POSITION>_Rubric.md` |
| All rubric parameters in one place | `docs/build-handoffs/Layer4_PreBuild_Audit.md` (Section 1C) |
| Testing harness spec | `docs/build-handoffs/Testing_App_Specification.md` |
| MFL API calls | `docs/data-layer/MFL_API_Specification.md` |
| MFL scoring rules | `docs/data-layer/MFL_Scoring_Rules_Decode.md` |
| UI architecture | `docs/ui/UI_Direction_Document.md` |
| Backend architecture | `docs/backend/Backend_Architecture.md` |
| Open questions + decisions | `docs/roadmap/Roadmap_and_Open_Questions.md` |
| Build sequence + progress (checkable) | `docs/build-handoffs/Build_Tracker.md` |
| Session handoff routine + template | `docs/build-handoffs/Handoff_Protocol.md` |
| Session 3 audit (decisions AD-01–25) | `/root/.claude/plans/session-3-audit-build-sequencing.md` |
| Code companion plan + Claude↔agy collab workflow (Section 9) | `Fable_TheWarRoom_code_plan.md` |
| Pre-build friction test results (T1-T3, 13 frictions, synthesis) | `Fable_Friction_Log.md` |
| Next session brief — path to 80%+ confidence on all 4 metrics | `Fable_Confidence_80_Brief.md` |

---

## Open Items at Last Session Close

| Item | Status |
|---|---|
| B5a — Engine Pipeline (`internal/engine`) | **DONE + MERGED to main 2026-06-27 (squash `6a47ef6`).** Pure-function spine: L1/L3/L5/L6 + L4 pluggable interface (identity default). `Pipeline.Score` accumulates per Backend_Architecture:259,266. L2 deferred (BasePoints supplied). depguard `engine-is-pure` green (NO store/db/IO). Fail-loud non-finite guards L3+L5 (shared `finite()`). make lint 0 / race green (3 planted gates). GLM 5.2: 2 real fail-loud gaps fixed. **Next = Testing Harness (row 13, HARD GATE)** |
| B5a — L3 modulators (SL-018 buffer, SL-021 cushion guard) not built | **OPEN by design — they consume per-position modulator strengths that ship WITH B5b.** B5a computes the RAW age pull only; modulators layer on in B5b |
| B5a — `Layer4Input` carries only `Player` | **OPEN by design — B5b adds the sub-signal fields the first real `Layer4` reads.** The harness's manual form fills them when that rubric lands |
| B5a — composition boundary not built | **OPEN by design — B5a is pure, inputs are parameters.** The Testing Harness (row 13) builds the FIRST composition boundary: stores + manual/MFL input → `engine.PlayerInput`+`Calibration`. Lives OUTSIDE `internal/engine` |
| B4 — Admin Parameter Store (`internal/store/params`) | **DONE + MERGED to main 2026-06-26 (squash `9f2c675`).** Generic `(key,position)` typed calibration store, shipped Go defaults + admin override, seed-once/no-reseed, two-lock. v1.0 = cap-tier %s (AD-21) + global scalars (Option C). Admin-only write, never B7 (AD-05); `validateOverride` range gate. make lint 0 / race green (8 tests). GLM 5.2: 3 real defects fixed (NaN gate, torn read, updated_at). **Layer-2 config floor COMPLETE → B5a unblocked** |
| B4 — `hasDefaults` row-count-only seed check (GLM L2) | **OPEN — forward-risk, not a present defect** (seedDefaults is transactional, no partial seed possible). Revisit with `n >= len(defaultParams())` or a content hash if a param migration is added |
| B4 — SetOverride reloads both full tables per write (GLM L5) | **OPEN — fine at v1.0 row counts.** Revisit when per-position tables make the table large |
| B4 — `Definitions` sort-test checks Key only, not (Key,Position) (GLM L6) | **OPEN — add a position tiebreaker case once a per-position fixture exists** |
| B4 — per-position calibration tables (peak_limit, S-curve, EMA α, …) | **OPEN by design — ship WITH the B5b engine layer that consumes each, never pre-seed into B4** |
| B3c — League State Store (`internal/store/state`) | **DONE + MERGED to main 2026-06-26 (squash `52c9760`).** 32-team mutable state: rosters + contracts + derived cap. Reader/Writer split (Writer→B7a only, un-assertable Reader); Initialize seeds once from normalize, no Reload; fail-loud (empty-seed, RowsAffected==1, orphan reconcile); franchise identity player-derived v1.0. make lint 0 / race green (14 tests). GLM 5.2: 2 real fail-loud gaps fixed |
| B3c — franchise identity player-derived (GLM MED, Christopher: document) | **OPEN by design** — a franchise emptied of all players is absent from `Franchises()`/returns ok=false. Canonical 32-team list owned by a future franchise registry; revisit when B6/M1 need "always 32." Documented on `Franchises()` |
| B3c — cross-call reader snapshot not isolated (GLM LOW-MED) | **OPEN — add a league-wide snapshot read API if a consumer needs it.** B7 is safe (holds `wmu`); engine/IPC readers composing two reader calls can straddle a `load()` swap |
| B3c — `MovePlayer` accepts phantom target franchise (GLM LOW-MED) | **OPEN — target-franchise validity is B7's responsibility** (B3c holds no franchise registry to validate against). Documented as the writer's job |
| B3b — League Rulebook store + `league` fetcher | **DONE + MERGED to main 2026-06-26 (squash `12afc3c`).** `internal/store/rulebook` (versioned snapshots + active pointer, side-loading `Reload`→`ChangeSet`, `Promote` confirm/rollback, layered overrides + validation gate) + `internal/ingestion/league` (two-endpoint `league`+`rules` fetcher, `$t`/collapse decode). make lint 0 / race green / live PASS. GLM 5.2 review: 2 real concurrency fixes (slice-alias, admin-write race) |
| B3b verification add-on (rule-change detection) | **DONE 2026-06-26** — `Reload` side-loads + returns `ChangeSet` (commissioner-gate signal); `Promote` applies-or-rolls-back; overrides survive a re-pull. Notification POPUP = deferred thin UI (later admin session). New-PLAYER verification routed to OQ-013, NOT the rulebook |
| pfrcoverage aggregate-NA silent drop (GLM Layer-1 lead, HIGH) | **OPEN — verify at calibration live-fetch** (recorded in Open Items above) |
| veteranfilm join-key normalization (GLM Layer-1 lead, MED) | **OPEN — hardening at calibration** (recorded in Open Items above) |
| B2b-Fetch-Kicker — kicking fetcher (K NFLProduction 0.40) | **DONE 2026-06-26 (`887fda4`, branch `session/b2b-fetch-kicker`, not merged)** — `internal/ingestion/kicking`. nflverse `player_stats_kicking_season` (separate from offense file). RAW gsis-keyed counts (FG made/att/missed/blocked, distance buckets, fg_long, PAT); rates unbound; REG-only; structural zero-leak. Live 45 kickers |
| Traded-kicker dedup policy | **RESOLVED 2026-06-26 (Christopher) — SUM per-team REG splits** (counts+buckets add, fg_long=max). No source aggregate row exists (unlike PFR `[0-9]TM`); raw-count aggregation, distinct from pfrcoverage prefer-aggregate. Rejected: drop-traded (loses 7/45), REG+POST (still split + playoff bias). Greg Joseph spot-checked vs raw |
| Madden K (0.60 signal) | **NO new code 2026-06-26** — recon-verified m24 carries `kickPower_rating`/`kickAccuracy_rating`(+awareness) for K, already captured by `madden` fetcher's dynamic `*_rating` parse + name/birthdate resolver |
| SL-OQ-042 (Madden K archival pipeline) | **RESOLVED 2026-06-26** — superseded by DECISION-011 (Madden K is a live 0.60 signal, not archival); whole-roster pull not position-conditional, so "keep K in routine pulls" holds with zero new logic. CAL-032 (predictive utility) stays open (calibration) |
| B2b-Fetch arc module-close (M17 + seam review) | **COMPLETE + MERGED to main 2026-06-26.** Item 1 pfr→gsis bridge promotion (crosswalk `PFRMap()`+`addBridge`; behavior-preserving — touchshare 637/pfrcoverage 775 unchanged, ras 4831 under both old+new maps, pfr bridge 7779). Item 2 shared CFBD helpers (`CFBDStatRow`/`FetchCFBDCategory`/`CFBDInt`/`CFBDFloat`/`Share`/`EmitDropAmbiguous[P,T]` in cfbd.go; collegeshare/collegedefense refactored; unit tests unchanged = equivalence proof). Item 3 Gemini seam re-review ran blind → all 6 findings triaged to FALSE POSITIVE against source (lr.N=maxBytes+1 sentinel, value-type build, gzip cleanup-on-error, PFRMap copy+test, pfrIdx>=0 guard, mergeKicking total) → **zero code changes.** **B2b-Fetch arc CLOSED → next module B3b.** |
| B2b-Fetch-Defense — gzip seam | **DONE 2026-06-26 (`0b6d12b`)** — `extcsv.go` `FetchCSVGz`/`StreamCSVGz` over shared `openCappedCSV`; cap bounds decompressed bytes; non-gzip body fails loud at gunzip. **Flag: agy/Gemini seam re-review at arc close** |
| B2b-Fetch-Defense — pfrcoverage (CB/S Coverage anchor) | **DONE 2026-06-26 (`55d5c28`)** — `internal/ingestion/pfrcoverage`. NGSCoverage REBOUND onto PFR advanced defense (nflverse has no defender NGS file). Passer-rating-allowed headline; traded-player `[0-9]TM`-aggregate dedup; rates `*float64` absent≠0. Live 775/2024 |
| B2b-Fetch-Defense — collegedefense (CollegeProductionShare) | **DONE 2026-06-26 (`0b142c9`)** — `internal/ingestion/collegedefense`. CFBD defensive+interceptions; per-component within-team shares (tackle/sack/TFL/PD/INT) RAW, engine combines per position. Same espn→gsis injected resolver; PPA never fetched. Live 665/2023, shares in [0,1] |
| NGSCoverage rebind decision | **RESOLVED 2026-06-26 (Christopher)** — nflverse publishes no defender NGS; CB/S Coverage anchor sourced from PFR advanced defense. Field name retained. Doc: `docs/data-layer/Defense_Scouting_Source_Map.md` §2 |
| IDP film source access | **RESOLVED 2026-06-26 → ELIMINATED (Option-D parallel, Christopher)** — no clean source for any IDP film sub-signal (IDPShow/IDPGuru/DynastyNerds paywalled; PFF/TDN already gone). Redesigned on Madden defense sub-attrs (already in m24) + NFLProduction + pfrcoverage. NO new fetcher; weights UNSET (calibration). Schema `IDPFilm` annotated. Doc §3 |
| pfr→gsis bridge promotion (M17) | **OPEN (module-close)** — 3rd consumer now (touchshare/ras/pfrcoverage); bridge lives only in live-test `livePfrToGSIS`. Promote `GSISForPFR` into `crosswalk.Map`, refactor the three. Do at B2b-Fetch arc close (next session) |
| Shared CFBD long-format helpers (M17) | **OPEN (module-close)** — collegeshare + collegedefense duplicate statRow/fetchCategory/parsers/poison-emit. Extract into shared `ingestion`. Do at arc close |
| Film reweight calibration (offense + defense) | **OPEN — calibration pass.** Eliminated-source fields (OffenseFilm RSP/Sharp, PFFGrade, DraftNetwork, IDPFilm *) retained-pending-redesign, populated by no fetcher. Durability never weights; quality = fidelity discount. Weights set against live data, not blind |
| pfrcoverage aggregate-NA silent drop (GLM lead, HIGH) | **OPEN — verify at calibration live-fetch.** `pickSeasonRow` commits to the `[0-9]TM` aggregate row; if that row's `tgt` is NA while per-team split rows carry targets, `rowCoverage` returns `hasCov=false` and the traded defender is silently skipped (fetcher.go:181) — splits never consulted. Mechanism real; trigger (PFR aggregate with blank counting cols) unconfirmed/unlikely. CHECK against a live 2024 mid-season-traded CB/S: does the `2TM` row populate every counting column the splits do? If not → fall back to summing splits' counting cols. Cannot trip a test. From 2026-06-26 GLM 5.2 Layer-1 freeze review |
| veteranfilm join-key normalization (GLM lead, MED) | **OPEN — hardening at calibration.** FTN↔PBP join (accumulate.go:67 & :136) keys on RAW `game_id`/`play_id`, no `TrimSpace` either arm. Low risk (both nflverse files share id conventions; live run gave sane 209 recv/45 pass) but a partial format skew = silently-wrong rates over a wrong denominator (only an ALL-miss trips `errEmpty`). Add `TrimSpace` both arms as insurance; while there, a charted-but-unjoined miss-counter would surface skew. From 2026-06-26 GLM 5.2 Layer-1 freeze review |
| B2b-Fetch-Offense — TouchShare (RB) | **DONE 2026-06-21 (`bc842ed`)** — `internal/ingestion/touchshare`, snap_counts→season aggregate, injected pfr→gsis, SL-OQ-021 per-active-game (GamesActive). Live 637. RAW; engine normalizes |
| B2b-Fetch-Offense — AgeTrajectory | **DONE 2026-06-21 (`94e3d44`)** — `internal/ingestion/agetrajectory`, players.csv birth_date (raw DATE, not derived age). Live 24961 |
| B2b-Fetch-Offense — RAS | **DONE 2026-06-21 (`bb15e01`)** — `internal/ingestion/ras`, combine.csv measurables (*float64 absent≠0, ht "F-I"→inches, injected pfr→gsis). RAS-EQUIVALENT (engine z-scores), not Platte's number. Live 4832 |
| RAS dedup policy (combine pfr collisions) | **RESOLVED 2026-06-21 — Christopher's call: drop-ambiguous + continue (silent exclude).** Live data: combine.csv shares one pfr_id across two distinct players; a gsis with 2+ rows is dropped entirely (lenient external boundary; ~17 affected). NOT fail-loud, NOT first-wins |
| IntCell/FloatCell seam extraction | **DONE 2026-06-21 (`eac431f`)** — to `extcsv.go`, nflproduction refactored on (M17). **Flag: module-close agy/Gemini re-review** (seam change) |
| `Profile.TouchShare` comment drift | **OPEN (cosmetic)** — still says "FantasyPros touch share"; it's snap_counts now (Option D). Fix at module close |
| `StreamCSV` streaming seam | **DONE 2026-06-21 (`f4eb542`)** — row-by-row `extcsv.go` sibling to `FetchCSV` for files too big to buffer; byte-cap fail-loud, by-name binding, one reused row in memory. M3 over-cap + callback-abort proven same commit. **Flag: module-close seam re-review** |
| Veteran-Film (FTN→PBP) | **DONE 2026-06-21 (`afc90a4`)** — `internal/ingestion/veteranfilm/`, FTN→pbp join on (game,play), RAW per-player trait RATES (receiver: contested/created/drop; passer: int-worthy/throwaway), structural zero-leak. Multi-year-capable (default `[2025]`); floor-suppression (params 30/100, calibration-tunable). Live 2025: 209 recv / 45 pass, rates in [0,1] |
| SchoolTier (CFBD `/teams`) | **DONE 2026-06-21 (`a19d285`)** — `internal/ingestion/schooltier/`, conference→`scouting.SchoolTier` keyed by school. Imports scouting + emits FINAL value (position-independent; documented exception). Pac-12 year-aware (P4 ≤2023), Notre Dame→P4. CFBD = authed JSON, h2 DISABLED, key needs TrimSpace, lenient decode. Live: 681 schools, P4=68/G5=66/FCS=129/nonFCS=418. **CFBD client local — extract to shared `ingestion/cfbd.go` at 2nd CFBD caller (M17)** |
| CollegeProductionShare (CFBD) | **DONE 2026-06-21 (`6fe195f`)** — `internal/ingestion/collegeshare/`. Rookie keying RESOLVED by recon (not the 3 handoff candidates): CFBD `playerId`=espn_id, db_playerids bridges espn→gsis. Christopher: gsis-keyed + bridge in crosswalk pkg (`GSISForESPN`, espn OPTIONAL, ambiguous-drop per RAS precedent). 2 CFBD calls (all FBS/call), local team-sum, within-team REC/YDS/rush shares via INJECTED resolver. Live 2023: 407 records, Malik Washington 0.444 top. Placeholder gsis for rookies = OQ-013 instance; yardage shares can be tinily negative (raw) |
| Madden (EA ratings-api) | **DONE 2026-06-21 (`1a382ed`)** — `internal/ingestion/madden/`. PREMISE DEGRADED on recon (m26 500 AND m25 empty → only m24, 2 seasons stale); Christopher: build lean vs m24. Paged EA api, every numeric `*_rating`→name→int map (string ratings skipped, non-int number fails loud), INJECTED name+birthdate resolver (~77%, brittleness contained). Live m24: 1404 records, ratings [0,99]. Lower-durability fallback behind FTN; weight = calibration |
| CFBD shared client (M17) | **DONE 2026-06-21 (`0046674`)** — `internal/ingestion/cfbd.go`: `NewCFBDClient` (h2-disabled) + `GetCFBD` (bearer, byte-capped, returns bytes for concrete lenient decode). Extracted from schooltier at 2nd CFBD caller; schooltier refactored + live re-verified. **Flag: module-close seam re-review** |
| crosswalk espn→gsis bridge | **DONE 2026-06-21 (`0046674`)** — `GSISForESPN` added to crosswalk.Map from the same db_playerids fetch. espn OPTIONAL (foundation MFL→gsis must not break); ambiguous espn→2-gsis dropped (RAS precedent, 4 live). Live: 7885 espn→gsis entries |
| scouting/types.go source drift | **FIXED 2026-06-21 (`a79aa3c`)** — TouchShare comment = snap_counts (not FantasyPros); PFFGrade/DraftNetwork/RSP/Sharp marked ELIMINATED-retained-pending-Film-redesign. Comments only; field set rework belongs to the Film redesign |
| B2b-Fetch-Offense — source access | **DECISION LOCKED 2026-06-19 → Option D (no code yet).** Eliminate manual; PFF/RSP/DraftNetwork/Sharp eliminated; veterans 0 manual (FTN film + nflverse/CFBD/EA), rookies 1 manual consensus-rank CSV/yr. All endpoints verified live. Doc: `docs/data-layer/Offense_Scouting_Source_Map.md`. Forces Film-component redesign (separate calibration pass — weights UNSET). Gate: CFBD key + targets check; Madden current blocked (fallback) |
| B2b-Fetch-Offense — CFBD API key | **RESOLVED 2026-06-20** — key minted + live-verified on CT105 (`/teams` → HTTP 200; gate cleared, both CFBD fetchers unblocked). Key in CT105 env `CFBD_API_KEY`, not committed. Targets question ANSWERED: CFBD exposes `[LONG,REC,TD,YDS,YPR]`, no targets → receiver `CollegeProductionShare` = **reception/yardage share**. Response is long-format (one row/stat) — share = Σplayer ÷ Σteam. See `docs/data-layer/Offense_Scouting_Source_Map.md` §4 |
| B2b-Fetch-Offense — rubric film reweight | **OPEN — calibration pass.** Eliminated sources were 100% of QB/WR/TE Film. New film = FTN(primary)+Madden(fallback) veterans / consensus-rank+Madden-rookie+combine+CFBD-college rookies. Durability never weights; quality = fidelity discount via CAL. Weights set against live data, not blind |
| B2 — MFL Data Ingestion | **COMPLETE 2026-06-18** — rosters (template) + schedule fetchers + ingestion boundary helpers (`MFLList`, `ValidatePlayerID`, `IsTeamAggregateID`). Gemini review: 2 BLOCKERs fixed. Live PASS (rosters 32/1217, schedule 16). **Merged to main `2866bd4`** |
| B1 — MFL API Client | **COMPLETE 2026-06-18** — transport template locked + live-tested (www47, rosters 200). Merged to main. 2 Gemini reviews, 2 real BLOCKERs fixed |
| Aggregate-filter airtight check (B2 review #1) | **CLOSED 2026-06-20** — proven against LIVE position data: zero real playable-position players carry an id in [151,782] (the rosters ID-range filter never silently drops a real player). New `internal/normalize` tests: `auditAggregateFilter` pure cross-tab + `TestAuditAggregateFilter_*` (offline, plants a real-player-in-range so the gate is seen to fail — teeth) and `TestLive_AggregateFilterAirtight` (opt-in `TWR_LIVE_MFL=1`, hard-asserts on the real DB). FINDING (not a bug): individual `Coach`/`PN` aggregates carry high sequential ids OUTSIDE the reserved block — caught by normalize's position-code filter (the join backstop), not the range filter; range stays [151,782] (widening would swallow real players sharing that id space). Two filters, two concerns, defended in depth |
| Players-endpoint fetcher | **NOT built** — B2 was rosters + schedule. B3's cross-reference join (and OQ-004 EDGE) needs it; build in B3 or as a B2-addendum (non-league `api` host, cache once/day) |
| OQ-012 League (fantasy) schedule source & schema | **RESOLVED (methodology) 2026-06-19** — Christopher supplied the deterministic NFL-mirrored schedule methodology; full spec at `docs/data-layer/League_Schedule_Methodology.md` (13-wk regular season + wks 14–17 playoffs, home/away + inaugural power-rank seeding). Generator build is a separate slot. Merged `179176e` |
| B2b-Schema — Scouting Schema | **COMPLETE + merged 2026-06-19 (`4da4b04`)** — `internal/scouting` leaf, all 10 positions, AD-16 walk passed, zero-leak verified, lint 0 / race green. SL-OQ-035/036 reserved via `SafetyRole`. Doc: `docs/scoring-engine/Scouting_Schema.md` |
| DECISION-011 — K Layer-4 Madden-driven (0.60/0.40) | **RECORDED 2026-06-19** — reverses K structural-exclusion + AD-10 + SL-020-at-K. Schema impact none (MaddenFilm already core). **Carried to B5b-K:** Film cap/curve calibration + K-rubric/AD-10/SL-020 mechanics rewrite. K rubric flagged on disk |
| OQ-004 EDGE position mapping source | **RESOLVED (B3, 2026-06-18) — EDGE→DE.** MFL labels edge rushers DE; 0 live EDGE records. `normalize` maps directly; no consensus-check machinery |
| OQ-005 Salary adjustment line item | **RESOLVED (B3, 2026-06-18)** — `salaryAdjustments` export (league-scoped) = per-franchise dead-cap ledger; `internal/ingestion/salaryadjustments` built + live-verified (0025=$5.495). Cap usage = Σ(salaries)+Σ(adjustments); negatives valid. Aggregation deferred to cap-math |
| B3 — MFL Data Normalization | **COMPLETE 2026-06-18** — `internal/domain` + `internal/normalize` (type system LOCKED, AD-18). 2 Gemini reviews (1 money BLOCKER fixed). Live PASS 1217/32. On `session/b3-mfl-normalization`, awaiting squash-merge |
| Players-endpoint fetcher | **BUILT (B3, 2026-06-18) — LEAGUE-scoped** (`internal/ingestion/players`). Global feed omitted commissioner-created players; league feed (2621) includes them. All 1217 rostered ids resolve |
| OQ-013 Created→official id reconciliation ramp | OPEN — auto-replace commissioner-created id with MFL's official id on refresh; deferred to refresh/sync layer, manual swap is the fallback. Noted 2026-06-18 |
| OQ-014 Money type / cap-math precision | OPEN — `domain.PlayerRecord.Salary` is `float64` (safe at scale; cap math must avoid exact `==`). Money-type decision deferred to cap-math layer. Noted 2026-06-18 |
| Q2.1 league-call-before-discovery | **SATISFIED at B2** — `rosters.Fetch` calls `DiscoverHost` first (ingestion owns "discover before any league-specific call"); schedule is a non-league `api` call, no discovery |
| OQ-006 Cap tier calibration | OPEN — resolve after live data |
| OQ-007 Scouting layer weight | OPEN — resolve after testing |
| OQ-008 Franchise tag calculation timing | OPEN |
| OQ-009 RFA eligibility window | OPEN |
| OQ-010 Playoff bid rules trigger | OPEN — needs commissioner confirmation |
| Session 3 audit pass | COMPLETE — Engine_Spec v3.0, Universal_Rubric_Template v1.2 |
| Go overlay (Phase 2 in coding-standards repo) | **MERGED to main** — PR #8 (squash `cf45454`), all 6 CI checks green. AD-06 struct-wrap, interface{} ifaceguard, file-cap filelen; 7/7 + 2 gates verified. **B0 unblocked.** |
| GitHub PAT push access (Friction #12) | **FIXED** — new fine-grained PAT (TheWarRoom + coding-standards, Contents R/W). All branches pushed; T3-as-designed validated the shared-repo channel live |
| AD-06 enforcement mechanism (`playerid.PlayerID` bypass) | **RESOLVED — struct-wrap** (Gate 1). Bypass fails to compile. `templates/go/playerid/example.go` |
| `interface{}`/`any` escape enforcement | **RESOLVED — `ifaceguard` vettool** (Gate 2). `tools/ifaceguard/`, pinned + tested, wired into `make lint` + pre-commit |
| 400-line file cap enforcement | **RESOLVED — `filelen` Makefile gate** (plan-fidelity sweep). funlen caps functions not files; this is the file gate |
| B1 transport client (`internal/mfl`) | T3 fixes pushed (`d573420`). T4 re-review flagged 3 more minor items for the *real* B1 (Session 2): stale `client.go:36` comment, timer leak (`time.NewTimer`+`Stop`), `rps>0` guard |
| Claude↔agy collab workflow (Section 9) | **REBUILT + VALIDATED** — 9.8 Triage Protocol, 9.2 dual-channel (git primary / SSH-relay secondary), 9.6 memory corrected. T3-as-designed + T4 passed (T4: 0 hallucinations) |
| Local-AI idea / CT106 / Hermes | **CUT (2026-06-13)** — local-AI dropped; CT106/AiderBox retired (hardware), weak-model path descoped; collab = Claude + agy only. See `[[strategic-pivot-claude-agy-only]]` |
| Cross-project cleanup (pivot follow-up) | OPEN — coding-standards Layer-11/Phase-3 (local-model guidance) now moot; Local AI Stack project CLAUDE.md needs a wind-down pass |

---

*Built by: Christopher Campbell + Claude (Anthropic)*
