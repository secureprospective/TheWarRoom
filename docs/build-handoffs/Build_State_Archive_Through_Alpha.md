# Build State Archive — Through Alpha Gate (2026-07-27)

Archived from `CLAUDE.md`'s "Current Build State" narrative and "Open Items at Last Session Close"
table, which had grown to ~25k tokens of accumulated per-session history (every "Prior:" entry kept
forever) — big enough that a plain file read hit the harness's token cap and truncated. Christopher's
call: archive the full history here (nothing lost), keep `CLAUDE.md` itself to current-state-only.

Every OQ referenced below that is still genuinely open lives authoritatively in
`docs/roadmap/Roadmap_and_Open_Questions.md` — that file, not this archive, is the live source of
truth for open questions. This archive is a historical record only; don't treat anything below as
current without checking it against `CLAUDE.md`'s present "Current Build State" line and the roadmap
doc first.

---

## Build-State Narrative (chronological, most recent first)

**Commissioner Suite build sequence, Session 0 — taxi/IR cap-math correctness fix MERGED to main (`5800a05`).** First session of a new locked 10-session (0-9) plan (`/root/.claude/plans/commissioner-suite-build-sequence.md`), derived from a link-by-link audit of MFL's real commissioner panel against this codebase. Fixed a live cap-math bug: MFL's `includeTaxiWithSalary`/`includeIRWithSalary` discount settings were never ingested — every rostered player counted 100% toward cap regardless of taxi/IR status, working only by coincidence because the real league's values happened to both be "100". Also fixed `normalizeRosterStatus` fail-loud erroring on MFL's real `"IR"` roster-status value (any franchise with an IR player would have broken roster ingestion outright — confirmed none exist yet, but IR/taxi are a mainstay 32-team dynasty mechanic Christopher expects to accumulate in-season). `RawConfig` gained the three MFL fields; `loadCellCap` now discounts a taxi/IR player's cap CONTRIBUTION (not their raw `CapSalary`, which other rule math like dead-cap/buyout still reads undiscounted) via a new `CapDiscounts` interface, override-aware through the existing rulebook `scopeSetting` mechanism (no new override machinery needed). `domain.RosterIR` joins the roster-status enum. `LeagueControls.tsx` gained a commissioner-facing IR/taxi slot-count control (0 = mechanic off), backed by two new IPC methods (rulebook writes bypass the B7 Coordinator by design). **GLM 5.2 was down this session — review ran via DeepSeek instead**: two real findings fixed (`capPercent` had no [0,100] clamp — an out-of-domain override could invert/overcount cap; the new IPC methods had no server-side input validation, only a frontend `.trim()` that isn't a real boundary). **Live gate on the Beelink** against a sandboxed copy of the real league DB: boots clean, confirmed real data (794 ROSTER/37 TAXI_SQUAD/0 IR), confirmed the currently-active stored rulebook config predates this fix (no discount fields yet) so cap math is numerically unchanged today — no regression. Christopher confirmed the built binary (`./build/bin/thewarroom`) directly. `make lint` 0 / `go test -race ./...` green.

**League matchup schedule now populates the Commissioner Calendar — MERGED to main.** New read-only ingestion (`internal/ingestion/leagueschedule`) pulls MFL's `schedule` export (the fantasy league's own weekly matchups — who plays whom, real franchise ids — distinct from the already-built, still-unwired `nflSchedule`/real-NFL fetcher). Follows the B-5 degradation-contract pattern exactly: `league_schedule_cache` mirrors `standings_cache.go` (non-append-only, latest-copy cache), `leagueScheduleOrCache` in `leagueschedule_app.go` mirrors `standingsOrCache` (live fetch → cache → honest stale/fail fallback via the shared `Freshness` contract, `fallbackParent()` context discipline included). `CalendarBoard.tsx` gained an Agenda/Matchups toggle — the Matchups pane is read-only (no stage/preview/confirm, since it's MFL-sourced reference data, not commissioner intent), franchise ids resolved to real names via the existing `rulebook.FranchiseNames()`. **GLM 5.2 was down this session — review ran via DeepSeek instead** (split backend/frontend, both under the payload size that resets the connection): caught one real bug — the home/away swap in `GetLeagueSchedule` assumed exactly one side is marked `isHome`, so a malformed MFL matchup (both sides "1" or both "0") would silently mislabel which franchise is home; fixed by having `Validate()` enforce the invariant at the shape layer, with regression tests for both malformed shapes. A frontend lead (0-0 tie rendering "vs") was triaged FALSE against source — `homeScore`/`awayScore` are generated as `string`, not `number`, so a non-empty `"0"` is already truthy in JS. **Live gate on the Beelink caught a second real bug the review didn't**: a future playoff week with no bracket seeded yet (`week 14 has zero matchups`) was failing the ENTIRE season's fetch — `Validate()` was treating a legitimate empty week as a glitch; fixed to only guard a fully-empty response. Re-verified live against Christopher's real league after the fix — matchups render with real names. `make lint` 0 / `go test -race ./...` green / tsc+vite clean.

**🚨 ALPHA GATE PASSED — 2026-07-27.** Christopher built (`wails build -tags webkit2_41`) and ran the Alpha binary (`./build/bin/thewarroom` — Wails packages the binary lowercase, note for next build) on the Beelink against his real 32-team league. **The one item B-5 harden left unverified — the outage test — was run for the first time and PASSED**: M2 rendered live, network disconnected, reload showed all 32 franchises under the amber `CACHED` strip with legible numbers, reconnect cleared the strip on the next reload. This was the last open item on the ALPHA GATE checklist (handoff 58). **TheWarRoom is now genuinely live for Christopher's real league**, not just merged-and-tested.

**STAGE 2 OF THE ROADMAP BUILD PLAN — trade rationale/picks-note + persisted trade audit + Week-9 deadline MERGED to main (`e936c90`).** `Trade` now carries a required `Rationale` (validated non-empty before a tx opens) and an Alpha-scope free-text, deliberately UNVALIDATED `PicksNote` (locked via the DeepSeek+Gemini panel — no pick-ownership ledger yet, that's post-Alpha). **The trade EVENT itself is now persisted for the first time** — `acquisitions.Trade()` had only ever recorded the player-move side effects; a new `trade_notes` table (additive `CREATE TABLE IF NOT EXISTS`, matching every sibling `init*Schema`) + `LogTradeNote` fixes that, capturing every franchise a leg touched (both source AND destination — see the DeepSeek-caught bug below). A commissioner-settable **§14 Week-9 trade deadline** hard-blocks `TRADE` once passed, riding the exact same `season_phases.meta` sub-phase-directive mechanism the existing UFA signing window already established (`trade_deadline.go` mirrors `signing_window.go` 1:1, including a redundant-toggle guard). `Trade`/`PlayerMove` split out of `request.go` (was at the 400-line `filelen` cap) into `request_trade.go`, the established per-op-family convention. **GLM 5.2 was down this session — review ran via DeepSeek instead**: caught one real bug (`involved_franchises` only recorded each move's DESTINATION, so a one-sided trade — player-for-picks, nothing coming back — silently dropped the sending franchise from the audit row; fixed by reading each player's pre-move franchise via `TxWriter.Player()` before `acquisitions.Trade` runs, with a dedicated regression test `TestExecute_TradeNoteRecordsSourceFranchise`) plus the redundant-toggle guard on `AppendTradeDeadline`. Two DeepSeek leads triaged FALSE (a claimed `buildMoneyRequest` fallthrough risk misunderstood Go `switch` semantics; the read-pool consistency concern mirrors the pre-existing `signing_window.go` pattern exactly, not a new regression). `make lint` 0 / `go test -race ./...` green / tsc+vite clean. **Live gate on the Beelink PASSED** (trade with rationale+picks note committed; deadline set in the past blocked a subsequent trade; clearing the deadline restored trading). Frontend: `TradeBuilder.tsx` gained required Rationale + optional PicksNote textareas gating "Review trade…"; `LeagueControls.tsx` gained a "Trade deadline (§14)" commissioner card mirroring the Signing Window control. **Stage 2 was the last item on the roadmap build plan — both stages complete.**

**STAGE 1 OF THE ROADMAP BUILD PLAN — `make verify` pre-push gate MERGED to main (`13e991a`).** Codeword `roadmap`'s market-research pass (docs/sources/, uncommitted) surfaced the Trade struct's picks gap; before touching that schema, Christopher routed the "do we need CI" question through an independent expert-AI panel (DeepSeek + Gemini, triaged against source — Gemini's schema answer was discarded as fabricated, DeepSeek's CI answer held up). **Locked: no GitHub Actions CI** — this repo is direct-to-main with no PR mechanism, so a remote check has nothing to gate; a **pre-push local gate is the correct shape instead.** `make verify` (lint + `go test -race ./...` + frontend build) now runs at git push via the existing pre-commit framework (`.pre-commit-config.yaml`), scoped to `stages: [push]`; the three existing commit-time hooks (golangci-lint, gitleaks, ifaceguard) got explicit `stages: [commit]` so they don't silently re-run redundantly at push once the push hook existed (pre-commit's undocumented-until-now default: omitting `stages` means "run at every installed stage"). **GLM 5.2 review (blind, direct-curl to z.ai, `finish_reason=stop`)** caught one real BLOCKER-class lead — the `verify` hook lacked `always_run: true`, so a push with an empty computed file delta (tag-only push, identical-tree merge) could silently skip the entire gate in exactly the corner case it exists for — now fixed. Also caught a MAJOR: no bootstrap step installed both hook types together, so a clone that ran only the plain `pre-commit install` would never get the pre-push gate with no visible warning — fixed via a new `make setup` target (installs both). Two GLM leads (`-race` newly load-bearing; `frontend/dist` possibly not gitignored) checked against source and found FALSE — both pre-existed / were already correct, no action.

**M2/M4 REFACTORS (handoffs 43/44) COMPLETE — MERGED to main (`1d23ff8`, live functional gate PASSED on the Beelink 2026-07-25).** **(1) M2 service extraction (Session 43):** new `internal/m2service` package now owns the aggregate/blend/join orchestration (`parseStanding`/`buildBlendInputs`/`aggregateScouting`/`buildRows`/`starterCount`/`clampWeight`/`franchiseDisplayName`) that used to live inline in `m2_app.go`; `GetPowerRankings` is now a thin adapter mirroring how `ScoreLeague` delegates to `rankings.Runner`. 11 new unit tests. **(2) M4 read-model store (Session 44):** new `frontend/src/store/transactions.ts` (Zustand) owns the IPC calls that `TransactionWorkspace`, `TradeBuilder`, and `LeagueControls` each used to fetch independently, per the `harness.ts` WF5 pattern; ephemeral per-op UI state stayed local per Christopher's standing ruling against a full store migration. **GLM 5.2 review split across bird (M2) + Hermes (M2 tests + M4 frontend) in parallel** — both `finish_reason=stop`. 2 real leads fixed: the M2 error-path `AggMode` echo returning the raw unresolved caller argument instead of the normalized value; `TransactionWorkspace.pickFranchise` clearing the opposite roster/pool slot AFTER the await instead of before (a stale-render window). Live gate on the Beelink: M2 board PASS; M4 Transact/Trade/League Controls PASS including the fast-switch stress case.

**UI B-5 HARDEN COMPLETE — MERGED to main (`53fea25`). THE B-1→B-5 UI BUILD TRACK IS DONE.** 📄 Handoff 58 is the full record. Four of five items shipped; item 5 (in-app Alpha feedback capture) CUT by Christopher (Alpha's audience is one person who already has better channels). **(1) The degradation contract:** `Freshness` DTO (`live`/`stale`/`fail` + `fetchedAt`) shared by M1 and M2; `standings_cache` holds last-known-good MFL standings and is the ONLY MUTABLE table in `internal/store/state` — every sibling is an append-only ledger with `RAISE(ABORT)` triggers. `GetPowerRankings` no longer treats a standings failure as fatal. Offseason is deliberately NOT a fourth freshness state (rides a separate `Phase` field). Data is NEVER dimmed/blurred/hidden to signal staleness. **(2) §1 delta-in-weight:** `+Δ` = font-weight 600, `−Δ` = 400. **(3) Dead `RankRow` fields stripped** (`agePull`/`l4Combined`/`capTier`). **(4) Board keyboard nav** J/K/Enter. **GLM 5.2 review split across bird (backend) + Hermes (frontend)** — caught a hole that would have silently defeated the entire cache: `standingsOrCache` passed the CALLER's context to the fallback read, so a timeout-triggered fallback failed instantly even with good cached data sitting right there. Fixed via `fallbackParent()`. Grid tracks hand-counted balanced in all four states (8/9/6/6).

**UI B-4b COMPLETE — M1 diagnostic sweep-out + Home 2×2 landing MERGED to main (`7f35a6d`).** M1 sweep: board holds the locked facet map exactly (`Rank·Player·Pos·Franchise·Base·Adjusted·Salary`) — AgePull/L4/CapTier removed to the B-4a Inspector. Adj/$M kept as the deliberate exception (locked "Cap efficiency view," a board-level lens an Inspector can't express). Home 2×2 replaces the placeholder — reads LOCAL state only (no live MFL calls), so a network failure can't blank the landing. Seasonal cards phase-driven not date-driven. GLM review split bird/Hermes after a 33.5KB combined payload reset the connection twice at ~6 min — 4 leads fixed.

**UI B-4a COMPLETE — Contextual Inspector MERGED to main (`f028aaf`).** Click an M1 row → per-player score anatomy (hero score, six layer bars, composite rollup, contract/cap block). Backend seam `GetPlayerScore(mflID)` is a pure read projection off already-persisted `season_scores`. Bug fixed en route: coverage scouting anchor fetched the current `SeasonYear` (2026), which nflverse doesn't carry yet — now uses `SeasonYear-1` like offense-film (see [[lesson_thewarroom_completed_season_anchors]]).

**UI B-3 COMPLETE — commissioner calendar agenda board MERGED to main (`f370e5c`).** Agenda/list board (not month-grid+drag) over the append-only `calendar_events` backend. Faithful to the append-only contract — reschedule/cancel re-send the head row's kind+payload verbatim. Auto-fire deliberately out of scope.

**UI B-2 COMPLETE — remaining modules MERGED to main (`13ca56d`).** Every remaining module migrated onto the Session-C token contract + `.twr-board*` grammar. Earlier B-2 increment (`433d35b`): M1/M2 board restyle onto the shared Session-B board grammar.

**Module-3 validation harness COMPLETE** — case 3G wired + MERGED (`e149c57`; 3H already done `ebe8287`). PFF retired everywhere. `pfrpassrush` fetcher stays unwired into scoring by decision — C-1 live evidence found the pressure composite largely redundant with the locked Madden IDP anchor; live DT/DE pressure weight deferred to the expert-panel gate. **Nothing in Module 3 is PENDING anymore.**

**League-calendar append-only backend MERGED to main** (`c22be1e`). Backend-only: `internal/store/state/calendar.go` (append-only `calendar_events` + double-immutable triggers), `internal/transactions/request_calendar.go` (SCHEDULE/RESCHEDULE/CANCEL), `GetCalendarEvents` IPC.

**Alpha Versioning & Releases — ALL 3 TIERS DONE** (T1 build-stamp+tag `v0.5.0`, T2 ledger-safety `facb73c`, T3 self-protection `0b971ca`).

**UI Build Track — COMPLETE.** B-1 shell (`ce710bc`) · B-2 M1/M2 (`433d35b`) + all remaining modules (`13ca56d`) · B-3 calendar agenda board (`f370e5c`) · B-4 (4a Inspector `f028aaf` + 4b sweep/Home `7f35a6d`) · B-5 harden (`53fea25`).

**Scouting engine:** S-Phase 0–4, 4b, and Thread B (crosswalk consolidation) all MERGED. FILM Thread C is COMPLETE — offense arm (`OffenseFilm`, `4e4a0ef`), IDP arm (`IDPFilm`, `a335de6`), and Coverage (`b0f5f46`) all MERGED + LIVE; the 5 weight knobs are LOCKED (expert-panel gate cleared 2026-07-20). Only remainder: the K5 rookie-tower manual-CSV loader is DATA-BLOCKED, deferred to OQ-016.

---

## Open Items at Last Session Close (archived — resolved items only; genuinely open items live in `docs/roadmap/Roadmap_and_Open_Questions.md`)

| Item | Status |
|---|---|
| B5a — Engine Pipeline (`internal/engine`) | DONE + MERGED 2026-06-27 (squash `6a47ef6`). Pure-function spine: L1/L3/L5/L6 + L4 pluggable interface. GLM 5.2: 2 real fail-loud gaps fixed. |
| B4 — Admin Parameter Store (`internal/store/params`) | DONE + MERGED 2026-06-26 (squash `9f2c675`). Generic `(key,position)` typed calibration store. GLM 5.2: 3 real defects fixed (NaN gate, torn read, updated_at). |
| B3c — League State Store (`internal/store/state`) | DONE + MERGED 2026-06-26 (squash `52c9760`). 32-team mutable state: rosters + contracts + derived cap. GLM 5.2: 2 real fail-loud gaps fixed. |
| B3b — League Rulebook store + `league` fetcher | DONE + MERGED 2026-06-26 (squash `12afc3c`). GLM 5.2 review: 2 real concurrency fixes (slice-alias, admin-write race). |
| B3b verification add-on (rule-change detection) | DONE 2026-06-26 — `Reload` side-loads + returns `ChangeSet`; `Promote` applies-or-rolls-back. |
| B2b-Fetch-Kicker — kicking fetcher | DONE 2026-06-26 (`887fda4`) — `internal/ingestion/kicking`. Live 45 kickers. |
| Traded-kicker dedup policy | RESOLVED 2026-06-26 — SUM per-team REG splits. |
| Madden K (0.60 signal) | No new code needed 2026-06-26 — already captured by the `madden` fetcher's dynamic parse. |
| SL-OQ-042 (Madden K archival pipeline) | RESOLVED 2026-06-26 — superseded by DECISION-011. |
| B2b-Fetch arc module-close (M17 + seam review) | COMPLETE + MERGED 2026-06-26. pfr→gsis bridge promoted into `crosswalk.Map`; shared CFBD long-format helpers extracted; Gemini seam re-review found 0 real issues. |
| B2b-Fetch-Defense — gzip seam | DONE 2026-06-26 (`0b6d12b`). |
| B2b-Fetch-Defense — pfrcoverage (CB/S Coverage anchor) | DONE 2026-06-26 (`55d5c28`). Live 775/2024. |
| B2b-Fetch-Defense — collegedefense (CollegeProductionShare) | DONE 2026-06-26 (`0b142c9`). Live 665/2023. |
| NGSCoverage rebind decision | RESOLVED 2026-06-26 — sourced from PFR advanced defense (nflverse has no defender NGS file). |
| IDP film source access | RESOLVED 2026-06-26 → ELIMINATED (Option-D parallel) — redesigned on Madden defense sub-attrs + NFLProduction + pfrcoverage. |
| pfrcoverage aggregate-NA silent drop (GLM lead, HIGH) | Noted as a live-data check to run at calibration — mechanism real, trigger unconfirmed. Not resolved by code; watch at calibration pass if this project resumes calibration work. |
| veteranfilm join-key normalization (GLM lead, MED) | Noted hardening item (add `TrimSpace` both join arms) — low risk, not yet applied. Watch at calibration pass. |
| B2b-Fetch-Offense — TouchShare (RB) | DONE 2026-06-21 (`bc842ed`). Live 637. |
| B2b-Fetch-Offense — AgeTrajectory | DONE 2026-06-21 (`94e3d44`). Live 24961. |
| B2b-Fetch-Offense — RAS | DONE 2026-06-21 (`bb15e01`). Live 4832. |
| RAS dedup policy (combine pfr collisions) | RESOLVED 2026-06-21 — drop-ambiguous + continue. |
| IntCell/FloatCell seam extraction | DONE 2026-06-21 (`eac431f`). |
| `StreamCSV` streaming seam | DONE 2026-06-21 (`f4eb542`). |
| Veteran-Film (FTN→PBP) | DONE 2026-06-21 (`afc90a4`). Live 2025: 209 recv / 45 pass. |
| SchoolTier (CFBD `/teams`) | DONE 2026-06-21 (`a19d285`). Live 681 schools. |
| CollegeProductionShare (CFBD) | DONE 2026-06-21 (`6fe195f`). Live 2023: 407 records. |
| Madden (EA ratings-api) | DONE 2026-06-21 (`1a382ed`). Live m24: 1404 records. |
| CFBD shared client (M17) | DONE 2026-06-21 (`0046674`). |
| crosswalk espn→gsis bridge | DONE 2026-06-21 (`0046674`). Live: 7885 entries. |
| scouting/types.go source drift | FIXED 2026-06-21 (`a79aa3c`). |
| B2b-Fetch-Offense — source access | DECISION LOCKED 2026-06-19 → Option D. Doc: `docs/data-layer/Offense_Scouting_Source_Map.md`. |
| B2b-Fetch-Offense — CFBD API key | RESOLVED 2026-06-20 — key minted + live-verified. |
| B2 — MFL Data Ingestion | COMPLETE 2026-06-18. Merged `2866bd4`. |
| B1 — MFL API Client | COMPLETE 2026-06-18. Merged. |
| Aggregate-filter airtight check (B2 review #1) | CLOSED 2026-06-20 — proven against live position data. |
| Players-endpoint fetcher | BUILT (B3, 2026-06-18) — LEAGUE-scoped, all 1217 rostered ids resolve. |
| OQ-012 League (fantasy) schedule source & schema | RESOLVED (methodology) 2026-06-19. Merged `179176e`. |
| B2b-Schema — Scouting Schema | COMPLETE + merged 2026-06-19 (`4da4b04`). |
| DECISION-011 — K Layer-4 Madden-driven (0.60/0.40) | RECORDED 2026-06-19. |
| OQ-004 EDGE position mapping source | RESOLVED (B3, 2026-06-18) — EDGE→DE. |
| OQ-005 Salary adjustment line item | RESOLVED (B3, 2026-06-18) — `salaryAdjustments` export. |
| B3 — MFL Data Normalization | COMPLETE 2026-06-18 — type system LOCKED (AD-18). |
| Q2.1 league-call-before-discovery | SATISFIED at B2. |
| Session 3 audit pass | COMPLETE — Engine_Spec v3.0, Universal_Rubric_Template v1.2. |
| Go overlay (Phase 2 in coding-standards repo) | MERGED to main — PR #8 (squash `cf45454`). B0 unblocked. |
| GitHub PAT push access (Friction #12) | FIXED — fine-grained PAT (TheWarRoom + coding-standards, Contents R/W). |
| AD-06 enforcement mechanism (`playerid.PlayerID` bypass) | RESOLVED — struct-wrap. |
| `interface{}`/`any` escape enforcement | RESOLVED — `ifaceguard` vettool. |
| 400-line file cap enforcement | RESOLVED — `filelen` Makefile gate. |
| B1 transport client (`internal/mfl`) | T3 fixes pushed (`d573420`). |
| Claude↔agy collab workflow (Section 9) | REBUILT + VALIDATED. |
| Local-AI idea / CT106 / Hermes | CUT (2026-06-13) — local-AI dropped, collab = Claude + agy only (historical; superseded — collab is now Claude + GLM/DeepSeek per the GLM Build Workflow). |

**Items from this table that were still genuinely open as of the archive date (2026-07-27) and were moved forward, not dropped:** OQ-006 (cap tier calibration), OQ-007 (scouting layer weight), OQ-008 (franchise tag timing), OQ-009 (RFA eligibility window), OQ-010 (playoff bid rules trigger), OQ-013 (created→official player-ID reconciliation — now folded into Commissioner Suite Session 1), OQ-014 (money type/cap-math precision), OQ-016 (K5 rookie-tower manual loader), film reweight calibration (offense+defense), pfrcoverage aggregate-NA edge case, veteranfilm join-key `TrimSpace` hardening. All of these live in `docs/roadmap/Roadmap_and_Open_Questions.md` — check there, not here, for current status.

---

## Commissioner Suite Sessions 0-2 (archived 2026-07-28)

**Session 0 — Taxi/IR cap-math correctness fix.** Merged `5800a05`. Traced a live bug: MFL's `includeTaxiWithSalary`/`includeIRWithSalary`/`includeTaxiWithContractYear` config fields existed in the league export but not in `RawConfig`, so `loadCellCap` summed every rostered player's salary unconditionally regardless of taxi/IR status — correct today only by coincidence (both real settings happened to be `"100"`). Fixed by threading the fields through, adding an `IR` roster status to the domain enum, and adding a commissioner IR/Taxi slot-count override (`rulebook.SetOverride`, `0` = mechanic disabled) reusing existing override machinery rather than inventing a parallel one.

**Session 1 — Activity/Transaction Feed + OQ-013 reconciliation.** Merged `ae584c9`. First session run end-to-end on the new GLM Build Workflow (GLM 5.2 implements, DeepSeek blind-reviews, GLM triages/fixes, Claude independently re-verifies and merges). Built a read-only chronological feed (`internal/store/state/feed.go`) — a single parameterized UNION ALL across the five existing append-only ledger tables, no schema change, no writes. `FeedBoard.tsx` shipped as a third summon-strip facet reusing the Session-D event grammar on backward-looking data. DeepSeek's blind review found 1 real MAJOR (a silent `ELSE 'RELEASE'` default that would have mis-labeled any future/unknown player status) + 3 real MINOR (dead code, a lexicographic-vs-numeric ID tiebreaker bug, an SQL/Go case-sensitivity mismatch) — zero false positives, all fixed with new pinning tests. OQ-013's reconciliation seam lives in `transactions_feed_app.go`'s `GetFeed`: a stale commissioner-created id renders as `PlayerUnknown=true` rather than hard-failing. **Live-gated PASS on the Beelink binary, 2026-07-28.**

**Session 2 — Transaction Correction + Roster/Position/Taxi/IR Enforcement (backend only).** Merged `4d80e1c`. The correction mechanism was a durable architecture decision (append-only-correction vs. true undo/delete), so it went through an expert panel first — GLM 5.2 (bird) and DeepSeek independently converged on append-only, reasoning from the codebase's own established `RAISE(ABORT)` append-only convention across every sibling ledger table plus league-governance/auditability arguments (a commissioner able to silently delete a mistake is worse for dispute resolution than an honest append-only correction trail). Locked: a `transaction_corrections` table mirroring the `calendar_events` idiom (same logical tx_id, new row per state change, latest-row-wins via `MAX(seq)`), plus a reconciled/net-state read projection so the feed shows corrections without ever hiding the original. Roster/position/taxi/IR limit enforcement added via a `RosterPolicy` port composed in app.go from the rulebook + players-DB.

**Process note — GLM quota exhaustion mid-session, Claude finished from the WIP:** GLM implemented the backend to a green build+test state, then hit its 5-hour z.ai quota mid-session with the work uncommitted. Claude committed the WIP, fixed a compile error and all 13 lint findings, then ran a 3-way blind DeepSeek review (split state/transactions/app.go to avoid a payload-size connection reset on the 74KB diff — the split-review pattern already proven on B-5 Harden). The review found 1 real BLOCKER (Trade's per-position net-delta math double-subtracted outgoing, letting an over-limit trade commit silently — verified the fix by reverting it and confirming a new regression test caught the bug, then re-applying), 1 real MAJOR (no test exercised the enforcement through the real Coordinator path with a non-nil policy — exactly why the BLOCKER was invisible to existing tests), and 5 real MINORs, all fixed with zero false positives adopted. **Frontend correction UI (the "Correct this entry" action) was never reached — flagged as a fast-follow, not silently dropped.** **Live-gated PASS on the Beelink binary (backend/enforcement half only), 2026-07-28.**

**Live-gate batching directive (2026-07-27, Christopher's call):** stop running the Beelink live/functional gate after every individual session; defer and batch them into one pass instead, tracked in `docs/build-handoffs/PENDING_LIVE_GATE_CATALOG.md`. That file is the live, current test tracker going forward — check there, not here, for what's pending.
