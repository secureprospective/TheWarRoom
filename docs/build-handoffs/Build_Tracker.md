# TheWarRoom — Build Tracker
**Version:** 1.0 — June 2026
**Status:** Operational. The forward source of truth for the build. Check a session off only when its close gate fully passes.
**Origin:** Session 2 sequencing plan (`very-good-now-i-replicated-feigenbaum.md`) as corrected by the Session 3 audit (`session-3-audit-build-sequencing.md`). Audit decisions referenced as `AD-##`.

This is the document opened at the start and end of every build session. One line per session. Read the legend once; it governs the whole build.

---

## Legend

**Status markers** (the checkbox):

| Mark | Meaning |
|---|---|
| `[ ]` | Not started |
| `[~]` | In progress (branch open, code being written) |
| `[x]` | **Complete** — all three close gates passed (see below) |
| `[!]` | Blocked — an open question or upstream gate is unresolved |

**A session is `[x]` only when all three close gates pass:**
1. **Build green** — `golangci-lint` clean, `go test ./...` (and `pnpm test` for frontend) pass. No `--no-verify`, ever.
2. **Functional verification** — Christopher used the actual behavior in the real app, not just "build succeeded."
3. **Handoff written** — the next session's handoff prompt is produced per `Handoff_Protocol.md` and saved, ready to paste when the session is cleared.

**Column meaning:**
- **Gate** — what must be true *before* the session starts (upstream dependency + any open question that blocks it).
- **WF** — the wireframe the session's code must match (anti-spaghetti contract). `—` = no code wireframe (dev artifact / scaffold has its own).
- **L** — architectural layer (1 = Real Football read-only, 2 = Logic Engine, 3 = User Interaction, — = infra/dev).

**Standing rules every session inherits** (from AGENTS.md + the three-layer law):
- File target < 250 lines, hard cap 400. Refactor over.
- All external input schema-validated at the boundary before business logic.
- Parameterized SQL only. No hardcoded secrets/IPs.
- No layer mixing. Modules never write B3c except through B7. Stores never import each other.

---

## Pre-Build Gate (must close before Session 1)

- `[x]` **G0 — Go overlay** — **MERGED 2026-06-13** (christopher-coding-standards PR #8, squash `cf45454`, all 6 CI checks green). golangci-lint v2 + depguard three-layer rules, struct-wrap playerid, ifaceguard vettool, filelen gate, SHA-pinned pre-commit. Copied into TheWarRoom + proven by deliberate-violation test at B0.

---

## Build Sequence (38 sessions)

### Tier 0 — Scaffold

| # | Status | Session | Scope | Gate | WF | L |
|---|---|---|---|---|---|---|
| 1 | `[x]` | **B0 — Project Scaffold** | Go module, Wails v2, React+Tailwind+Zustand, SQLite WAL lifecycle hooks, golangci-lint + pre-commit, AGENTS.md + SYSTEM_MAP.md, IPC ping-pong. Locks the 5 patterns (AD-03). **COMPLETE 2026-06-17: all 3 gates passed — build green (make lint 0 / go test -race / wails build); First-Instance review via Gemini Collaboration Loop (agy out of usage), findings applied; functional verify on Beelink (window + Ping → ✓ pong, journal mode wal). Handoff 02-B1.md written.** | G0 closed | B0 | — |

### Tier 1 — Data Pipeline (Layer 1)

| # | Status | Session | Scope | Gate | WF | L |
|---|---|---|---|---|---|---|
| 2 | `[x]` | **B1 — MFL API Client** | HTTP transport only: rate limiting, backoff, host routing. `Do()` + `DiscoverHost()`. No domain types. **COMPLETE 2026-06-18: all 3 gates passed — build green (make lint 0 / go test -race, 8 tests); TWO Gemini first-instance reviews (client + live test) caught 2 real BLOCKERs the linters missed (NaN past rps>0 guard; retries bypassing the limiter → storm), both triaged + fixed; live smoke test PASS on Beelink (host www47, rosters 200, 140386 bytes, real payload). Squash-merged to main. Handoff 03-B2.md written.** | B0 | 1A | 1 |
| 3 | `[x]` | **B2 — MFL Data Ingestion** | Layer-1 fetchers → Raw records. **COMPLETE 2026-06-18: rosters (template-setter) + schedule fetchers; ingestion boundary helpers (`ValidatePlayerID` RISK-003 #1, `IsTeamAggregateID` 0151–0782, `MFLList` array/object decoder). Gemini first-instance review on rosters caught 2 BLOCKERs (single-element array collapse; empty-payload silent drop) — triaged + fixed; fail-loud kept (#3); `Fetch(ctx,c,year[,leagueID])` params not globals (#4A). make lint 0 / go test -race green; live PASS on Beelink (rosters 32 franchises / 1217 records; schedule 16 matchups). Contracts fetcher DEFERRED (OQ-005). Handoff 04-B3.md written.** | B1 · **OQ-005** | 1B | 1 |
| 4 | `[x]` | **B3 — MFL Data Normalization** | Raw → typed internal records. RISK-003 via `internal/playerid` (AD-06). Internal types locked — **reviewed deliverable** (AD-18). **COMPLETE 2026-06-18.** `internal/domain` (leaf: PlayerRecord/Roster + Position/ContractStatus/RosterStatus enums + PosFlag admin sentinel) + `internal/normalize` (pure Layer-1: Lookup cross-ref join, classifyPosition PK→K/EDGE→DE/XX→FLAG, dirty-contractStatus table, salary str→float, RISK-003 site #2, fail-loud). **OQ-004 RESOLVED (EDGE→DE)** + **OQ-005 RESOLVED (salaryAdjustments dead-cap ledger).** Two B2-addendum fetchers also shipped: `players` (LEAGUE-scoped — fixes commissioner-created-player omission) + `salaryadjustments`. **TWO Gemini reviews:** normalize (4/5 applied incl. determinism sort + reserved-range hardening; ToUpper REJECTED — would break case-specific aggregate codes) + salaryAdjustments money path (**BLOCKER**: MFL HTTP-200 `{"error"}` payload would silently wipe dead-cap → shared `ingestion.CheckAPIError` guard). make lint 0 / go test -race green; **live PASS (CT105→MFL): 1217 records / 32 franchises, reserved-range invariant clean on 2621-player DB, salaryAdjustments 0025=$5.495.** Reserved-range anomaly now FLAGS+continues (not halt, Christopher). New OQ-013 (created→official id ramp) + OQ-014 (Money type). Handoff 05-B2b-Schema.md written. | B2 · ~~OQ-004, OQ-005~~ ✓ | 1C | 1 |
| 5 | `[x]` | **B2b-Schema — Scouting Schema** | **COMPLETE 2026-06-19.** `internal/scouting` leaf (`Profile` + `OffenseFilm`/`IDPFilm`/`NGSCoverage` conditional groups + `SchoolTier`/`SafetyRole` enums) — unified field set, all 10 positions, AD-16 walk passed with Christopher. NGS boundary structural (Coverage non-nil at CB/S only); zero-leak verified field-by-field; leaf imports only playerid. **SL-OQ-035/036 reserved** via `SafetyRole` (monolithic v1.0). **DECISION-011** (K Layer-4 Madden-driven 0.60/0.40) recorded — reverses K structural exclusion + AD-10 + SL-020-at-K; mechanics rewrite carried to B5b-K. make lint 0 / go test -race green. Design doc: docs/scoring-engine/Scouting_Schema.md. | B1 · B3 *(design-ordering, not code dep — AD-13)* | 1B | 1 |
| 6 | `[x]` | **B2b-Fetch-Offense** | Scouting fetchers for offense-relevant sources (unblocks QB/RB/WR/TE). **COMPLETE 2026-06-21 (branch session/b2b-fetch-offense).** 9 fetchers built + reviewed + LIVE on CT105: crosswalk (MFL->gsis FOUNDATION + espn->gsis bridge; 7979/7885), nflproduction, touchshare, agetrajectory, ras, veteranfilm (FTN->PBP streamed; 209 recv/45 pass), schooltier (CFBD /teams; 681), collegeshare (CFBD season stats, espn->gsis keyed; 407/2023), madden (EA m24, name+birthdate INJECTED resolver; 1404). Seams: extcsv StreamCSV + IntCell/FloatCell, shared cfbd.go (M17), crosswalk espn->gsis + name-keying via injected resolver. Source-access pivot LOCKED (Option D — PFF/RSP/DraftNet/Sharp eliminated; FTN primary + Madden fallback). Madden current-season BLOCKED (m26 500, m25 empty) — m24-only fallback, flagged. Zero-leak verified field-by-field. make lint 0 / race green / live PASS each commit. Doc drift fixed (scouting comments). Handoff 07-B2b-Fetch-Defense.md written. | B2b-Schema | 1B | 1 |
| 7 | `[x]` | **B2b-Fetch-Defense** | **COMPLETE 2026-06-26 (merged to main `677102a`).** Every defensive scouting input fed; no defensive fetcher remains. New gzip seam (`FetchCSVGz`/`StreamCSVGz`). Two new fetchers, RAW/gsis-keyed, zero-leak, live-verified CT105: **pfrcoverage** (CB/S Coverage anchor — recon found nflverse has NO defender NGS file → REBOUND onto PFR advanced defense `advstats_season_def.csv.gz`, passer-rating-allowed headline; traded-player [0-9]TM-aggregate dedup; live 775/2024) and **collegedefense** (defensive CollegeProductionShare — CFBD defensive+interceptions, per-component within-team market shares tackle/sack/TFL/PD/INT RAW for engine to combine per position; live 665/2023, shares in [0,1]). **DECISIONS:** NGSCoverage rebind onto PFR; **IDP film ELIMINATED (Option-D parallel)** — no clean source for IDPShow/IDPGuru/DynastyNerds (+PFF/TDN already gone), redesigned on Madden defense sub-attrs (already in m24) + NFLProduction + pfrcoverage, NO new fetcher, weights UNSET. Doc: `docs/data-layer/Defense_Scouting_Source_Map.md`. Schema IDPFilm/NGSCoverage annotated. Carry-forward (module-close): promote pfr→gsis bridge into crosswalk (M17, 3 consumers), extract shared CFBD long-format helpers (M17), agy/Gemini gzip-seam re-review, Film reweight calibration. Handoff 08 written. | B2b-Schema | 1B | 1 |
| 8 | `[x]` | **B2b-Fetch-Kicker/Archival** | **K fetcher COMPLETE 2026-06-26 (branch `session/b2b-fetch-kicker`, `887fda4`).** One new fetcher: **kicking** (`internal/ingestion/kicking`) — K's Layer-4 NFLProduction signal (0.40, DECISION-011). Recon corrected the handoff: nflverse `player_stats_season` is offense-only; kicking lives in the separate `player_stats_kicking_season` file. RAW gsis-keyed counts only (FG made/att/missed/blocked, FG-by-distance buckets, fg_long, PAT), rates unbound (engine-derived), REG-only, structural zero-leak. **Traded kickers SUMMED across per-team REG splits** (no source aggregate row exists; Christopher's call — legit raw-count aggregation, distinct from pfrcoverage prefer-aggregate). Live 45 distinct kickers; Greg Joseph sum spot-checked vs raw. **Madden K (0.60): NO new code** — m24 kickPower/kickAccuracy already captured by `madden` fetcher's dynamic `_rating` parse + name/birthdate resolver (recon-verified live). SL-OQ-042 RESOLVED (DECISION-011); CAL-032 open. Doc: `docs/data-layer/Kicker_Scouting_Source_Map.md`. make lint 0 / race green / live PASS. **MODULE-CLOSE DONE + WHOLE B2b-Fetch ARC SQUASH-MERGED to main 2026-06-26:** M17 pfr→gsis bridge promoted into `crosswalk.Map` (`PFRMap()`/`addBridge`; behavior-preserving, ras 4831 under old+new maps) + shared CFBD long-format helpers extracted to `cfbd.go` (`CFBDStatRow`/`FetchCFBDCategory`/`CFBDInt`/`CFBDFloat`/`Share`/`EmitDropAmbiguous[P,T]`; unit tests unchanged = equivalence proof) + Gemini seam re-review (blind; all 6 findings triaged FALSE-POSITIVE against source → 0 code changes). **The B2b-Fetch arc is COMPLETE.** Next = B3b (row 9). | B2b-Schema | 1B | 1 |

### Tier 2 — Logic Engine (Layer 2)

| # | Status | Session | Scope | Gate | WF | L |
|---|---|---|---|---|---|---|
| 9 | `[x]` | **B3b — League Rulebook** | **COMPLETE + merged to main 2026-06-26 (squash `12afc3c`).** First Layer-2 store; SET THE STORE TEMPLATE. `internal/store/rulebook` — immutable versioned config snapshots + explicit active pointer; `Reload()` SIDE-LOADS (candidate version + `ChangeSet`, never auto-promotes — stability over freshness); `Promote(ver)` = confirm OR rollback; commissioner overrides as separate layered records w/ `validateOverride` gate; all SQL parameterized; concurrency-hardened (slice-copy reads, serialized admin writes — from GLM review). `internal/ingestion/league` — Layer-1 fetcher; CORRECTED handoff premise (scoring is in the separate `rules` export, NOT `league` — two calls); RAW `$t`-unwrapped collapse-tolerant decode. make lint 0 / race green / live `TWR_LIVE_MFL=1` PASS (cap=125, MG rule decoded). GLM 5.2 review: 2 REAL concurrency findings, fixed. Next = B3c (row 10). | B3 | 2 | 2 |
| 10 | `[x]` | **B3c — League State Store** | **COMPLETE + merged to main 2026-06-26 (squash `52c9760`).** Second Layer-2 store. `internal/store/state` — owns `rosters` + `contracts` tables + derived cap usage. **Reader/Writer split** enforces the single-writer law (AD-02/AD-05): `Writer()` to B7a only; `Reader()` returns a wrapper that is NOT type-assertable to the mutation surface (planted test proves it). `Initialize()` **pulls** from normalize and seeds ONCE — **no `Reload()`/versioning** (runtime state is B7-owned at runtime, the deliberate divergence from B3b). Atomic per-player tx writes on the single-writer pool; reads deep-copy; mutations serialize under `wmu`. Fail-loud throughout (empty-seed guard, `RowsAffected==1`, orphan-roster reconcile in load). Franchise identity is **player-derived in v1.0** (Christopher's call — documented limitation; canonical 32-team list owned by a future registry). Mutations defined: `MovePlayer`/`SetRosterStatus`/`ApplyContract` (B3c defines the writer surface; does NOT wire B7). make lint 0 / race green (14 tests incl. concurrency + 2 planted drift tests). GLM 5.2 blind review: cleared boundary/SQL/cap by tracing; 2 REAL fail-loud gaps found + fixed + planted-tested. Next = B4 (row 11). | B3 | 2 | 2 |
| 11 | `[x]` | **B4 — Admin Parameter Store** | **COMPLETE + merged to main 2026-06-26 (squash `9f2c675`).** Third + final Layer-2 store — Layer-2 config floor now COMPLETE → B5a unblocked. `internal/store/params` — a CONFIG store like B3b but SIMPLER: ships built-in Go defaults (NO external fetch), seeds ONCE on a fresh DB, loads existing on restart (no reseed). **GENERIC `(param_key, position)` typed key/value table** + per-row `[min,max]` + `is_calibrated` flag; TYPED public surface (`GetCapTiers`/`GetGlobal`/`Definitions`) over generic storage — adding a param is a data row, not code, so per-position tables fold in with their engine layer. v1.0 scope = Option C (GLM-5.2 consensus): Layer-5 cap-tier **percentages** (Cold 1.2 / Hot 4.8 — AD-21, NOT the cap amount which is B3b) + global scalars (Layer-3 decay 0.03, Cushion Guard 8.00/0.10). Admin-only override path w/ `validateOverride` RANGE gate, NEVER through B7 (AD-05); two-lock concurrency. make lint 0 / race green (8 tests). GLM 5.2 blind review: 3 REAL defects fixed (NaN range-gate bypass HIGH, `GetCapTiers` torn read MED, `created_at`→`updated_at` LOW) + planted NaN/out-of-range/no-reseed gates; 3 forward-risk leads deferred. Next = B5a (row 12). | B0 | 2 | 2 |
| 12 | `[x]` | **B5a — Engine Pipeline** | **COMPLETE + merged to main 2026-06-27 (squash `6a47ef6`).** `internal/engine` — the PURE-FUNCTION scoring spine. Layers as pure functions: L1 hygiene (RAS imputation + salary floor), L3 decay (`(1-rate)^max(0,age-peak)`), L5 cap scaling (salary%-of-cap → Cold ×1.15 / Neutral ×1.0 / Hot ×0.85), L6 tiebreaker (veteran→RAS→scarcity). L4 a **pluggable `Layer4` interface w/ identity default** (Combined 1.0; real per-position rubrics are B5b) — DI seam mirrors B3c. `Pipeline.Score` chains them and accumulates at the two fixed points (`ScoutingAdjusted = BasePoints × AgePull × Combined`, then `× CapMultiplier` — Backend_Architecture:259,266). **L2 base scoring DEFERRED — `BasePoints` is a supplied input** (own block; no L2 file to split, AD-17 moot). **All calibration arrives as PARAMETERS** via `PlayerInput`+`Calibration`; engine imports NO store/db/IO (**depguard `engine-is-pure` green proves it**). Fail-loud on non-finite inputs across L3+L5 (shared `finite()` helper, M17). Confidence scores stay off `Result` (Hard Constraint). make lint 0 / race green (3 planted-failure gates incl. div-by-zero + 2 NaN). GLM 5.2 blind review: 2 REAL fail-loud gaps (L3 NaN-pull MED, L5 NaN-salary mislabel LOW) found + fixed pre-merge. Next = Testing Harness (row 13, HARD GATE). | B3b · B3c · B4 | 3 | 2 |
| 13 | `[x]` | **Testing Harness** | **COMPLETE + merged to main 2026-06-27 (squash `0f421ed`).** v1 = the composition boundary B5a omitted + the Wails sandbox (Module 1 + Module 3). `internal/composition` — reads B4 params via narrow PORT interfaces (fake-testable) + harness input → `engine.PlayerInput`+`Calibration`; supplies per-position peak limits + L1 defaults until B5b; **fail-loud on poisoned player AND store values** (the boundary is the pure engine's fail-loud gate); engine stays untouched/pure (depguard green). `internal/harness` — **Module 3 three-state runner PASS/FAIL/PENDING** (PENDING = encoded spec awaiting its B5b block, NEVER counted as FAIL; **rubric-pluggable → cases auto-evaluate when a rubric registers**, proven w/ fake rubric; 3C wired exemplar, 3L runs today) + Module 1 rookie ranking over identity-L4 scouting baseline exposing every engine intermediate. Wails IPC (ScoreRookies/RunValidationSuite/GetParams/SetParam-live-override, ifaceguard-clean) + React (Module 1 table / Module 3 board / live admin sidebar). **GLM 5.2 blind review: 1 real boundary gap + 2 real-low fixed; 2 leads verified safe vs source.** wails build green (webkit2gtk-4.1); lint 0; race green; **functional gate PASSED — operated live on Beelink, board moves on param change.** Next = B5b-QB (row 14). | B5a | — | — |

### Tier 2 — L4 Position Rubrics (QB first, DT second, then resume — AD-15)

| # | Status | Session | Scope | Gate | WF | L |
|---|---|---|---|---|---|---|
| 14 | `[x]` | **B5b-QB** | **COMPLETE + merged to main 2026-06-27 (squash `7048881`).** First REAL Layer 4 + offense skeleton-setter. New pure pkg `internal/engine/l4/offense`: QB rubric `Combined = film × RAS × breakout` (Backend_Architecture:256). **SL-020: RASEffective forced to exactly 1.000** (`const`, RAS unread — unbypassable). Film = S-curve over an upstream composite, **neutral 1.000 when no source populated** (Data-Parity Rule; sources eliminated pending calibration). Breakout weights 4 sub-signals (breakout age/school tier/college share/age trajectory) then S-curves; **per-sub-signal `Has*` presence flags so ABSENT ≠ a real extreme** (GLM B1 fix). Shared `scurve` (NaN/Inf-safe — GLM M1 fix) + `interp` + Data-Parity helpers (M17, RB/WR/TE reuse). Contract grew ("version the boundary") in 3 places: `engine.ScoutingInput` (off PlayerInput) + `Layer4Input.Scouting` + `Score(p,sc,c)`; `composition` (raw sub-signals, fail-loud, school-tier normalized at boundary); `harness` (fixtures + `rubrics()` registers QB real + **K identity PLACEHOLDER** — eval3C gates on both QB+K; real Madden K is B5b-K). Math verified vs QB_Rubric §5 (Daniels ≈1.050, Carr ≈1.039). **3C flips PENDING→PASS, suite 0 FAIL** (3A stays PENDING — also gates B5b-WR). depguard `engine-is-pure` proven over new pkg (planted import fired). GLM 5.2 BLIND: 2 real defects fixed pre-merge, 3 minors triaged non-defect. lint 0 / race green / functional gate passed live on Beelink. Next = B5b-DT (row 15). | Harness · B5a · B2b-Off | 4 | 2 |
| 15 | `[x]` | **B5b-DT** | **COMPLETE + merged to main 2026-06-27 (squash `c12bd98`).** First REAL DEFENSE Layer 4 + the architecture stress-test (AD-15: QB first, DT second). New pure pkg `internal/engine/l4/defense`: DT `Combined = film × RAS × breakout`. **ACTIVE RAS component** (±8% cap / steep 10 / raw÷10 / **rookie position-weight 1.00** — receding 0.50/0.10 schedule pends an NFL-data-stage input, "version the boundary") — the DT-vs-QB difference (QB's RAS is SL-020-forced to 1.000). **SL-005 film compression ±3%** (steep 10) but **Data-Parity neutral** (IDP film weights UNSET — sources eliminated pending redesign). Breakout weights 0.20/0.20/0.45/0.15 (college-share elevated to 0.45). **SL-021 Cushion Guard, BOTH halves:** L3 = `engine.ApplyCushionGuard` (sibling PURE fn, rides `Calibration.Cushion{RASThreshold,DeclineFactor}` consumed by `Pipeline.Score`, no-op when threshold 0 / factor out-of-range; composition ships 8.00/0.90 DT-only) + L4 = breakout Age-Trajectory cushion inside `DT.Apply` past peak 30. SL-019 EXCLUDED; NGS Coverage absent (CB/S-only). **M17:** shared `scurve`/`interp`/Data-Parity helpers extracted to new pure pkg `internal/engine/l4/curve`; offense refactored onto it. **"Version the boundary":** `Layer4Output.FilmRaw` (sandbox debug hook, case 3D) + `Calibration` cushion fields + `DT.PFFAlpha()` introspection (case 3G). Harness: DT registered (`rubrics()`+`realRegistry()`); **case 3F real + PASS — closes BOTH cushion halves** (L3 engine + L4 rubric via the registered DT); 3D/3G stay PENDING on co-gated positions (LB/WR, DE); suite 0 FAIL. Two contrasting DT fixtures differentiate on RAS+breakout. Math verified vs DT_Rubric (age 32/RAS 9 → age-traj 0.20→0.23). depguard `engine-is-pure` proven over defense pkg (planted import fired). **GLM 5.2 BLIND: M4 (3F only tested L3 half) + m2 (cushion factor range) + m3 (negative decay rate) fixed; M1/M2/M3 verified safe vs source (boundary guards); m1/m4/m5 deliberate carve-outs.** lint 0 / race green / functional gate passed live on Beelink. Next = B5b-RB (row 16). | Harness · B5a · B2b-Def | 4 | 2 |
| 16 | `[x]` | **B5b-RB** | **COMPLETE + merged to main 2026-06-27 (squash `f9875f6`).** Standard offense rubric in `engine/l4/offense` (rb.go): `Combined = film × RAS × breakout`, no SL-020/SL-021 exotica. **ACTIVE RAS, MEDIUM tier** (±4% cap / steep 8.0 / raw÷10 / **rookie position-weight 0.60 only** — SL-018 receding 0.30/0.06 schedule deferred, "version the boundary", same pattern as DT) — first non-High-tier RAS position (SL-004). **Film Data-Parity neutral** (offense film weights UNSET pending redesign). Breakout weights 0.35/0.20/0.30/0.15 (college workload elevated to 0.30); RB curves triaged vs RB_Rubric §4 (aggressive 20–21 breakout-age dropoff, peak-25 age trajectory). **TouchShare DEFERRED** (Christopher's call — boundary not grown; it lands with the film redesign that consumes it). **Case 3B (Herbert pattern) real + PASS** — converted `gatedPending`→`eval3B`; asserts the §7 claim ("L4 pulls below 1.00 when the scouting profile is genuinely thin") via a genuinely-thin vet (breakout-driven Combined ≈0.966) vs strong Bijan (≈1.073). The spec's literal Herbert pulls sub-1.0 only via FILM (§5), which is neutral this session, so 3B is asserted through the live breakout pathway §7 also describes — no assertion weakened (Christopher's gate call). 3A stays PENDING (also gates WR); suite 0 FAIL. depguard `engine-is-pure` proven over rb.go (planted used-import fired + reverted). **GLM 5.2 BLIND: 1 REAL defect fixed** — `schoolTierNorm` was position-blind (template 0.70/0.40/0.10) but RB_Rubric §4 specifies a SOFTER non-P4 penalty (0.75/0.45/0.15); now position-aware (RB branch, following the peakLimit/cushionGuard idiom), zero regression for QB/WR/DT (specs confirm template); withFilm false-positive triaged out vs source, two LOW findings consistent-by-design vs QB/DT. lint 0 / race green / functional gate passed live on Beelink (3B green, RB rankings differentiate, RB Bravo L4 < 1.0). **CARRY-FORWARD leads:** SL-018 L3 RAS-buffer (§3) is a Layer-3 mechanic, not built (out of L4 scope); RAS receding schedule + film weights pend their data/calibration. Next = B5b-WR (row 17). | Harness · B5a · B2b-Off | 4 | 2 |
| 17 | `[x]` | **B5b-WR** | **COMPLETE + merged to main 2026-06-27 (squash `448a2bf`).** The canonical standard-offense template (WR_Rubric §7). New pure file `engine/l4/offense/wr.go`: `Combined = film × RAS × breakout`. **ACTIVE RAS, HIGH tier** (±8% cap / steep 10.0 / raw÷10 / **rookie position-weight 1.00 only** — SL-018 receding 0.50/0.10 schedule deferred, "version the boundary"; AD-09 — **NOT forced to 1.000**, mirrors DT's RAS block not QB's SL-020). **Film Data-Parity neutral** (offense film weights UNSET — confirmed via Offense_Scouting_Source_Map §5 line 80 "0.00 — empty"). Breakout weights 0.40/0.25/0.20/0.15 (breakout age the gold-standard, highest weight of any position); curves triaged vs WR_Rubric §4 (breakout-age ≤19→1.00…≥22→0.10, target-share usage, peak-29 age trajectory). **School tier uses the TEMPLATE** (1.00/0.70/0.40/0.10 — boundary `schoolTierNorm` already position-aware from B5b-RB; **no WR branch added**, verified vs §4). **SL-019 EXCLUDED (SL-022)** — Layer 3 carries WR aging, breakout sub-signals use base curves (like QB, unlike DT's cushion). **Cases 3A + 3K real + PASS:** 3A (Lockett — "L4 stays non-negative for declining elite-draft vets") converted `gatedPending`→`eval3A`, asserts the SL-OQ-017 static-signals-offset-age claim on **BOTH WR AND QB** (the co-gate made meaningful — GLM fix) via the live breakout pathway (film neutral), the deliberate contrast to RB's Herbert (3B); 3K (S-curve clamp) asserts extreme/NaN/Inf stay in band through the registered WR rubric. 3D detail updated (WR registered, still PENDS on LB — correct three-state); suite 0 FAIL. WR registered in `rubrics()`+`realRegistry()`; three contrasting WR fixtures differentiate (Alpha elite / Bravo RAS-absent-neutral / Charlie thin). depguard `engine-is-pure` proven over wr.go (planted USED store import fired exact message + reverted). **GLM 5.2 BLIND: 1 REAL fixed** — `eval3A` required PosQB but never asserted on it (dead gate, masked regressions); now asserts the non-negative property on QB too. MED3 (SchoolUnset→0.0 floor) triaged FALSE-POSITIVE vs `composition.go:128` (`HasSchoolTier=false` for Unset → `curve.Present` returns neutral); MED4 (single-NaN via Present/Interp) triaged defense-in-depth vs `playerspec.go:95,104` fail-loud; RAS ~6.5 imputation = L1-wiring carry-forward; LOW comment-math fixed. lint 0 / race green / functional gate passed live on Beelink. **CARRY-FORWARD:** SL-018 receding RAS schedule + L3 RAS-buffer (§3) + offense film weights all pend their data/calibration stage; breakout three-zone classification (§4) not surfaced (no consumer). Next = B5b-TE (row 18). | Harness · B5a · B2b-Off | 4 | 2 |
| 18 | `[x]` | **B5b-TE** | **COMPLETE + merged to main 2026-06-27 (squash `6d7a1b3`).** Tight-end rubric — the FIRST **SL-019 RAS-modulator** instance. New pure file `engine/l4/offense/te.go`: standard offense template (`Combined = film × RAS × breakout`) + SL-019. **ACTIVE RAS, HIGH tier** (±8% cap) but **steepness 11.0 — STEEPER than WR's 10.0** (TE_Rubric §3, SL-004 amended Medium→High); rookie position-weight 1.00 only (SL-018 receding schedule deferred). **Film Data-Parity neutral.** Breakout weights 0.35/0.20/0.30/0.15 (same as RB; college usage 0.30); curves vs §4 (breakout-age ≤20→1.00…≥23→0.15, TE target-share usage ≤0.08→0.10/0.15→0.50/≥0.22→1.00, peak-29 age trajectory). **School tier = TEMPLATE** (no TE branch — `schoolTierNorm` already position-aware; verified §4). **SL-019 mechanic:** `modulated = base + (1−base)×0.35×RAS_norm` applied to the breakout-age AND age-trajectory sub-signals (symmetric, SL-OQ-024). Extracted as shared pure helper **`curve.SL019(base, rasNorm, strength, hasRAS)`** (M17 — DE/LB inherit; strength a param, not hardcoded). Absent/out-of-range RAS and non-finite base contribute nothing (Data-Parity + defense-in-depth); an absent breakout age stays neutral (not modulated — modulating the neutral would manufacture a lift for unknown data). Math verified vs §4 worked examples (breakout-age 22: 0.666/0.623/0.570; age-traj 32: 0.399/0.270), **pinned through the rubric's actual breakpoint tables** (age 32→0.10, breakout 22→0.50). **New case 3M (real + PASS):** asserts breakout rises with RAS (hi>lo>absent) AND that breakout-age and age-trajectory are EACH RAS-modulated in isolation AND that school/college are NOT modulated (negative contract) — non-vacuous. TE is the first SL-019 instance; **3E stays DE's canonical instance** (PENDS — TE does not gate it; correct three-state). Suite count 12→13, 0 FAIL. TE registered in `rubrics()`+`realRegistry()`; TE Alpha/Bravo athletic-vs-non-athletic vet fixtures reproduce the §5 Higbee/Henry structural finding (athletic profile keeps breakout up). depguard `engine-is-pure` proven over te.go (planted USED store import fired exact message + reverted). **GLM 5.2 BLIND: no hard defect** (math/clamping/Data-Parity all verified correct); acted on the real coverage leads — eval3M made non-vacuous (prove each sub-signal in isolation + negative contract), magnitude pinned through the curve tables, `curve.SL019` base-finiteness guard added (shared-helper defense-in-depth for DE/LB without a Scurve backstop). Triaged out vs source: "peak 29" prose (conflates the L3 peak limit with the L4 age-trajectory curve — no code issue; curve is monotonic decline as specced). lint 0 / race green / functional gate passed live on Beelink (3M green, TEs differentiate). **INFRA: added a `Bash(scp/ssh -i /root/.ssh/bird*)` allow rule to settings.json** — the data-exfil classifier hard-blocks the review dispatch even with the sandbox override; an explicit permission allow rule clears it (Christopher's call). **CARRY-FORWARD:** SL-018 receding RAS schedule (all positions, rookie-weight-only) + **TE L3 amplified age-buffer (0.30×RAS_norm, §3 — 3× WR's standard)** + offense film weights all pend their data/calibration stage; SL-OQ-024 (modulator symmetry) ships symmetric for TE. Next = B5b-DE (row 19). | Harness · B5a · B2b-Off | 4 | 2 |
| 19 | `[x]` | **B5b-DE** | **COMPLETE + merged to main 2026-06-27 (squash `202b0e0`).** Defensive-end / EDGE rubric — the **SECOND SL-019 instance** and the FIRST on defense. New pure file `engine/l4/defense/de.go`: standard-shaped defense rubric (`Combined = film × RAS × breakout`). **ACTIVE RAS, HIGH tier** (±8% cap / **steepness 10.0 — NOT TE's 11.0**, standard High-tier shape like WR/DT / raw÷10 / rookie position-weight 1.00 only — SL-018 receding deferred; AD-09 not forced). **Film standard ±5%** (NOT DT's SL-005 ±3% compression), Data-Parity neutral (IDP film weights unset). **NO SL-021 cushion** (DT-specific). Breakout weights 0.30/0.20/0.35/0.15 (College Production Share elevated 0.35 — sack+TFL market share, cleanest pre-NFL pass-rush signal); curves vs DE_Rubric §4 (breakout-age RB-shaped ≤19.5→1.00/20.0→0.80/20.5→0.50/≥21→0.15, college-share ≤0.12→0.15/0.20→0.55/≥0.28→1.00, peak-30 age trajectory). **School tier = TEMPLATE** (no DE branch — verified `schoolTierNorm` template + `peakLimit(PosDE)=30`). **SL-019 REUSE (the point of the build):** DE inherits the shared `curve.SL019(base, rasNorm, strength, hasRAS)` helper VERBATIM at strength 0.35 on breakout-age (present-only) + age-trajectory (always) — proving the M17 extraction. Six §4 worked examples verified (breakout-age base0.15: 0.447/0.373/0.274; age-traj: 0.675/0.410/0.146). **Case 3E converted `gatedPending`→real `eval3E`** (mirrors eval3M): asserts breakout rises with RAS (hi>lo>absent) AND each sub-signal RAS-modulated in isolation AND school/college NOT modulated (negative contract) — non-vacuous. Suite stays 13 (3E pre-existed, just flips), 0 FAIL; 3M (TE) still green; 3G stays PENDING (PFFAlpha-assertion wiring deferred — both DT+DE now registered). DE registered in `rubrics()`+`realRegistry()`; DE Alpha/Bravo athletic-vs-non-athletic fixtures reproduce the §5 Garrett/Lawrence finding. `cases.go` split → `cases_eval.go` (400-line cap). depguard `engine-is-pure` proven over de.go (planted USED `store/params` import fired exact message + reverted). **GLM 5.2 BLIND: 0 HARD defects** (recomputed all six worked examples by hand within 1e-3, confirmed eval3E non-vacuous + SL-019 reuse correct + boundary clean); closed 2 coverage leads — new **magnitude-through-Apply** test (pins SL-019 strength/raw÷10 divisor/RAS steepness-10-not-11 AT the call site, the standing "pin magnitude through the consumer" lesson) + college-share curve anchors. lint 0 / race green / functional gate passed live on Beelink (3E green, DEs differentiate). **EDGE routing (case 3J / SL-OQ-023) DEFERRED to B5b-LB** per Christopher's call ("version the boundary" — don't build the DE-vs-LB router until LB exists). **CARRY-FORWARD:** SL-018 receding RAS schedule + **DE L3 amplified age-buffer (0.30×RAS_norm, §3)** + IDP film weights pend their data/calibration stage; SL-OQ-029 college-share data source carried. Next = B5b-LB (row 20). | Harness · B5a · B2b-Def | 4 | 2 |
| 20 | `[x]` | **B5b-LB** | **COMPLETE + merged to main 2026-06-27 (squash `2be23f1`).** Linebacker rubric — the **FIRST non-SL-019 IDP rubric**; proves the Layer-4 architecture COMPOSES. New pure file `engine/l4/defense/lb.go`: standard-shaped rubric (`Combined = film × RAS × breakout`) built by REUSING two shipped idioms with NO new engine mechanic — DT's **SL-005 film compression** (cap ±3% / steepness 10.0, Data-Parity neutral) AND RB's **Medium-tier RAS** (±4% cap / **steepness 11.0 — NOT RB's 8.0** / raw÷10 / rookie position-weight 0.60 only — SL-018 receding deferred) — while applying **NEITHER SL-019 (excluded: Medium tier + scheme-driven longevity, SL-OQ-027 gating rule) NOR SL-021 cushion (DT-specific)**. RAS explicitly NOT elevated under SL-005 (§3 — inflating it would manufacture false confidence in scheme-mismatched athletes at the most scheme-dependent position). Breakout weights 0.25/0.20/0.40/0.15 (**College Production Share 0.40 — the HIGHEST of any position**; breakout age dropped to 0.25 — LBs break out later); curves vs LB_Rubric §4 (breakout-age ≤20→1.00/21→0.75/22→0.45/≥23→0.15, college-share ≤0.10→0.15/0.18→0.55/≥0.25→1.00, peak-29 age trajectory ≤25→1.00…29→0.50…≥33→0.00). **School tier = TEMPLATE** (no LB branch — verified `schoolTierNorm` template + `peakLimit(PosLB)=29`). Warner §5 worked examples verified by hand (film 0.914→1.029, breakout 0.788→1.046, RAS@6.5→1.01627). **EDGE classification routing SCOPED (case 3J, OQ-004/SL-OQ-030, Christopher's call):** new boundary resolver `composition.ResolveRubricPosition` routes the DE/LB ambiguous pair by `pass_rush_snap_share` (**≥0.50 → DE, else LB**) regardless of MFL tag; runs BEFORE Assemble so the resolved role drives BOTH rubric AND calibration (the role IS the position). New `PlayerSpec.PassRushSnapShare`/`Has*` fields + range validation; wired into `RankRookies` at the `reg[s.Position]` seam. **Cases 3D + 3J converted `gatedPending`→real `eval3D`/`eval3J`** in new `cases_eval_dispatch.go` (400-line cap split). 3D = SL-005 cross-position (LB & DT film equal + compressed, both below WR's ±5%, non-vacuous). 3J = routing over the §3J spec fixtures (75%→DE, 25%→LB, LB@80%→DE, LB@50%→DE ≥-boundary, share-less passthrough). Suite stays 13, 0 FAIL; 3G/3H/3I stay PENDING (correct). LB registered in `rubrics()`+`realRegistry()`; LB Alpha/Bravo fixtures + live EDGE-routing test. depguard `engine-is-pure` proven over lb.go (planted USED `store/params` import). **GLM 5.2 BLIND: 0 HARD defects** (recomputed all worked numbers by hand within tolerance); applied 4 leads (0.50 + low-share 3J fixtures, Warner film anchor at call site, code/comment contradiction fix at the ≥ boundary, narrowed clamp-test comment); triaged 1 (GLM misread cushionGuard no-op — `ApplyCushionGuard` gates `threshold<=0` first, existing comment correct). lint 0 / race green / functional gate passed live on Beelink ("pass" — 3D+3J flip green, LBs differentiate). **CARRY-FORWARD:** SL-018 receding RAS schedules (rookie-only across ALL positions) + L3 RAS-buffers (WR/RB/LB standard 0.10×RAS_norm, TE/DE amplified 0.30×RAS_norm) + IDP/offense film weights pend their data/calibration stage; SL-OQ-030 pass-rush threshold + SL-OQ-031/CAL-024/CAL-025 (LB college-share + SL-005 depth) carried. **Next = B5b-CB (row 21) — first NGS-anchor position (case 3I); confirm CB vs S sequencing.** | Harness · B5a · B2b-Def | 4 | 2 |
| 21 | `[ ]` | **B5b-CB** | `engine/l4/defense`. NGS anchor (**CB+S only**). SL-019 modulator. | Harness · B5a · B2b-Def | 4 | 2 |
| 22 | `[ ]` | **B5b-S** | `engine/l4/defense`. NGS anchor. SL-019 modulator. SL-OQ-035/036 carried. | Harness · B5a · B2b-Def | 4 | 2 |
| 23 | `[ ]` | **B5b-K** | `engine/l4/kicker`. All three components return 1.000; **`combine` yields 1.000, not special-cased** (AD-10). | Harness · B5a · B2b-K/A | 4 | 2 |

### Tier 2 — Output Store

| # | Status | Session | Scope | Gate | WF | L |
|---|---|---|---|---|---|---|
| 24 | `[ ]` | **B6 — Per-Season Output Store** | Persist engine outputs, `scoring_config_id` tagged. **Immutability both ways: append-only API + SQLite UPDATE/DELETE trigger** (AD-04, DECISION-010). | B5a · Sessions 14–23 | B6 | 2 |

### Tier 3/2 — First Module, then Transaction Engine

| # | Status | Session | Scope | Gate | WF | L |
|---|---|---|---|---|---|---|
| 25 | `[ ]` | **M1 — Asset Rankings** | 32-team rankings view + **per-team roster drill-down** (AD-20). First visible engine validation. Split **M1b** only if scope tightens. | B6 · B3c | 5 | 3 |
| 26 | `[ ]` | **B7a — Transaction Foundation + Coordinator** | `TransactionHandler` interface (designed to accept Phase-2 clock/snipe — AD-01); `deadcap/calculator.go`; **the sole-writer coordinator** (only `StateWriter`; atomic parameterized B3c write; handlers get `StateReader` only). Built + tested first (AD-02). | B6 · B3b · B3c | 6 | 2 |
| 27 | `[ ]` | **B7b — Acquisitions** | UFA, RFA, waivers. Validation + calc + formatted output. No live clock (Phase 2). | B7a · **OQ-009, OQ-010** | 6 | 2 |
| 28 | `[ ]` | **B7c — Trades** | Trades + trade block. Two-team cap, pick windows, Week-9 deadline hard block, format validation. DOT voting deferred (Phase 2). | B7a | 6 | 2 |
| 29 | `[ ]` | **B7d — Contract Mechanics** | Franchise tag, extension, restructure, buyout. Shared dead-cap/cap math. | B7a · **OQ-008** | 6 | 2 |

### Tier 3 — Remaining Modules

| # | Status | Session | Scope | Gate | WF | L |
|---|---|---|---|---|---|---|
| 30 | `[ ]` | **M4 — Transaction UI** | In-app transaction interface. Submissions route through B7 App methods only. Completes Phase 1 critical path. | B7a–d · B3c | 5 | 3 |
| 31 | `[ ]` | **M2 — Power Rankings** | Weekly power rankings view. | B6 · B3c | 5 | 3 |
| 32 | `[ ]` | **M5 — Free Agency Intelligence** | Free agent pool with scores. | B6 · B3c | 5 | 3 |
| 33 | `[ ]` | **M3 — Matchup Predictions** | Matchup score prediction view. | B6 · B3c | 5 | 3 |
| 34 | `[ ]` | **M6 — Rookie Draft Intelligence** | Rookie draft view + rankings. | B6 · B3c | 5 | 3 |
| 35 | `[ ]` | **M7 — Trade Analyzer** | Trade value analysis. | B6 · B3c · B7 | 5 | 3 |
| 36 | `[ ]` | **M8 — Commissioner Dashboard** | Commissioner tools, league health. | B7 · B6 · B3c | 5 | 3 |
| 37 | `[ ]` | **M9a — Engine Calibration UI** | B4 tuning surface; admin write path (not via B7 — AD-05). Christopher-only. | B4 | 5 | 3 |
| 38 | `[ ]` | **M9b — Commissioner Rules UI** | B3b governance surface; admin write path (not via B7 — AD-05). | B3b | 5 | 3 |

---

## Open Question Gates (must resolve before the named session)

| OQ | Topic | Blocks |
|---|---|---|
| OQ-004 | EDGE position mapping source | Session 4 (B3) |
| OQ-005 | Salary adjustment line item | Sessions 3–4 (B2, B3) |
| OQ-009 | RFA eligibility window | Session 27 (B7b) |
| OQ-010 | Playoff bid rules trigger | Session 27 (B7b) |
| OQ-008 | Franchise tag calculation timing | Session 29 (B7d) |

OQ-006 (cap tier calibration) and OQ-007 (scouting weight) are **post-live calibration, not build gates** — the engine runs on shipped B4 defaults.

---

## Conditional Splits (decide at scoping, not now)

- **M1b** — per-team roster view as its own session, only if M1 (Session 25) proves too large.
- **L2 sub-split** — `engine/scoring/{offense,defense,special}.go`, only if L2 exceeds 400 lines (checked before Session 12).

---

*Built by: Christopher Campbell + Claude Opus 4.8 (Anthropic). Audit basis: `session-3-audit-build-sequencing.md`.*
