HANDOFF — Session 2: B1 — MFL API Client (HTTP transport only)
Project: TheWarRoom · Stack: Go · Wails v2 · React+Tailwind+Zustand · SQLite WAL

== WHERE WE ARE ==
- Just completed: Session 1 (B0). Build green: yes (make lint 0 issues; go test -race green; full `wails build` produces the desktop binary). Branch merged: PENDING — session/b0-scaffold committed (0501ea3) but NOT yet merged to main. Merge waits on (a) agy First-Instance Template Review sign-off and (b) Beelink functional verification (headless CT105 can compile the GUI but not display it).
- Working tree: clean on session/b0-scaffold. Pre-commit hooks active and passing (golangci-lint-full, gitleaks, ifaceguard).
- This session's branch: session/b1-mfl-client (branch from main AFTER B0 merges).

== READ FIRST ==
- docs/build-handoffs/Build_Tracker.md (row 2, B1)
- Fable_TheWarRoom_code_plan.md §4 WF 1A (transport client skeleton — Do + DiscoverHost both sanctioned, Confidence-80 Gate 5 reconciliation) and §6 B1 brief
- docs/data-layer/MFL_API_Specification.md (League ID 14432; host discovered at runtime, never hardcode www47)
- SYSTEM_MAP.md (internal/mfl invariants) and AGENTS.md (layer rules)

== GATE CHECK (confirm before writing code) ==
- Upstream complete: B0. Verified: build-green yes; functional + agy sign-off PENDING — B0 must be fully closed and merged to main before B1 starts.
- Open questions that block this session: none. (OQ-005 blocks B2, not B1.)
- RISK-001 (rate limiting) and RISK-002 (host routing) live here.

== WHAT THIS SESSION BUILDS ==
- internal/mfl ALREADY EXISTS from pre-build friction testing (client.go, types.go) — built by agy, 10/10 on the lint rubric, 2 logic bugs found+fixed in T3 (commit d573420). It currently passes the full G0 overlay lint clean. B1 is NOT a greenfield build: it FORMALIZES this client to WF 1A and applies the 3 outstanding T4-review fixes:
    1. Stale comment at client.go:36 (verify line; the file changed since the review).
    2. Timer leak — replace any bare time.After/timer use with time.NewTimer + explicit Stop() in the backoff path.
    3. Add an rps > 0 guard in the rate-limiter constructor (reject non-positive rates).
- Add real tests: internal/mfl/client_test.go — table-driven, including a forced-429 backoff path and host-discovery recovery from a stale cached host (T3 finding #2).
- Public surface: Do(ctx, Request) (Response, error) + DiscoverHost(ctx, year, leagueID) — DiscoverHost MUST route through Do (inherits rate-limiting/backoff). No third exported surface without a plan amendment. No domain types.
- Layer: 1. Transport only — nothing here knows what a roster IS.

== CONSTRAINTS ACTIVE THIS SESSION ==
- Standards: <250-line target / 400 cap (make filelen); no hardcoded host/IP; context.Context first arg on Do/DiscoverHost (already so); close resp.Body (bodyclose).
- Architectural: NO application domain types, NO ingestion logic. depguard layer1-no-upward-import already forbids importing engine/store/transactions/api AND database/sql from internal/mfl — it will fail the build if violated.
- Anti-spaghetti: on a 429, back off (1→2→4…→60s) and return the error; the caller decides. Never retry-storm.

== CARRIED FROM LAST SESSION (B0 learnings) ==
- Package tree: internal/-rooted layout LOCKED (Christopher confirmed). The tree is law — see SYSTEM_MAP.md.
- modernc.org/sqlite DSN confirmed WORKING: `_pragma=busy_timeout(5000)&_pragma=journal_mode(WAL)&_txlock=immediate` (write pool, MaxOpenConns(1)); `_pragma=busy_timeout(5000)&mode=ro` (read pool). WAL verified under concurrent readers + single writer, zero SQLITE_BUSY (internal/db/pools_test.go).
- depguard fires correctly under module path github.com/secureprospective/TheWarRoom — proven by deliberate-violation test (sql-confined rule caught database/sql in a non-data package).
- Lint exceptions discovered + documented (only these are sanctioned): `//nolint:gochecknoglobals` for the go:embed assets var (main.go) and sentinel errors; `//nolint:containedctx` for the Wails app-lifetime ctx stored on the App struct (bound IPC methods get no per-call context).
- ifaceguard + filelen are wired into `make lint` and the pre-commit hook; all three gates proven to fire on deliberate violations, then scratch deleted.
- DI: the SQLite pools (internal/db.Pools) live on the App struct, opened in OnStartup, closed in OnShutdown. B1 does NOT open its own DB — it's transport only and imports no DB at all (depguard enforces).
- Toolchain installed on CT105: Wails v2.12.0, pnpm 10.34.3, libgtk-3-dev + libwebkit2gtk-4.0-dev + pkg-config (for GUI compile). go 1.26.4 pinned in go.mod.
- Open items carried: none blocking B1.

== CLOSE GATE FOR THIS SESSION ==
- Build green: make lint (0 issues) + go test -race ./internal/mfl/...
- Functional check: Do() against the live MFL league endpoint returns 200; a forced 429 exercises backoff (not a retry-storm); DiscoverHost recovers the league host and survives a stale cached host.
- Handoff: write Session 3's handoff (B2 — MFL Data Ingestion; gate OQ-005) before clearing.
