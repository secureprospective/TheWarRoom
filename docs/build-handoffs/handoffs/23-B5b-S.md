HANDOFF — Session 23: B5b-S (safety rubric — second NGS-anchor position + FOURTH SL-019 instance)
Project: TheWarRoom · Stack: Go · Wails v2 · React+Tailwind+Zustand · SQLite WAL

== WHERE WE ARE ==
- B5b-CB (row 21, squash `8eab1f9`) is MERGED. EIGHT real rubrics live: QB (SL-020-forced RAS),
  RB (Medium RAS), WR (High RAS), TE (High @ steep 11.0 + 1st SL-019), DT (SL-021 cushion + SL-005
  film ±3%), DE (High @ steep 10.0 + 2nd SL-019), LB (SL-005 film + Medium RAS @ steep 11.0, first
  non-SL-019 IDP + EDGE dispatch), CB (first NGS anchor + 3rd SL-019 @ REDUCED 0.30 strength).
- Shared `internal/engine/l4/curve` helpers: Scurve, Interp, SubSignal, Present, SL019. Defense pkg
  is `internal/engine/l4/defense` (dt.go/de.go/lb.go/cb.go). cb.go is the DIRECT template for S.
- Case status: 3A 3B 3C 3D 3E 3F 3I 3J 3K 3M GREEN; 3G/3H PENDING; 3L runs. Suite 13 cases, 0 FAIL.
- Case 3I ("NGS anchor present only at CB & S") is ALREADY GREEN and its eval3I loop ALREADY
  includes PosS — so registering S auto-EXTENDS the assertion to S with zero harness change (S must
  report HasNGSAnchor()=true; if it doesn't, 3I FAILS). No new case for S.

== WHY S IS NEXT (sequencing) ==
Build_Tracker row 22. S is the second (and last) NGS-anchor position and the FOURTH SL-019 instance.
The S_Rubric calls itself "structurally parallel to CB — same peak limit (28), same High tier, same
gating criterion, same modulator strengths (0.30/0.30/0.25)." This is the near-twin build: copy cb.go
to s.go, change the handful of S-divergent constants/curves, register, done. CB+S complete the IDP set;
after S only B5b-K (identity placeholder → real Madden-driven, row 23) remains in the L4 arc.

== WHAT B5b-S BUILDS (the DELTAS from cb.go — read S_Rubric.md, triage every number) ==
- New `internal/engine/l4/defense/s.go` (type S, NewS, Apply, HasNGSAnchor) mirroring cb.go. REUSE
  curve.SL019 at strength 0.30 (do NOT re-implement). Add HasNGSAnchor() bool { return true } — this
  is what flips/holds 3I for S (S is the SECOND NGS-anchor position, S_Rubric §2/§7).
- **RAS: HIGH tier, ±8%, inflection 0.50, but steepness 10.0** (NOT CB's 11.0 — S athletic profiles
  are more continuous across the box-to-deep spectrum than CB's bimodal boundary/slot, §3). Rookie
  weight 1.00 only. AD-09 not forced.
- **Film: STANDARD ±5%, steepness 12.0, Data-Parity neutral** (coverage film weights unset — same as
  CB). NGS is a dedicated 0.30 anchor INSIDE the film composite (§2) — invisible at the engine
  boundary, asserted only via HasNGSAnchor (3I), exactly like CB.
- **Breakout: ±5% cap, steepness 11.0** (NOT CB's 10.0 — S college production data is cleaner, §4).
  Weights **0.20/0.25/0.40/0.15** (same as CB: breakout-age 0.20 / school 0.25 / college-share 0.40 /
  age-traj 0.15).
  - Breakout-age curve: IDENTICAL to CB (≤19.5→1.00, 20.5→0.75, 21.5→0.45, ≥22.5→0.15).
  - **College Production Share = INT + TACKLE market share** (NOT CB's PD + INT — safeties are more
    tackle-involved, §4), and the threshold is LOWER: **≤0.08→0.15, 0.14→0.55, ≥0.20→1.00** (CB was
    0.08/0.16/0.24 — safeties have lower absolute market-share ceilings).
  - Age-trajectory curve: IDENTICAL to CB (peak 28: ≤24→1.00, 25→0.85, 26→0.70, 27→0.55, 28→0.50,
    29→0.35, 30→0.20, 31→0.10, ≥32→0.00). `peakLimit(PosS)=28` already set in defaults.go (verify).
  - SL-019 strength 0.30 on breakout-age (present-only) AND age-trajectory (always) — SAME as CB.
- **School tier = TEMPLATE** (no S branch — verify schoolTierNorm template path + no S case needed).
- **No SL-021 cushion** (DT-specific; cushionGuard(PosS) already returns (0,0) — verify).
- Verify §5 worked examples by hand at strength 0.30 (Kyle Hamilton push age 24 all-maxed; the
  age-34 pull-attempt — the Lockett pattern's 8th instance amplified by SL-019).

== THE HARNESS ==
- Register S in `harness_app.rubrics()` AND `realrubric_test.go realRegistry()` (add domain.PosS).
- 3I needs NO change — eval3I already loops {PosCB, PosS} and asserts every registered NGS position
  reports the anchor; S registering simply makes the S branch live. Add TestRealSRegistryHolds3I
  (3I still PASS with S registered, suite 0 FAIL) — same shape as TestRealCBRegistryFlips3I but the
  point is S now co-asserts. (No PENDING→PASS flip for S; 3I is already green from CB.)
- NO new suite case for S's SL-019 (proven by s_test.go unit worked-examples at strength 0.30 +
  ranking differentiation), same call as CB. Suite stays 13.
- Add S Alpha / S Bravo fixtures to fixtures.go (athletic-vs-non-athletic vet, like CB Alpha/Bravo) +
  TestRealSRankingDifferentiates. Pick free MFLIDs (CB used 0601/0602; S can use 1001/1002 or check
  fixtures.go for the next free block — do NOT collide).
- s_test.go mirrors cb_test.go: ImplementsLayer4, HasNGSAnchor (+ negative: DT/DE/LB/CB... wait CB
  DOES have it now — assert the NON-NGS controls DT/DE/LB do NOT, and CB+S DO), SL019WorkedExamples
  at 0.30, CurvesReproduceSpecAnchors (the S-divergent college-share 0.14/0.20 + breakout steep 11.0),
  RASActive (steep 10.0 — pin it distinct from CB's 11.0), FilmStandardBand, SL019LiftsBreakout,
  MagnitudeThroughApply (pin RAS steep 10.0 AND breakout steep 11.0 AND SL-019 0.30 at the call site —
  the standing "pin magnitude through the consumer" lesson), ComponentsClampedToCap.

== GATE CHECK (confirm with Christopher before writing code) ==
1. Sequence: S next (recommended — completes the NGS pair + IDP set), then B5b-K (row 23).
2. S = High-tier RAS at steepness 10.0 (NOT CB's 11.0) + standard ±5% film + SL-019 strength 0.30 +
   breakout steepness 11.0 (NOT CB's 10.0) + College Production Share = INT+Tackle (≤0.08/0.14/≥0.20)
   + peak 28. Confirm the two steepness swaps vs CB are intentional (they are, per §3/§4).
3. 3I already green + already includes S — no flip, S just co-asserts. No new suite case. Confirm.
4. SafetyRole (free vs box safety, SL-OQ-035/036) stays SCHEMA-only / NOT a rubric split in v1.0 —
   one S rubric scores all safeties (the schema's SafetyRole is reserved for a later split). Confirm.

== CONSTRAINTS ACTIVE THIS SESSION ==
- No work on main; branch session/b5b-s. Never git --no-verify.
- CT105: export PATH=$PATH:/usr/local/go/bin; go build ./...; GOMEMLIMIT=1500MiB GOGC=20 make lint
  (repo ROOT); go test -race ./... . Beelink GUI: wails dev -tags webkit2_41.
- Files < 400 lines (AD-17). Engine stays PURE — defense pkg imports only engine + curve; depguard
  engine-is-pure must stay green (PROVE with a planted USED store import that REFERENCES a real
  exported symbol, e.g. `var _ = params.ValueType("")` — a blank `_` trips revive first and masks
  depguard; run the proof, do NOT commit it; do NOT add a vacuous empty DepguardPure test — GLM
  flagged that on CB, no other rubric has one).
- Confidence/raw values INTERNAL — never on Layer4Output/Result (FilmRaw is debug-only; HasNGSAnchor
  is a RUBRIC method, not a Layer4Output field — same as CB/DT.PFFAlpha).
- Lint gotcha: never name a param/var `cap` (use capBand). Avoid a bare `switch` on domain.Position
  unless you enumerate ALL members (exhaustive linter).
- Every custom mechanic proven by a planted failure (M3); shared logic REUSED not copied (M17 — S
  must reuse curve.SL019 at 0.30). Pin MAGNITUDE through Apply, not just the helper.
- Review gate: GLM 5.2 (OpenCode on bird), BLIND, output is LEADS — triage every finding vs source.
  INLINE a self-contained prompt AND the composition/playerspec/defaults BOUNDARY files. Run:
  scp -i /root/.ssh/bird /tmp/prompt.md x@192.168.1.195:/tmp/ ; ssh -i /root/.ssh/bird x@192.168.1.195
  'opencode run -m zai/glm-5.2 "$(cat /tmp/prompt.md)"'. scp/ssh have a standing settings.json allow
  rule. GLM track record: CB 0 / LB 0 / DE 0 / TE 0 / WR 1 / RB 1 / DT 3 / QB 2 — earns its keep,
  still triage. (GLM has misread no-op guards + over-flagged debug passthroughs — verify vs code.)

== CARRIED FORWARD — leads, not blockers ==
- SL-018 receding RAS position-weight schedules ship the ROOKIE weight only across ALL positions.
- L3 RAS-buffers (Layer-3 age-pull): WR/RB/LB 0.10×RAS_norm, TE/DE 0.30×, CB/S 0.25×RAS_norm — all
  Layer-3 mechanics, NOT built. Build at the L3 / data-wiring session.
- Coverage/IDP film weights UNSET (calibration pass) — film stays Data-Parity neutral everywhere,
  including CB's + S's 0.30 NGS anchor (the composite source is unpopulated in v1.0).
- 3G (DT PFFAlpha assertion-wiring) still gatedPending. 3H (confidence floor) needs component
  confidence inputs on Layer4Input — not built.
- SL-OQ-035/036 (SafetyRole free-vs-box split) + CAL-028/CAL-029 (S-NGS bundle) pend the pipeline /
  calibration session. SL-OQ-033/034 + CAL-026/027 (CB) likewise.
- The scouting [0,1]-normalized schema vs "engine normalizes from raw" reconciliation pends the
  real-data-wiring session.
- After S: B5b-K (row 23) flips the K identity PLACEHOLDER to a real Madden-driven rubric
  (DECISION-011, 0.60 Madden / 0.40 NFLProduction; combine yields a REAL value, no longer 1.000).
  Then B6 (output store, row 24) — Tier 2 L4 arc complete, first persistence layer.

== CLOSE GATE FOR THIS SESSION ==
- go build + make lint 0 + go test -race green; engine depguard green (planted USED import, run not committed).
- S constants/curves verified vs S_Rubric §2/§3/§4 (esp. the CB-divergent: RAS steep 10.0, breakout
  steep 11.0, college-share INT+Tackle ≤0.08/0.14/≥0.20); S reuses curve.SL019 at 0.30 (confirm by
  reading s.go vs cb.go — no re-implementation).
- TestSMagnitudeThroughApply pins RAS steep 10.0 + breakout steep 11.0 + SL-019 0.30 AT the call site.
- 3I stays GREEN with S registered (S co-asserts the NGS anchor); suite stays 13, 0 FAIL. S Alpha
  separates from S Bravo in Module 1.
- GLM 5.2 BLIND review (sliced, inlined incl. boundary files; pin magnitude through Apply); triage.
- Functional gate: Christopher operates the harness — 3I stays green, Ss differentiate. Squash-merge
  after he confirms. Then write handoff 24 (B5b-K).
