HANDOFF — Session 24: B5b-K (kicker rubric — flips the identity PLACEHOLDER to a real Madden-driven Layer 4; LAST L4 build)
Project: TheWarRoom · Stack: Go · Wails v2 · React+Tailwind+Zustand · SQLite WAL

== WHERE WE ARE ==
- B5b-S (row 22, squash `4209026`) is MERGED. NINE real rubrics live: QB (SL-020-forced RAS),
  RB (Medium RAS), WR (High RAS), TE (High @ steep 11.0 + 1st SL-019), DT (SL-021 cushion + SL-005
  film ±3%), DE (High @ steep 10.0 + 2nd SL-019), LB (SL-005 film + Medium RAS @ steep 11.0, first
  non-SL-019 IDP + EDGE dispatch), CB (1st NGS anchor + 3rd SL-019 @ 0.30), S (2nd NGS anchor + 4th
  SL-019 @ 0.30, RAS steep 10.0 / breakout steep 11.0 / INT+Tackle college share). **K is the ONLY
  position still on the identity PLACEHOLDER** — `engine.IdentityLayer4()` in rubrics()+realRegistry().
- Shared `internal/engine/l4/curve` helpers: Scurve, Interp, SubSignal, Present, SL019, NeutralNorm.
- Case status: 3A 3B 3C 3D 3E 3F 3I 3J 3K 3M GREEN; 3G/3H PENDING; 3L runs. Suite 13 cases, 0 FAIL.
- Case 3C ("SL-020 — QB & K Layer-4 RAS forced to exactly 1.000") is ALREADY GREEN and gates on
  BOTH QB and K being registered. The K identity placeholder satisfies it today (all components
  1.000). B5b-K replaces the placeholder with a rubric whose FILM is active but whose RAS is STILL
  forced to 1.000 — so 3C MUST STAY GREEN (K's RAS stays exactly 1.000). Verify, do not break it.

== WHY K IS NEXT / WHAT MAKES IT DIFFERENT ==
Build_Tracker row 23 — the LAST L4 build (completes the Tier-2 L4 arc; B6 output store, row 24, is next).
K is NOT a copy of any defense/offense rubric. It is the DECISION-011 reversal of the old "structural
exclusion" model:
- OLD (superseded): all three K components hardcoded to 1.000 (flat exclusion); the identity
  placeholder is literally this.
- NEW (DECISION-011, Christopher 2026-06-19, PINNED): K's **FILM component is ACTIVE**, the ONLY
  position where film is Madden-majority. **RAS stays forced to 1.000 (SL-020 partial)** and
  **Breakout stays forced to 1.000** (no breakout framework at K). So `Combined = film × 1.000 ×
  1.000 = film`. **This reverses AD-10** ("K combine yields 1.000, not special-cased") — film now
  moves K's valuation.

== ⚠️ THE RUBRIC BODY ON DISK IS STALE — READ THE RIGHT SOURCE ==
`docs/scoring-engine/K_Rubric.md` §2/§3/§4 STILL DESCRIBE THE OLD EXCLUSION MODEL (film_cap 0.00,
film_steepness 0.0, "Film output hardcoded to 1.000", "Madden archival only — not Layer 4
valuation"). **DO NOT code from those numbers.** The header of K_Rubric.md carries a supersession
pointer; the AUTHORITATIVE source is **docs/scoring-engine/Scouting_Schema.md → DECISION-011**:
- K Film ACTIVE, sub-signal split **PINNED: Madden 0.60 / NFLProduction 0.40**.
- RAS still excluded (SL-020); no film/breakout sub-signals beyond Madden+NFLProduction.
- **DEFERRED TO THIS BUILD (calibration, not data-shape):** the Film component's **cap + curve**
  (`film_cap` / inflection / steepness) — UNSPECIFIED in any doc. This is the gate-check decision.

== WHAT B5b-K BUILDS ==
- New pure file `internal/engine/l4/kicker/k.go` (NEW PACKAGE `kicker` under engine/l4 — verify the
  depguard `engine-is-pure` list covers `engine/l4/kicker`; if depguard is path-scoped to existing
  pkgs, add the new pkg to the allow rule, mirroring how `defense`/`offense` are listed).
  - type K, NewK, Apply. Combined = film × 1.000 × 1.000.
  - **Film = S-curve over a Madden 0.60 / NFLProduction 0.40 composite** at the cap/steepness
    Christopher pins (gate-check). Data-Parity neutral when BOTH sub-signals absent (film → 1.000).
    Use curve.Scurve + the Has*-gated SubSignal/Present helpers (M17 — reuse, do not re-implement).
  - **RASEffective hardcoded to exactly 1.000** (SL-020 — like QB's forced RAS; an unbypassable
    const, RAS unread). This is what keeps case 3C green. Mirror qb.go's SL-020 treatment.
  - **BreakoutEffective hardcoded to exactly 1.000** (no breakout framework at K).
  - NO HasNGSAnchor (K is not a coverage position). NO SL-019. NO SL-021.
- Check `engine.ScoutingInput` for the Madden + NFLProduction fields (MaddenFilm is a universal
  core field per DECISION-011; verify NFLProduction has an input field + Has* flag, or add it
  "version the boundary"-style if the K film composite needs it and it is not yet carried).

== THE HARNESS ==
- Replace `domain.PosK: engine.IdentityLayer4()` with `domain.PosK: kicker.NewK()` in BOTH
  harness_app.rubrics() AND realrubric_test.go realRegistry().
- **Case 3C must STAY GREEN** — eval3C asserts QB & K RAS forced to exactly 1.000. Add/extend a test
  proving K's RASEffective is exactly 1.000 through Apply even with Madden/NFLProduction populated
  (the active-film path must not leak into RAS). Suite stays 13, 0 FAIL.
- Add K Alpha / K Bravo fixtures to fixtures.go (high-Madden vs low-Madden kicker) +
  TestRealKRankingDifferentiates — K Alpha (strong Madden kick power/accuracy) out-scores K Bravo
  on the Layer-4 Combined (which == film, since RAS+breakout are 1.000). Pick the next free MFLID
  block — CB used 0601/0602, S used 0901/0902; K can use 1001/1002 (verify no collision in fixtures.go).
- k_test.go: ImplementsLayer4, RASForcedToOne (exactly 1.000 — the SL-020 proof, like QB),
  BreakoutForcedToOne, FilmActiveMaddenMajority (0.60/0.40 split pinned by a worked example through
  Apply — pin the cap+steepness AND the 0.60/0.40 weights at the call site, the standing
  "pin magnitude through the consumer" lesson), FilmDataParityNeutral (both sub-signals absent →
  film 1.000 → Combined 1.000), ComponentsClampedToCap (M3 planted failure).

== GATE CHECK (confirm with Christopher BEFORE writing code) ==
1. Sequence: K next (recommended — last L4 build, completes the arc), then B6 (output store, row 24).
2. **THE film cap + steepness + inflection** — UNSPECIFIED anywhere (DECISION-011 explicitly defers
   "how strongly Layer 4 may move K's valuation" to this build). Christopher must pin: film_cap
   (±?% — other positions use ±5% standard / ±3% SL-005 compressed; K is sparse-data so a CONSERVATIVE
   cap, e.g. ±3%, is defensible — propose ±3% + steepness 10.0 + inflection 0.50 as the starting
   recommendation, but this is HIS call). Without these numbers the rubric cannot be coded.
3. Sub-signal split Madden 0.60 / NFLProduction 0.40 — already PINNED (DECISION-011). Confirm.
4. RAS + Breakout stay forced to exactly 1.000 (SL-020 partial / no breakout framework); only Film
   is active; Combined = film. Confirm — and confirm 3C must STAY green (K RAS still 1.000).
5. AD-10 reversal acknowledged (K combine no longer flat-1.000; film moves it) — confirm the
   Build_Tracker/architecture-decisions note for AD-10 should be updated to record the DECISION-011
   reversal at close.

== CONSTRAINTS ACTIVE THIS SESSION ==
- No work on main; branch session/b5b-k. Never git --no-verify.
- CT105: export PATH=$PATH:/usr/local/go/bin; go build ./...; GOMEMLIMIT=1500MiB GOGC=20 make lint
  (repo ROOT); go test -race ./... . Beelink GUI: wails dev -tags webkit2_41.
- Files < 400 lines (AD-17). Engine stays PURE — new kicker pkg imports only engine + curve;
  depguard engine-is-pure must stay green (PROVE with a planted USED store import referencing a real
  exported symbol, e.g. `var _ = params.ValueType("")` — a blank `_` trips revive first and masks
  depguard; run the proof, do NOT commit it; do NOT add a vacuous empty DepguardPure test — GLM
  flagged that on CB).
- Confidence/raw values INTERNAL — never on Layer4Output/Result (FilmRaw is debug-only).
- Lint gotcha: never name a param/var `cap` (use capBand). Avoid a bare `switch` on domain.Position
  unless you enumerate ALL members (exhaustive linter — note PosK is the last unhandled member in
  some switches; B5b-K may surface an exhaustive gap that the identity placeholder hid).
- Every custom mechanic proven by a planted failure (M3); shared logic REUSED not copied (M17).
  Pin MAGNITUDE through Apply, not just the helper.
- Review gate: GLM 5.2 (OpenCode on bird), BLIND, output is LEADS — triage every finding vs source.
  INLINE a self-contained prompt AND the qb.go SL-020 reference + boundary files. Run:
  scp -i /root/.ssh/bird /tmp/prompt.md x@192.168.1.195:/tmp/ ; ssh -i /root/.ssh/bird x@192.168.1.195
  'opencode run -m zai/glm-5.2 "$(cat /tmp/prompt.md)"'. scp/ssh have a standing settings.json allow
  rule. GLM track record: S 0 / CB 0 / LB 0 / DE 0 / TE 0 / WR 1 / RB 1 / DT 3 / QB 2.

== CARRIED FORWARD — leads, not blockers ==
- SL-018 receding RAS position-weight schedules ship the ROOKIE weight only across ALL positions
  (N/A at K — RAS forced to 1.000).
- L3 RAS-buffers (Layer-3 age-pull) for every position: all Layer-3 mechanics, NOT built. K peak
  limit = 30 (verify peakLimit(PosK) in composition/defaults.go).
- Coverage/IDP/offense film weights UNSET (calibration pass) — film stays Data-Parity neutral
  everywhere EXCEPT K once this build activates K's Madden+NFLProduction film.
- 3G (DT PFFAlpha assertion-wiring) still gatedPending. 3H (confidence floor) needs component
  confidence inputs on Layer4Input — not built.
- SL-OQ-035/036 (S SafetyRole split) + CAL-028/029 (S/CB NGS+college-share) pend calibration.
- SL-OQ-041 (FG 70+ scoring), SL-OQ-042 (Madden K pipeline — v1.0 rec: keep K in routine pulls),
  CAL-032 (Madden K archival utility) — K-specific, pend pipeline/calibration.
- AFTER K: the entire L4 rubric set (10/10 positions) is real. **B6 (output store, row 24)** —
  Tier-2 L4 arc complete, FIRST persistence layer for scores.

== CLOSE GATE FOR THIS SESSION ==
- go build + make lint 0 + go test -race green; engine depguard green over the new kicker pkg
  (planted USED import, run not committed).
- K Film constants verified vs DECISION-011 (Madden 0.60 / NFLProduction 0.40) + Christopher's
  pinned cap/steepness; RAS + Breakout proven forced to exactly 1.000 through Apply.
- Case 3C STAYS GREEN with the real K rubric (K RAS still exactly 1.000); suite stays 13, 0 FAIL.
- K Alpha separates from K Bravo on Combined (== film). TestKFilmMagnitudeThroughApply pins the
  0.60/0.40 split + cap + steepness at the call site.
- GLM 5.2 BLIND review (sliced, inlined incl. qb.go SL-020 reference + boundary files); triage.
- Functional gate: Christopher operates the harness — 3C stays green, Ks differentiate by Madden.
  Squash-merge after he confirms. Update AD-10's note to record the DECISION-011 reversal. Then
  write handoff 25 (B6 — output store).
