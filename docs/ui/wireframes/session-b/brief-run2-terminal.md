# SESSION B — Component Hierarchy & Typography · RUN 2

You are the conceptual design lead for "TheWarRoom" — a 32-team dynasty fantasy-football command console (Go + Wails desktop, React + Tailwind, dark-only, desktop-first). The design target is Anduril, not SaaS: a command console that costs more than a house. Dark, precise, data-dense without a pixel of waste. Confident hierarchy. Controls that look like they actuate real hardware. The UI must communicate capability before the user clicks anything.

You have creative latitude and you are expected to use it — push past tired dashboard patterns. But two rules bind you: (1) the locked-context below is LOCKED — Session A's spatial system is CONFIRMED and is not yours to redesign; if you believe a locked decision is wrong, write a section titled RIPCORD ITEMS stating the decision, your objection, and your alternative — do not design around it silently. (2) Ground every proposal in the real surfaces and real data shapes in the facet map below — no invented features.

Technical floor: WebKitGTK webview (motion = transforms/opacity only, cheap compositing — no heavy filters/blurs on large surfaces; the Beelink APU compositor has locked up on expensive paint), CSS custom properties carry ALL tokens, virtualized lists, Tailwind-expressible.

Standing architectural fact: a future Chat engine will have COMPLETE control of this application — a terminal over a Guix-style simple command language, with a lightweight LLM translating user language into commands. You are not designing that chat box, but every mechanical control you propose must be conceivable as a command verb + arguments that the GUI merely projects (a Command Ledger maps each control to a verb). If you propose an interaction with no plausible command equivalent, flag it yourself.

Standing design law — the spectrum commitment: this one product must delight a 1–2-league casual AND a 15–25-league hardcore portfolio manager. Three rules bind everything you propose: (1) light, snappy, speedy — every control acknowledges in <100ms, motion is feedback not decoration, nothing may feel heavy; (2) layered engagement — every surface must read at three altitudes (glance / operate / interrogate) with depth DISCOVERED, never demanded: the casual is never shown configuration to get started, the expert is never capped; (3) configurability lands as one-click presets to adopt, never toggles to study — defaults must be excellent because casuals never configure. If a proposal serves one end of the spectrum by taxing the other, redesign it.

## Locked context — Session A CONFIRMED spatial system (build ON this, do not redesign)
- **Base unit 4px.** Scale 4 › 8 › 12 › 16 › 24 › 32 › 48 › 64. No off-scale values (no 2/6/10px). This is your typographic and component rhythm too.
- **Elevation is optical only:** 1px hairline borders + inset bevels. NO dropshadows, NO blurs (WebKitGTK cheap-paint floor). A "raised" actionable tile = 1px inset top/left highlight; on `:active` it swaps to a 1px bottom/right inset (depressed micro-switch). This is the confirmed actuation language — extend it, don't replace it.
- **Fixed 4-column instrument shell:** nav rail (192px, [PORTFOLIO SECTOR] top ≤160px + [MODULE SECTOR] below) / fluid workspace / contextual inspector (fixed **320px**, slides in via `transform: translateX` as a pure overlay — NOT a width-trade) / comms (right-edge 48px idle strip, summons over at 320px). Calendar summons as a generous top-right overlay. Inside the rigid zones, data bleeds to the edges; 1px gray-800 hairlines + 12px gutters are the only breathing room.
- **Density tiers Narrative / Tactical / Matrix ride CSS custom properties ONLY; Tactical is default.** Confirmed row heights: Narrative 48px, Tactical 32px, Matrix 22px. There is NO "Pro Mode" toggle ever — the density tiers + a curiosity trigger ARE the progression.
- **Color:** one meaning per color (green=elite/positive, blue=good/info, amber=warning/watch, red=danger, gray=muted). You are NOT setting values this session (that is Session C) — work in GREYSCALE. Use weight, size, and position for hierarchy, never hue.
- **M1 board confirmed treatment:** 32px module header + 36px sticky filter sub-header (pins on scroll, 68px fixed overhead); Adjusted Score + Base Score dominate (wider, brighter gray-50), supporting context recedes (gray-500); 32px Tactical rows; active row gets a 2px LEFT-axis inset border; hover highlights the full row across the workspace bleed.
- **Panel edge-resize (Ledger A12, Christopher-named):** the inspector/nav dividers are drag-resizable — 1px ghost guide during drag, commits width on RELEASE (no live per-frame reflow). You OWN the handle anatomy + its 4 interaction states this session.

## Facet map — real surfaces & data shapes this session must honor
- **M1 Asset Rankings board** (the dense archetype). Real columns: **Rank, Player, Pos, Franchise, Base, Adjusted Score, Salary** — plus a position filter (QB/RB/WR/TE/… chips) and a view toggle (Global / By franchise / Cap efficiency). 32 franchises, ~500+ rostered players. Plausible row: `4 · Jahmyr Gibbs · RB · Motor City Maulers · 71.20 · 88.40 · $42.10`.
- **M2 Power Rankings board:** a franchise table with a live weight slider (0–100%) + sum/top-N toggle; columns include franchise, a blended power score, MFL all-play%, PA. 32 rows.
- **Contextual Inspector** (320px): populates on ANY entity click, no navigation. Anatomy to design: **engine score dominant, layer-breakdown bars (the 6 scoring layers), contract block, notes block.**
- **Transaction Workspace / Trade Builder / League Controls:** franchise rail → roster → per-player action panel; the ConfirmModal is the commit gate for every priced/destructive op (WAIVER/SIGN/TAG/EXTENSION/BUYOUT/TRADE/commissioner ops).
- **Home / League Landing:** 2×2 card grid + seasonal cards (calendar-triggered), at Narrative density.
- Data states are REAL and must be designed, not hand-waved: a fresh install (no league loaded), an in-flight MFL fetch, an MFL-unreachable failed refresh, and offseason emptiness.

## Your charge — define the component language (answer with COMMITTED anatomy, named values)
1. **Type scale** — how many steps, exact px, the weight that carries hierarchy when size is compressed (max ~24px, single sans like Inter, monospace where numbers align/columns). What a section label looks like at 11px. Numbers are the hero of this app — commit to the numeric treatment (tabular-figures, alignment, how Adjusted Score reads bigger than Salary).
2. **Table / row anatomy** — how a 32-row ranked board stays scannable at Matrix (22px). Row structure, column alignment rules, the sortability affordance, the selected/hover/active states (extend the confirmed inset-bevel language). The glance/operate/interrogate reads of ONE player row: what a casual gets in half a second (name + one number + a color slot), what the operator gets (the Tactical columns), what it opens into (inspector).
3. **The deferred M1 column spec** — which of the 7 columns show at which density tier, alignment per column, which are sortable, what the sort-active header looks like. Do the same shape for M2 and a roster table.
4. **The Contextual Inspector anatomy** — the 320px panel: engine-score-dominant header, the 6-layer breakdown bars, contract block, notes block. Its three altitudes.
5. **Card anatomy** — the Home 2×2 card at Narrative, and the seasonal card. How a card differs from a table row in the same system.
6. **Button + form-control language** — the 4 interaction states for every control class (rest / hover / pressed / disabled), with <100ms pressed acknowledgment as a HARD rule (pressed is instant; async work never blocks the pressed feedback). Text inputs, selects, chips, the weight slider, toggles.
7. **The ConfirmModal** — the single most important component: the commit gate for destructive league ops. It must feel like ARMING and FIRING a system — honestly, without theater. Define its anatomy, the arm→fire actuation path, and how a preview (dollar breakdown / rejection reason) sits inside it.
8. **Empty / Loading / Error anatomy** — ONE component class, applied everywhere, answering four honest states: **no data yet** (fresh install — a casual's first five minutes are almost all empty-state, so emptiness must feel intentional, oriented, inviting — never broken), **fetch in flight** (skeleton language, NOT spinners), **MFL unreachable** (failed refresh — what the surface says, what stays usable from cache), **seasonal emptiness** (an offseason matchup view is EMPTY BY NATURE not by failure — design the difference).
9. **The global keyboard map** — one page: what every key does, per density tier (Matrix locks J/K/Enter/T as the floor; Tactical a working subset; Narrative near-zero). Each keymap row names its future Command Ledger verb, so keyboard + GUI + future chat terminal are three faces of one command surface.
10. **The edge-resize handle** — its 4 interaction states (idle / hover / dragging-with-ghost / released-commit), consistent with the pressed-microswitch language.

## Provocation seed for THIS run
**"TERMINAL-FORWARD"** — type does all the work, chrome approaches zero. There are almost no boxes, buttons, or borders; hierarchy is carried entirely by the type system — weight, size, tabular alignment, and monospace columns — the way a well-set terminal or a Bloomberg screen reads without a single container. A row is not a card in a box; it is a line of type that aligns. Controls are text that actuates. Density comes from removing chrome until only the data and its labels-as-type remain. Push this hard, then be honest in RIPCORD about where zero-chrome fails the casual's first five minutes and the ConfirmModal's need to feel like a physical commit gate.

## Output
An opinionated design-direction document in markdown, **target ≤250 lines**. Commit to specifics — named px, named weights, named states, named anatomy — not mood language. Flag uncertainty explicitly rather than guessing. End with a **RIPCORD ITEMS** section (or "none").
